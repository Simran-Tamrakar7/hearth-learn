/** Playwright manual Part 0 — Background & Context */
export const chapters = [
  {
    id: "pw-0-what",
    title: "0. What is Playwright, Really",
    minutes: 35,
    level: "beginner",
    phase: "Part 0 · Background & Context",
    partName: "Part 0 · Background & Context",
    overviewText: "Playwright was built by ex-Puppeteer engineers at Microsoft who started fresh in January 2020 — multi-browser, auto-waiting, and designed for modern SPAs from day one.",
    why: "Interviewers expect you to explain Playwright's origin and why it differs from Selenium and Puppeteer — not just list features.",
    when: "Read before installing Playwright or comparing it to Cypress/Selenium in architecture reviews.",
    practical: { app: "SaaS dashboard — tool evaluation", scenario: "Senior engineer asks why Playwright ships its own browser binaries instead of using installed Chrome.", pass: "You explain patched builds expose automation hooks, version-matched via playwright install, and eliminate driver mismatch.", fail: "You say Playwright is 'faster Selenium' without naming CDP architecture or the Puppeteer lineage." },
    advantages: ["Ex-Puppeteer team applied lessons from Chrome-only CDP limitations", "January 2020 release designed for SPAs, shadow DOM, and async rendering", "MIT-licensed with Microsoft-funded dedicated engineering team", "Fast 2–4 week release cadence with low abandonment risk", "Purpose-built patched browser binaries — no stock Chrome dependency", "Auto-waiting and tracing built into core design from the start"],
    limitations: ["Younger than Selenium — fewer enterprise war stories in regulated industries", "Patched browsers may differ subtly from user-installed Chrome builds", "JS-first docs require translation to Python throughout this manual", "Microsoft backing doesn't guarantee every edge case gets fixed immediately", "Multi-language bindings mean Python API occasionally lags JS features", "Origin story knowledge alone doesn't substitute for hands-on practice"],
    contentMarkdown: "## 0. What is Playwright, Really\n\n### History — built by Microsoft, evolved from the Puppeteer team\n\nThe core engineers who built Playwright — Andrey Lushnikov, Pavel Feldman, and Boris Yankov — previously worked at Google on Puppeteer, the Node.js library for controlling headless Chrome. When they moved to Microsoft, they didn't try to patch Puppeteer's limitations — they built something new from scratch, applying everything they'd learned. Puppeteer was built specifically for Chrome/Chromium via the Chrome DevTools Protocol (CDP), and its creators knew exactly where that architecture broke down — mainly, it couldn't reliably control Firefox or Safari/WebKit. Playwright was designed from day one to fix that: one API surface that talks to all three major browser engines. It first released publicly in January 2020, making it young next to Selenium (which dates to 2004) — that youth is a selling point, since Playwright was designed with full knowledge of modern web apps (SPAs, shadow DOM, complex async behavior) instead of being retrofitted onto assumptions from the mid-2000s web. Playwright also doesn't use the actual shipped Chrome/Firefox/Safari binaries — it uses patched, purpose-built builds of Chromium, Firefox, and WebKit that expose extra automation hooks not available in consumer browsers, which is why it installs its own browser binaries (playwright install) rather than reusing whatever's already on your machine. Open-source, actively maintained, backed by Microsoft. Playwright is MIT-licensed and fully open-source on GitHub. \"Backed by Microsoft\" means a dedicated, funded engineering team rather than volunteers on a side project; a fast release cadence (new minor versions roughly every 2–4 weeks, usually adding real capabilities); and integration hooks into the Microsoft dev ecosystem (VS Code extension, Azure DevOps pipeline support). Practically, this means when you hit a rough edge, the odds it gets fixed soon are much higher than with a stagnant tool — and it's a plus for your own job security in this skillset, since companies are less nervous adopting a tool with strong institutional backing. Microsoft also uses Playwright internally across its own products (e.g., parts of Office/Teams web testing), so the team is dogfooding it at scale — bugs found internally often get fixed before external users even hit them.\n\n### Why it was created — gaps in Selenium/Puppeteer\n\nThree concrete gaps drove Playwright's creation: Selenium's flakiness problem (its WebDriver protocol requires manually managed waits — miss one and your test fails randomly or hits the wrong element because the page hadn't finished rendering); Puppeteer's Chrome-only limitation (cross-browser testing meant a separate toolchain for Firefox/Safari, so most teams didn't bother and non-Chrome bugs shipped undetected); and poor support for modern web patterns (SPAs, shadow DOM, deeply nested iframes, apps making dozens of async network calls before finishing rendering). Playwright's answer to all three: auto-waiting built into the core engine, native multi-browser support from day one, and locators/APIs specifically designed to pierce shadow DOM and handle iframes without special ceremony. A fourth gap worth naming is debuggability — Selenium failures historically gave a generic stack trace and little else, while Playwright was built with tracing, screenshots, and video baked in from the start, because the team had personally felt the pain of debugging flaky CI failures with almost no evidence to go on.",
    customSummary: "## 0. What is Playwright, Really\n\n- Built by ex-Puppeteer engineers (Google → Microsoft) who rebuilt from scratch instead of patching Puppeteer.\n- Released Jan 2020 — young, designed for modern web (SPAs, shadow DOM) from day one; uses purpose-built patched browser binaries, not stock Chrome/Firefox.\n- MIT-licensed, open-source, Microsoft-funded team → fast release cycle (2–4 weeks), low abandonment risk, dogfooded internally at Microsoft.\n- Created to fix 3 core gaps: Selenium's manual-wait flakiness, Puppeteer's Chrome-only scope, poor support for SPAs/shadow DOM/iframes — plus a 4th, debuggability (tracing/screenshots/video built-in from the start).",
    chapterNum: 1,
  },
  {
    id: "pw-1-where",
    title: "1. Where Playwright is Used",
    minutes: 30,
    level: "beginner",
    phase: "Part 0 · Background & Context",
    partName: "Part 0 · Background & Context",
    overviewText: "Playwright fits E2E user-journey tests, regression suites, API setup via APIRequestContext, visual regression, web scraping, and cross-browser CI — clustering in frontend-heavy and dedicated QA automation teams.",
    why: "Knowing where Playwright gets used — and where it doesn't — prevents recommending it for load testing or native mobile.",
    when: "Read when scoping a test strategy or answering when would you pick Playwright over Cypress in an interview.",
    practical: { app: "HRM system", scenario: "Team wants API seeding before UI tests without switching tools.", pass: "Propose APIRequestContext in the same pytest project for setup calls.", fail: "Suggest a separate Postman collection with manual cookie copying." },
    advantages: ["E2E specs mirror real user journeys with auto-waiting", "APIRequestContext shares pytest project — no context switching", "Built-in to_have_screenshot() — no third-party visual plugin", "Cross-browser via single CLI flag or pytest parameter", "Web scraping reuses same Browser → Context → Page model", "Strong fit for dedicated QA teams with Python polyglot stacks"],
    limitations: ["API testing less flexible than dedicated httpx/REST Assured suites", "Visual regression needs baseline maintenance discipline", "Web scraping use case is out of manual scope", "Mobile emulation is web-only — not native app testing", "Industry adoption still growing versus Selenium in legacy enterprises", "Cross-browser CI requires three engine installs via playwright install"],
    contentMarkdown: "## 1. Where Playwright is Used\n\nWeb UI test automation (functional, regression, E2E). This is the primary use case and the one this manual focuses on almost entirely — functional testing (does a specific feature work as intended), regression testing (do previously-working features still work after a change), and end-to-end testing (simulating a full real user journey across multiple pages/features, not just one isolated piece). API testing. Playwright isn't just a browser tool — it ships APIRequestContext, letting you send raw HTTP requests (GET/POST/PUT/DELETE) without a browser at all. One tool covers both your UI layer and your backend API layer, instead of maintaining Playwright for UI and a separate tool (Postman/requests) for APIs. It also enables a practical hybrid pattern: using API calls to quickly set up test state (create a user, seed data) and only using the slower UI layer to test the actual feature under test. Visual regression testing. Beyond \"does the button work\" — this asks \"does the page still look right?\" Playwright can screenshot a page and pixel-diff it against a saved baseline, catching things functional tests miss entirely, like a CSS change making text invisible or a layout shift pushing a button off-screen. Web scraping / data extraction. Because Playwright fully renders JavaScript-heavy pages (unlike simple HTTP-request scrapers), it's also popular for scraping sites that load content dynamically. A side use case, not this manual's focus — but worth knowing it exists, since it sometimes comes up in interviews or side projects. Cross-browser compatibility testing. Running the identical suite against Chromium, Firefox, and WebKit to catch browser-specific bugs before real users do. This is what the multi-browser API makes possible in practice — you write the test once and just re-target the browser engine. Industries: e-commerce, SaaS, banking, healthcare. E-commerce checkout/payment flows are directly tied to revenue, so a broken \"Buy Now\" button costs money by the minute. SaaS ships UI changes constantly, often daily, so manual regression alone can't keep pace. Banking has heavy compliance/audit requirements that make documented, repeatable, automated test evidence often mandatory. Healthcare carries similar compliance pressure, plus accessibility requirements that are often legally required rather than nice-to-have. Worth adding EdTech (frequent releases, wide device variety among students) and travel/booking platforms (complex multi-step flows like date pickers, filters, and payment) as well. Relevance to your Bizlevate work: an HRM system is closer to SaaS/internal tooling — useful framing for pitching automation value internally, since it means fewer manual regression passes on Payroll/Attendance/Leave every release cycle.",
    customSummary: "## 1. Where Playwright is Used\n\n### Primary use: Web UI automation — functional, regression, E2E testing\n### API testing via APIRequestContext — same tool for UI + backend; also used to seed test data fast\n### Visual regression testing — pixel-diffing against baselines to catch layout/CSS bugs\n### Web scraping — secondary use case, handles JS-rendered pages well\n### Cross-browser testing — one suite run against Chromium/Firefox/WebKit\n- Heavy industries: e-commerce, SaaS, banking, healthcare (+ EdTech, travel/booking) — driven by revenue risk, release velocity, or compliance.\n- Bizlevate angle: HRM ≈ SaaS/internal tool → automation reduces manual regression load on Payroll/Attendance/Leave per release.",
    chapterNum: 2,
  },
  {
    id: "pw-2-capabilities",
    title: "2. What Playwright Can Do",
    minutes: 32,
    level: "beginner",
    phase: "Part 0 · Background & Context",
    partName: "Part 0 · Background & Context",
    overviewText: "Playwright runs Chromium, Firefox, and WebKit with auto-waiting, network interception, multi-tab/context support, mobile emulation, headless/headed modes, trace recording, and pytest-xdist parallelization.",
    why: "This chapter is your capability map — interviewers ask what Playwright can do beyond basic click-and-type.",
    when: "Read before Part 2 hands-on chapters. Revisit when scoping advanced features like route mocking or device emulation.",
    practical: { app: "E-commerce checkout", scenario: "Dev needs to test empty-cart API response without backend changes.", pass: "Use page.route() to fulfill mock JSON before goto.", fail: "Add time.sleep(3) and hope the real API returns empty." },
    advantages: ["Multi-browser via one API — chromium, firefox, webkit", "Auto-waiting on every action and expect() assertion", "page.route() mocks network without backend changes", "Multiple Page objects per context for multi-tab flows", "Device descriptors emulate mobile viewport and touch", "Trace and video capture for post-mortem CI debugging"],
    limitations: ["@playwright/test runner is JS-only — Python uses pytest-playwright", "Mobile emulation tests mobile web not native apps", "Video capture is opt-in unlike some tools' default recording", "Parallel execution requires pytest-xdist setup", "Network mocking adds test complexity if overused", "Headless vs headed timing can differ subtly on slow CI agents"],
    contentMarkdown: "## 2. What Playwright Can Do\n\nAutomates Chromium, Firefox, and WebKit through one API. Write test logic once, swap the browser_type (chromium, firefox, webkit) to run the same test against a different engine — no rewriting locators or logic. Genuinely rare: most tools need separate driver setups per browser; Selenium historically needed different WebDriver binaries with occasional locator inconsistencies between them. Mobile web emulation (no real device needed). Built-in device descriptors (e.g., \"iPhone 13\", \"Pixel 5\") auto-set viewport size, user-agent, touch support, and device pixel ratio, giving realistic mobile-web testing without a device lab. Important boundary: this emulates mobile web browsers, not native mobile apps.\n\n### Auto-waiting — no manual sleep/wait hacks\n\nThe single most-cited reason teams switch to Playwright. Before every action (click, fill, etc.), Playwright automatically waits for the target element to be attached to the DOM, visible, stable (not mid-animation), enabled, and able to receive events. If any condition isn't met within the timeout, it fails with a clear error explaining which condition wasn't met — dramatically easier to debug than Selenium's generic \"element not interactable\" error. Network interception & mocking. page.route() intercepts any request the page makes and lets you fulfill it, modify it, abort it, or pass it through unchanged. This unlocks scenarios otherwise hard to trigger reliably: simulating a server error, a slow/timing-out API, or an empty-data state — all without needing the real backend to cooperate. Multi-tab, multi-context, multi-user simulation in one test. Each BrowserContext is fully isolated (separate cookies, storage, cache), so you can open two contexts in the same test to simulate two different logged-in users hitting the same feature simultaneously (e.g., a real-time chat or shared document editor). Older tools required two completely separate browser sessions/processes for this. Headless & headed execution. Headless means no visible browser UI — faster, and required in most CI environments. Headed means you see the actual window — invaluable while writing/debugging a new test. Same test code, just flip a launch option.\n\n### Built-in test runner in JS/TS — Python uses pytest-playwright instead\n\nPlaywright's official JS/TS package includes its own test runner (@playwright/test) with built-in parallelization, fixtures, and reporting. The Python version has no equivalent built-in runner — the community-maintained pytest-playwright plugin fills that role, which is why this manual leans so heavily on pytest concepts (fixtures, conftest.py) rather than a Playwright-native config system. Trace/video/screenshot capture out of the box. No third-party plugin needed — Playwright can record a full trace (DOM snapshots, network activity, console logs, screenshots at each step), plus optionally a video and screenshots on failure. This built-in observability is a major reason debugging CI failures in Playwright is noticeably less painful than in older tools. Parallel test execution. Tests run across multiple worker processes simultaneously instead of sequentially, cutting total suite runtime. In Python this is handled via pytest-xdist rather than anything Playwright-native.\n\n### Codegen — record-and-generate\n\nPlaywright ships a playwright codegen tool — open a browser, click around manually, and Playwright generates the equivalent script in real time. Great for scaffolding a first draft of a test or quickly finding a working locator, even though hand-tuning is almost always needed afterward. Web-first assertions via expect(). Unlike a plain assert, Playwright's expect() retries automatically until the condition is true or times out — so assertions themselves benefit from the same auto-waiting philosophy as actions. Geolocation, permissions, and device emulation beyond viewport. Playwright can simulate GPS coordinates, grant/deny browser permissions (camera, notifications, location), throttle network conditions, and emulate color-scheme (dark/light mode) or timezone/locale — useful for testing region- or permission-dependent features.",
    customSummary: "## 2. What Playwright Can Do\n\n### One API automates Chromium, Firefox, WebKit — no per-browser rewrite\n- Mobile web emulation via device descriptors (not native apps).\n### Auto-waiting before every action — biggest flakiness fix vs Selenium\n### Network interception/mocking via page.route() — simulate errors, slow APIs, empty states\n- Multi-context = isolated sessions in one test → simulate multiple simultaneous users.\n### Headless (fast, CI) vs headed (debugging) — same code either way\n- JS/TS has built-in test runner; Python relies on pytest-playwright.\n### Built-in trace/video/screenshot capture — no plugins needed\n- Parallel execution: native in JS/TS, via pytest-xdist in Python.\n- Extras: codegen (record → script), retrying expect() assertions, geolocation/permissions/network/timezone emulation.",
    chapterNum: 3,
  },
  {
    id: "pw-3-why-pw",
    title: "3. Why Companies Choose Playwright Over Alternatives",
    minutes: 28,
    level: "beginner",
    phase: "Part 0 · Background & Context",
    partName: "Part 0 · Background & Context",
    overviewText: "Teams choose Playwright for CDP speed, auto-waiting reliability, native cross-browser support, modern web patterns, and strong debugging — with honest trade-offs for Cypress-invested JS teams and Selenium Grid enterprises.",
    why: "Tool selection answers need scenario-based reasoning, not abstract Playwright is more flexible.",
    when: "Read before tool-selection meetings or interviews.",
    practical: { app: "Enterprise HRM with Okta SSO", scenario: "Login redirects through Okta domain and back.", pass: "Note Playwright handles cross-origin transparently — weigh team Python skills.", fail: "Claim identical cross-origin handling to Cypress cy.origin()." },
    advantages: ["Persistent WebSocket CDP — faster than WebDriver HTTP round-trips", "50–80% flake reduction reported by Selenium migrators", "get_by_role survives CSS refactors via accessibility tree", "WebKit on Linux CI catches Safari bugs without Mac hardware", "Trace Viewer + Codegen + VS Code extension reduce MTTR", "Python bindings suit polyglot teams with existing Python stacks"],
    limitations: ["JS-only Cypress teams may not gain enough from switching", "Selenium Grid migration has real infrastructure cost", "Playwright job postings still fewer than Selenium in some regions", "Speed advantage matters less on trivial smoke suites", "Enterprise BrowserStack contracts may delay migration ROI", "Honest scoping requires naming k6 and Appium complements"],
    contentMarkdown: "## 3. Why Companies Choose Playwright Over Alternatives\n\nSpeed vs Selenium. Playwright talks to the browser directly over CDP/WebSocket, a lower-overhead path than Selenium's WebDriver protocol, which adds an extra HTTP layer (the WebDriver server) between your test code and the browser. Reliability vs Selenium. This comes almost entirely from auto-waiting. A Selenium suite without carefully hand-tuned explicit waits produces intermittent failures unrelated to real bugs — just timing. Teams migrating to Playwright commonly report their flaky-test rate dropping substantially, simply because the waiting problem is handled by the framework instead of by every individual test author. Modern web app support (SPAs, shadow DOM, iframes) vs older tools. Selenium and older Puppeteer-era approaches predate SPAs (React/Angular/Vue apps without full page reloads) and shadow DOM (encapsulated web components) being the norm. Playwright's locator engine was built with these patterns in mind — it pierces shadow DOM by default and has first-class frame_locator() support for iframes, rather than needing the workarounds older tools demanded. Lower flakiness due to auto-waiting. Worth repeating deliberately — it's the recurring theme of the whole manual. If there's one concept to have rock-solid early on, it's this one. Growing job market demand. Playwright adoption has grown fast in recent years, and job postings mentioning it (vs. Selenium-only postings) have trended upward. It's increasingly what QA automation job descriptions actually ask for, which is part of why this manual exists in your learning path. Setup simplicity.\n\n```bash\npip install pytest-playwright && playwright install\n```\n\ngets you a fully working multi-browser environment in minutes — no separate driver downloads, no version-matching browser-to-driver headaches (a classic Selenium pain point where WebDriver version had to exactly match the installed browser version).\n\nConsistent, unified API across languages. Playwright supports Python, JS/TS, Java, and .NET with a near-identical API shape across all of them, so teams with mixed-language stacks can share testing knowledge/patterns more easily than with Selenium, where idioms vary more by language binding. Cost. Fully free and open-source, with no paid tier required to access core functionality — cloud execution grids like BrowserStack are optional add-ons, not a requirement to use Playwright itself.",
    customSummary: "## 3. Why Companies Choose Playwright Over Alternatives\n\n- Faster than Selenium (direct CDP vs WebDriver's extra HTTP layer).\n### More reliable due to built-in auto-waiting — fewer timing-based flaky failures\n- Better native support for SPAs, shadow DOM, iframes than older tools.\n### Rising job market demand — increasingly a named requirement in QA postings\n- Easier setup (no driver/version matching), consistent API across Python/JS/Java/.NET, fully free/open-source.",
    chapterNum: 4,
  },
  {
    id: "pw-4-not-covered",
    title: "4. What This Manual Will NOT Cover",
    minutes: 20,
    level: "beginner",
    phase: "Part 0 · Background & Context",
    partName: "Part 0 · Background & Context",
    overviewText: "This manual excludes @playwright/test JS runner, native mobile (Appium), and load testing (k6/JMeter). Python + pytest-playwright only; mobile web emulation is in scope.",
    why: "Scope honesty prevents mis-selling Playwright for Appium or k6 workloads.",
    when: "Read at start. Revisit when asked for mobile app or load tests.",
    practical: { app: "Native mobile HRM app", scenario: "Product wants iOS App Store automation in Playwright.", pass: "Explain viewport emulation vs native — recommend Appium.", fail: "Attempt native mobile automation in Playwright." },
    advantages: ["Clear boundaries save sprint time and interview credibility", "Python focus avoids split-brain JS/Python test infrastructure", "Mobile web vs native distinction named explicitly", "k6/JMeter named as correct load-testing complements", "JS doc translation skill is teachable mechanical work", "Walking away from wrong tool is senior-engineer signal"],
    limitations: ["No @playwright/test or playwright.config.ts coverage", "Appium remains separate skill to learn for native mobile", "Load testing requires additional tool investment", "Official docs are JS-first — constant translation overhead", "Stakeholders may want one tool for all testing needs", "Scope limits may disappoint teams seeking universal automation"],
    contentMarkdown: "## 4. What This Manual Will NOT Cover\n\nJavaScript/TypeScript Playwright. This manual is Python-focused. A huge amount of Playwright's own official docs and community content is written JS/TS-first, since that's Playwright's native language and where the built-in test runner lives. You'll often find JS examples online and need to mentally translate syntax to Python — this manual sticks to Python + pytest-playwright throughout, with no translation guide included. Mobile native app testing. Mobile web emulation is in scope — testing a website as it renders in a mobile browser. Testing an actual native iOS/Android app is a fundamentally different problem requiring Appium, which automates the OS-level app itself rather than a browser. Relevant since you've already completed a full Appium roadmap separately. Load/performance testing. Playwright automates one browser session behaving like one real user — it's not built to simulate thousands of concurrent users hitting a server to measure throughput/latency under load. That's a separate discipline with its own dedicated tools: k6 (modern, scriptable, popular in CI pipelines), JMeter (older, GUI-heavy, still widely used in enterprises), and Locust (Python-based, code-first). If load testing comes up in your Bizlevate work, treat it as a separate learning track. Deep security/penetration testing. Playwright can be used in support of some security-adjacent checks (e.g., verifying an auth bypass doesn't work, checking headers), but it's not a security scanning tool — dedicated tools like OWASP ZAP or Burp Suite cover that discipline properly. Unit testing / component-level testing frameworks. Playwright operates at the browser/E2E layer. Testing an individual function or a UI component in isolation is typically the job of tools like pytest (for backend units), Jest, or React Testing Library — Playwright complements these rather than replacing them. Non-Playwright MCP/AI-agent browser automation tools. The manual's addendum on Playwright MCP is scoped narrowly to Playwright's own MCP server — general AI-agent browser tooling outside that isn't covered.",
    customSummary: "## 4. What This Manual Will NOT Cover\n\n### Python + pytest-playwright only — no JS/TS Playwright coverage\n### No native mobile app testing — that's Appium's domain (already covered separately)\n### No load/performance testing — that's k6/JMeter/Locust's domain, treat as separate track\n- Also out of scope: deep security/pen testing (ZAP/Burp), unit/component testing (pytest/Jest/RTL), general AI-agent browser tooling beyond Playwright's own MCP server.",
    chapterNum: 5,
  },
  {
    id: "pw-5-pyramid",
    title: "5. The Testing Pyramid & Where UI Automation Fits",
    minutes: 25,
    level: "beginner",
    phase: "Part 0 · Background & Context",
    partName: "Part 0 · Background & Context",
    overviewText: "The testing pyramid places many fast unit tests at the base, fewer integration tests in the middle, and few slow UI tests at the top — Playwright belongs at the E2E peak, not as a replacement for unit tests.",
    why: "Misplacing Playwright as your only test layer creates slow, brittle suites that erode team trust.",
    when: "Read when designing test strategy or defending E2E budget to engineering leadership.",
    practical: { app: "Bizlevate HRM", scenario: "Manager wants Playwright tests for every utility function.", pass: "Redirect pure logic to unit tests; reserve Playwright for user journeys.", fail: "Write E2E tests for date-formatting helpers." },
    advantages: ["Pyramid framing justifies E2E budget with clear ROI", "Unit tests catch logic bugs faster than any browser tool", "Integration tests cover API contracts without UI overhead", "Playwright E2E validates critical user journeys end to end", "Smoke subset strategy scales pyramid top sustainably", "Honest pyramid talk signals mature test-strategy thinking"],
    limitations: ["Pyramid is a model not a strict formula — context varies", "Teams under-invest in unit tests despite pyramid advice", "E2E tests remain inherently slower than lower layers", "Over-automation at pyramid top creates maintenance burden", "Stakeholders may resist anything below E2E visibility", "Pyramid alone doesn't solve flaky-test culture problems"],
    contentMarkdown: "## 5. The Testing Pyramid & Where UI Automation Fits\n\nUnit tests form the wide base. Fast, cheap, isolated — testing one function/method with no external dependencies (no browser, no database, no network). Because they're so fast and cheap to run, you want many of them. Integration/API tests form the middle. These test how multiple units work together, or test a service's API layer directly without a UI. Slower than unit tests, faster than UI tests — Playwright's APIRequestContext lives here. UI/E2E tests form the narrow top. These test the full system through the actual browser UI — the slowest, most expensive, and most brittle layer, since a UI test can break because of an unrelated CSS class rename, not just a real bug. This is where Playwright's browser automation primarily lives. Inverting the pyramid is a known anti-pattern. If you go heavy on UI tests and light on unit tests, you get a suite that's slow to run, expensive to maintain, and where a single small change can break dozens of tests for reasons unrelated to actual functionality — often called \"ice cream cone\" testing. Playwright is a top-of-pyramid tool by design. That's fine as long as it isn't asked to do the whole pyramid's job alone. The healthiest use is reserving it for cases that genuinely need a real browser — critical user journeys, cross-browser concerns, visual checks — not validating every possible input to a form field one at a time. Practical implication for Bizlevate. Reserve Playwright E2E tests for core critical paths (e.g., can an employee successfully submit and get approval on a leave request end-to-end) rather than trying to E2E-test every validation rule on every field — push those smaller checks to a cheaper test layer where possible.",
    customSummary: "## 5. The Testing Pyramid & Where UI Automation Fits\n\n- Unit tests (many, fast, cheap) → Integration/API tests (fewer) → UI/E2E tests (fewest, slow, brittle).\n### Inverting the pyramid = slow, fragile suites — \"ice cream cone\" anti-pattern\n- Playwright is a top-of-pyramid tool — best for critical end-to-end journeys and cross-browser/visual checks, not exhaustive field-level validation.\n- Bizlevate application: E2E-test core flows (e.g., leave-request-to-approval); push granular checks to a lower, cheaper test layer.",
    chapterNum: 6,
  },
  {
    id: "pw-6-when-automate",
    title: "6. When (and When Not) to Automate",
    minutes: 28,
    level: "beginner",
    phase: "Part 0 · Background & Context",
    partName: "Part 0 · Background & Context",
    overviewText: "Automate high-frequency regression on stable features and critical business paths. Hold off on exploratory testing, changing UIs, and subjective visual judgment.",
    why: "Automating too early on unstable features produces abandoned tests that erode suite trust.",
    when: "Read before writing your first spec. Revisit when prioritizing which Bizlevate modules to cover.",
    practical: { app: "Bizlevate HRM", scenario: "New Appraisal UI is under active redesign.", pass: "Hold automation until UI stabilizes; automate stable Attendance clock-in/out now.", fail: "Write full E2E suite for a module changing weekly." },
    advantages: ["ROI framework prevents wasted automation effort", "Stable-feature criterion reduces maintenance churn", "Critical-path focus aligns tests with business risk", "Exploratory testing stays human — not forced into scripts", "Data-driven scenarios are strong automation candidates", "Bizlevate module examples make criteria concrete"],
    limitations: ["Stability judgment requires product roadmap visibility", "Holding off automation can look like QA inaction to managers", "Critical-path definition varies across stakeholders", "Manual testing cost is hard to quantify for ROI math", "Automating too late misses early regression value", "One-off flows rarely justify automation investment"],
    contentMarkdown: "## 6. When (and When Not) to Automate\n\nAutomation needs to earn its cost. Not every test case should be automated — this is a common early-career misconception. Automation has real upfront and ongoing cost (writing it, maintaining it when the UI changes, debugging failures), so it needs to be justified by return on that investment. Good candidates: high-frequency regression checks. Anything re-run every release/sprint. If you'd manually test it more than a few times, automate it. Good candidates: stable features. UI/flow that isn't expected to change drastically soon. Automating a feature mid-redesign means rewriting the automation almost immediately. Good candidates: critical business paths.\n\n### Login, checkout, payroll run, leave approval — where a failure is high-cost if missed\n\nGood candidates: data-driven scenarios. The same flow tested with many input variations (e.g., leave request with different date ranges/leave types) — tedious manually, cheap once automated. Good candidates: cross-browser/cross-device checks. Manually re-testing the same flow across 3 browsers every release doesn't scale; automation does this trivially. Poor candidates: one-off or exploratory testing. Testing something once, or open-ended exploratory testing looking for unexpected bugs, doesn't benefit from a fixed automated script. Poor candidates: highly volatile UI. A screen still being actively redesigned means automation would need constant rewriting, costing more than it saves short-term. Poor candidates: purely visual/subjective judgment calls. \"Does this look aesthetically good\" isn't something a Playwright assertion can meaningfully check — visual regression testing catches unexpected changes, not subjective quality. Poor candidates: low-frequency, low-risk features. A rarely-used admin setting that almost never breaks may not be worth the automation investment. A useful mental framework. Automation pays off when (cost of manual testing) × (frequency of testing) exceeds (cost to write) + (cost to maintain) the automated version over time. New teams often automate too early on unstable features and then abandon the tests — worse than not automating at all, since broken/ignored tests erode trust in the whole suite. Practical implication for Bizlevate. A stable, frequently-regression-tested flow like Attendance clock-in/out is a strong automation candidate. A brand-new Appraisal module UI still under active iteration might be better left to manual testing until it stabilizes.",
    customSummary: "## 6. When (and When Not) to Automate\n\n- Automation needs positive ROI (writing/maintenance cost vs. repeated manual-testing cost).\n- Good candidates: high-frequency regression, stable features, critical business paths, data-driven scenarios, cross-browser/device checks.\n- Poor candidates: one-off/exploratory testing, still-changing UI, subjective visual judgment, rarely-used low-risk features.\n### Automating too early on unstable features often backfires — broken/abandoned tests erode trust\n- Bizlevate application: automate stable flows (e.g., Attendance clock-in/out); hold off on modules under active redesign (e.g., new Appraisal UI).",
    chapterNum: 7,
  },
  {
    id: "pw-7-ecosystem",
    title: "7. The Playwright Tooling Ecosystem",
    minutes: 25,
    level: "beginner",
    phase: "Part 0 · Background & Context",
    partName: "Part 0 · Background & Context",
    overviewText: "Playwright's ecosystem spans Inspector, Trace Viewer, Codegen, VS Code extension, pytest-playwright, pytest-xdist, Playwright MCP, and enterprise cloud grids — each solving a distinct workflow problem.",
    why: "Knowing which tool to reach for — Inspector vs Trace Viewer vs Codegen — saves hours of debugging.",
    when: "Read before your first CI failure. Revisit when setting up local dev workflow.",
    practical: { app: "CI pipeline", scenario: "Test fails on CI but passes locally.", pass: "Open trace.zip in Trace Viewer to inspect DOM and network at failure step.", fail: "Re-run locally 20 times hoping to reproduce." },
    advantages: ["Inspector for live step-through debugging during development", "Trace Viewer for post-hoc CI failure diagnosis", "Codegen records interactions and exports Python locators", "VS Code extension enables in-editor run and locator picking", "pytest-playwright bridges pytest fixtures to browser lifecycle", "pytest-xdist enables free parallel execution without paid cloud"],
    limitations: ["Codegen output needs cleanup — not production-ready as-is", "Trace files add CI artifact storage if retained broadly", "Playwright MCP is emerging — APIs may shift", "Cloud grids (BrowserStack etc.) add cost beyond open-source core", "Allure/HTML reporting requires separate plugin setup", "Tool proliferation can overwhelm beginners choosing where to start"],
    contentMarkdown: "## 7. The Playwright Tooling Ecosystem\n\nCore library. playwright / pytest-playwright is the browser automation engine itself, covered throughout Parts 1–4. Playwright Inspector. A GUI debugging tool that lets you step through a test line-by-line, pause execution, and interactively explore locators. Trace Viewer. A post-run visual timeline of everything that happened in a test (DOM snapshots, network calls, console logs) — the primary tool for debugging CI failures after the fact, since you can't attach a live debugger to a CI runner. Codegen. The record-and-generate tool that scaffolds a first-draft script from manual clicking. VS Code extension. The official Microsoft extension adding test discovery, run/debug buttons directly in the editor, and live locator picking. pytest-playwright plugin. The Python-specific bridge into pytest's fixture/runner ecosystem — most of this manual's test-structure content depends on it. pytest-xdist. Not Playwright-specific, but the standard way Python Playwright suites achieve parallel execution. Playwright MCP. A newer addition letting AI agents/LLMs drive a browser through Playwright via the Model Context Protocol — notable because it's a genuinely new frontier rather than legacy tooling. Cloud execution grids. BrowserStack, LambdaTest, Sauce Labs — third-party services for running Playwright tests on real device/browser combinations at scale. Not part of Playwright itself, but commonly paired with it in enterprise CI setups. Allure / HTML Reporter. Reporting tools for turning raw test results into readable dashboards. Why this matters as a map. When you hit a wall later — \"how do I debug this failing CI test\" or \"how do I see what the test actually did\" — this section is the index telling you which tool in the ecosystem actually solves that problem, instead of trying to solve everything with print statements.",
    customSummary: "## 7. The Playwright Tooling Ecosystem\n\n- Core library + surrounding tools form the full ecosystem.\n- Debugging: Playwright Inspector (live) and Trace Viewer (post-run, essential for CI failures).\n- Authoring: Codegen (record→script), VS Code extension (in-editor run/debug/locator picking).\n- Python-specific: pytest-playwright (fixtures/runner bridge) + pytest-xdist (parallelization).\n### Emerging: Playwright MCP — AI agents driving browsers via Playwright\n- Enterprise add-ons: cloud grids (BrowserStack/LambdaTest/Sauce Labs); reporting tools (Allure/HTML Reporter).",
    chapterNum: 8,
  },
  {
    id: "pw-8-how-to-use",
    title: "8. How to Use This Manual",
    minutes: 18,
    level: "beginner",
    phase: "Part 0 · Background & Context",
    partName: "Part 0 · Background & Context",
    overviewText: "Every part ships in Expanded (learn) and Summarized (review) formats. Parts 1–4 are sequential foundation; Parts 5–8 are reference material. Bizlevate HRM anchors practical examples throughout.",
    why: "Understanding the manual's structure prevents skipping foundations or drowning in reference chapters too early.",
    when: "Read first. Revisit when jumping between parts or preparing for interviews.",
    practical: { app: "Self-study plan", scenario: "You have two weeks before an interview.", pass: "Complete Parts 0–1 expanded, then review summarized versions for Parts 2–4.", fail: "Jump straight to Part 7 career chapters without hands-on practice." },
    advantages: ["Dual format supports both deep learning and quick review", "Sequential Parts 1–4 build skills in correct order", "Reference Parts 5–8 available without linear reading", "Bizlevate examples ground theory in real HRM workflows", "Addendums are core content — not optional footnotes", "Part-by-part delivery matches incremental study pace"],
    limitations: ["Non-linear reading risks missing prerequisite concepts", "Summarized versions omit nuance needed for deep interviews", "Bizlevate examples may not map to every reader's domain", "Manual scope is Playwright/Python only — no Cypress parity yet", "Addendum chapters increase total reading volume", "Self-paced study requires discipline without external deadlines"],
    contentMarkdown: "## 8. How to Use This Manual\n\nTwo formats per part, always. An Expanded version (full explanation, reasoning, context, and practical ties to your Bizlevate work) and a Summarized version (tight, scannable, revision-friendly). Use Expanded for first-pass learning; use Summarized for quick review before interviews or when you just need a refresher. Sequential but not rigid. Parts 1–4 build on each other (Foundations → Core Interactions → Test Structure → Advanced Techniques) and are best read in order the first time. Parts 5–7 and Part 8 can be treated more as reference material to dip into as needed once the fundamentals are solid. Addendums are gap-fills, not optional extras. Sections marked \"(+ addendum: ...)\" throughout the outline were added specifically to cover practical topics that a first-pass syllabus tends to miss (e.g., cookie management, DB-level assertions, OAuth popup handling). Treat these with equal weight to the numbered sections around them. Bizlevate framing throughout. Where relevant, sections tie back to your actual work context (HRM system: Employee Management, Attendance, Leave, Payroll, Onboarding/Offboarding, Appraisal; plus TADA) so the manual isn't purely academic. Part-by-part delivery. You provide raw notes/data for each part in chat, and each part gets fully expanded (with gaps filled in, clearly reasoned from standard Playwright practice) and then condensed into the summarized version.",
    customSummary: "## 8. How to Use This Manual\n\n- Every part delivered in two formats: Expanded (learn) and Summarized (review).\n- Parts 1–4 = sequential foundation; Parts 5–8 = reference material, dip in as needed.\n- Addendums are core content, not optional footnotes.\n- Bizlevate work (HRM modules + TADA) used as the practical anchor throughout.\n- Process: you send raw data per part → gets expanded + summarized here, one part at a time.",
    chapterNum: 9,
  },
];
