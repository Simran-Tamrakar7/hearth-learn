/** Playwright manual Part 6 — Pro-Level Practices */
export const chapters = [
  {
    id: "pw-43-framework",
    title: "43. Building a Scalable Framework from Scratch",
    minutes: 40,
    level: "advanced",
    phase: "Part 6 · Pro-Level Practices",
    partName: "Part 6 · Pro-Level Practices",
    overviewText: "Five-layer framework architecture: config, pages, tests, utils, test_data. BasePage, Settings class, API client utilities, and when structure beats a pile of scripts.",
    why: "Interviewers ask 'how would you structure a 500-test suite?' Framework discipline — not a new tool — is the answer that separates senior automation engineers.",
    when: "Read when your tests/ folder exceeds 20 files or onboarding new engineers takes weeks.",
    practical: { app: "Enterprise HRMS automation", scenario: "New hire cannot find where login helpers live across 40 scattered test files.", pass: "pages/, utils/, config/ layout with BasePage and documented conftest fixtures.", fail: "Copy-paste login in every file; no shared Settings for environment URLs." },
    advantages: ["Feature-folder mirroring (tests/leave/, pages/leave_page.py) aids navigation", "BasePage centralizes navigation and generic wait helpers", "utils/api_client.py seeds data faster than UI setup", "Settings class reads env vars once for entire suite", "Tests read as scenarios — page methods hide locator details", "Separation of concerns enables parallel team contribution"],
    limitations: ["Over-abstraction creates indirection hard to debug for newcomers", "Framework ceremony unjustified for suites under 30 tests", "God-object Page classes accumulate every method over time", "Deep inheritance hierarchies brittle when pages share little", "utils/ becomes dumping ground without naming conventions", "Framework patterns differ from JS @playwright/test project layout"],
    contentMarkdown: "## Building a Scalable Framework from Scratch\n\nFramework vs. a pile of scripts — what actually changes. Every technique in Parts 1–5 works fine on a handful of test files. A \"framework\" is what you get when a suite has grown past the point where ad-hoc organization holds up — dozens or hundreds of tests, multiple contributors, multiple environments, and a real cost to inconsistency. The shift isn't a new tool; it's discipline applied consistently: one way to structure folders, one way to manage config, one way to write a locator, one way to handle test data — enforced by convention and code review (Chapter 45), not left to whoever wrote a given file that week.\n\nLayered architecture — separating concerns cleanly. A mature framework typically has five distinct layers, each with one job:\n\nTest layer — the actual test functions, read almost like plain English, containing assertions and business-flow logic only.\n\nPage Object layer (Chapter 23) — locators and page-level actions, one class per page/component.\n\nUtility/helper layer — cross-cutting reusable logic that isn't tied to a specific page (date formatting, random data generation, retry helpers).\n\nData layer — fixtures, factories, and API clients used to set up/tear down test state (Chapter 25, 27).\n\nConfig layer — environment, credentials, and runtime settings (Chapter 24).\n\nMixing these — a locator hardcoded inside a test function, an API call buried inside a page object — is the single most common way a framework degrades back into an unmaintainable pile of scripts over time, even if it started out well-organized.\n\nproject/\n\n├── tests/                  # test layer — one file per feature/module\n\n│   ├── smoke/\n\n│   ├── regression/\n\n│   └── modules/\n\n├── pages/                  # page object layer\n\n│   ├── base_page.py\n\n│   └── leave_request_page.py\n\n├── api/                    # API client layer (Ch. 27's APIRequestContext wrapped)\n\n│   └── leave_api_client.py\n\n├── utils/                  # cross-cutting helpers\n\n│   ├── data_generators.py\n\n│   └── date_helpers.py\n\n├── fixtures/               # shared, reusable pytest fixtures beyond conftest.py\n\n│   └── auth_fixtures.py\n\n├── config/\n\n│   ├── environments.py\n\n│   └── .env.staging\n\n├── test_data/\n\n│   └── users.json\n\n├── conftest.py\n\n├── pytest.ini\n\n└── requirements.txt\n\nAn API client layer wraps raw APIRequestContext calls into named, reusable methods.\n\n```python\nclass LeaveApiClient:\n    def __init__(self, request_context):\n```\n\nself.request = request_context\n\n```python\ndef create_leave_request(self, employee_id, days):\n```\n\nresponse = self.request.post(\"/leave-requests\", data={\"employee_id\": employee_id, \"days\": days}) return response.json()[\"id\"]\n\n```python\ndef delete_leave_request(self, leave_id):\n```\n\nself.request.delete(f\"/leave-requests/{leave_id}\")\n\nThis mirrors the Page Object Model's core idea (Chapter 23) but applied to the API layer instead of the UI layer: if an API endpoint's shape changes, you fix one method in one class, not every test that happens to call that endpoint directly. Combined with the hybrid pattern from Chapter 27 (API for setup, UI for the behavior under test), this is what most of a scalable framework's test data plumbing actually looks like in practice.\n\nA factory pattern centralizes how test objects get built, separate from where the data itself lives.\n\n```python\nclass UserFactory:\n    def __init__(self, api_client):\n```\n\nself.api_client = api_client\n\n```python\ndef create_employee(self, **overrides):\n```\n\ndefaults = {\"role\": \"employee\", \"department\": \"Engineering\"} defaults.update(overrides) return self.api_client.create_user(defaults)\n\nA factory (as distinct from the static/faker-based fixtures in Chapter 25) builds objects with sensible defaults that any individual test can override only the fields it actually cares about — create_employee(department=\"HR\") — rather than every test needing to specify every field. This keeps tests focused on what's actually relevant to that test, and centralizes \"what does a valid employee record look like\" in one place so it doesn't drift inconsistently across dozens of test files.\n\nDesign principles worth stating explicitly, since they guide every decision above. DRY (Don't Repeat Yourself) applied to test code specifically means: if the same three lines of setup appear in five test files, that's a fixture or a page-object method waiting to be extracted, not something to leave duplicated \"because it's just test code.\" Single Responsibility — a page object should only know about its own page; a fixture should do one logical piece of setup, not five unrelated things bundled together because they happened to run at the same time. Fail fast and fail clearly — a framework should be built so that when something breaks, the failure message (helped by the logging/error-handling discipline in Chapter 42) points as directly as possible at the actual cause, not three layers of abstraction away from it.\n\nBizlevate application — mapping the layered structure onto real modules. A realistic layout: tests/modules/test_leave_management.py, tests/modules/test_attendance.py, tests/modules/test_payroll.py, each backed by pages/leave_request_page.py, pages/attendance_page.py, etc., and a shared api/ layer for seeding employees, leave balances, and attendance records via the HRM's own API rather than clicking through onboarding UI for every test that just needs an employee to exist.",
    customSummary: "## Building a Scalable Framework from Scratch\n\nA framework is disciplined, consistent structure applied to a large suite — not a new tool.\nFive-layer separation: tests (business logic/assertions only) → page objects (locators/actions) → utilities (cross-cutting helpers) → data layer (fixtures/factories/API clients) → config (environment/credentials). Mixing layers (e.g. a locator inside a test) is the main way frameworks degrade.\nAPI client classes wrap raw APIRequestContext calls into named, reusable methods — same benefit as POM, applied to the API layer, and central to the hybrid setup pattern from Ch. 27.\nFactory pattern builds test objects with sensible defaults, letting individual tests override only what they care about — keeps \"what does a valid record look like\" defined in one place.\nGuiding principles: DRY (extract repeated setup into fixtures/methods), Single Responsibility (one page object = one page; one fixture = one job), fail fast/clearly (errors should point at the real cause).\nBizlevate mapping: tests/modules/ per HRM module, backed by matching page objects and a shared API layer for seeding data instead of clicking through onboarding UI every time.",
    chapterNum: 43,
  },
  {
    id: "pw-44-scale",
    title: "44. Managing Test Suites at Scale",
    minutes: 35,
    level: "advanced",
    phase: "Part 6 · Pro-Level Practices",
    partName: "Part 6 · Pro-Level Practices",
    overviewText: "Marker discipline, flake diagnosis workflow, quarantine strategies, TestRail integration, ownership models, and selective execution at hundreds of tests.",
    why: "A 45-minute suite blocking every PR is a process failure. Scale management is about signal-per-minute, not raw test count.",
    when: "Read when CI runtime exceeds team patience or flaky tests erode trust in the suite.",
    practical: { app: "400-test regression suite", scenario: "Suite takes 45 minutes; developers skip CI and merge without waiting.", pass: "Smoke on PR (3 min), sharded regression nightly, quarantine @flaky tests with ticket.", fail: "Run everything always; disable failing tests without tracking tickets." },
    advantages: ["Marker vocabulary (smoke/regression/critical) enables reliable CI slicing", "pytest-xdist -n auto parallelizes across CPU cores", "Quarantine pattern documents known flakes without deleting coverage", "TestRail sync links automated results to manual test cases", "Ownership per module folder clarifies who fixes broken tests", "Historical flake tracking identifies tests needing rewrite"],
    limitations: ["Parallel workers need isolated test data — shared DB causes collisions", "Quarantine without expiry dates accumulates permanent skipped tests", "TestRail integration adds process overhead small teams skip", "Sharding requires CI platform matrix or custom orchestration", "Marker sprawl without convention makes -m filtering unreliable", "Over-parallelizing hits staging environment rate limits"],
    contentMarkdown: "## Managing Test Suites at Scale\n\nThe core problem at scale isn't writing more tests — it's keeping an already-large suite trustworthy and fast. Once a suite passes a few hundred tests, new problems appear that don't exist at a small scale: runtime becomes a real bottleneck in the CI feedback loop, ownership becomes unclear (\"whose test is this, and who fixes it when it breaks?\"), and duplication creeps in as different contributors unknowingly write near-identical coverage for the same flow.\n\nTest ownership and tagging by module keeps a large suite navigable.\n\n```python\n@pytest.mark.owner(\"payroll-team\")\n@pytest.mark.module(\"payroll\")\ndef test_payroll_calculates_overtime_correctly():\n```\n\n...\n\nCustom markers (Chapter 22) aren't limited to smoke/regression — tagging tests by owning team or product module makes it possible to answer \"who do I ask about this failing test\" and \"run only the Payroll module's tests\" without reading file paths. This becomes genuinely necessary once a suite spans multiple teams, each responsible for a different slice of the application (mirroring how Bizlevate's own modules — Employee Management, Attendance, Leave, Payroll, Onboarding/Offboarding, Appraisal — might eventually be owned by different feature teams).\n\nTest impact analysis avoids running the entire suite for every small change. A large, mature suite increasingly can't afford to run everything on every commit — test impact analysis (conceptually: mapping which tests actually exercise which parts of the codebase, often via code-coverage data) lets CI run only the subset of tests plausibly affected by a given change, with the full suite still running on a schedule or before release as a safety net. This is more of an engineering practice than a single Playwright feature — it typically involves coverage tooling and CI configuration working together — but it's worth knowing the concept exists as a suite scales, since it directly addresses the \"CI takes 40 minutes and nobody wants to wait for it\" problem. It resurfaces again in Chapter 50 as a named skill worth having for senior QA automation roles.\n\nDeduplication and coverage mapping prevent a suite from silently testing the same thing five different ways. As a suite grows across contributors, it's common for three different people to each write a slightly different \"can a user log in\" test without realizing the others exist. A periodic audit — grouping tests by the actual feature/flow they exercise (not just by file name) — surfaces this kind of redundancy. Requirements traceability (covered in more depth in Chapter 46's addendum) is the more formal version of this same idea: mapping each test back to a specific requirement or user story, which makes both duplication and gaps visible at the same time.\n\nSuite health metrics turn \"the suite feels slow/flaky\" into something measurable and trackable over time. Worth tracking deliberately, usually via CI/reporting tooling (Chapter 40): total runtime trend (is the suite getting slower release over release?), flaky-test rate (tying back to Chapter 38's detection discipline), and pass-rate trend on the full suite versus just smoke tests. Treating these as metrics with an owner — not just a vague feeling that \"tests take a while now\" — is what actually keeps a large suite healthy over years rather than degrading quietly until someone declares test-automation bankruptcy and rewrites it from scratch.\n\nSplitting suites by execution frequency is the practical response to growing runtime. This ties directly back to the smoke/regression split from Chapter 22 and the CI patterns from Chapter 39, but at scale it often grows a third tier: a very small \"critical path\" tier (a handful of tests, run on every single commit, minutes), a broader smoke tier (run on every PR merge, tens of minutes), and a full regression tier (nightly or pre-release, potentially hours) — each tier answering a different question at a different cost, rather than one undifferentiated \"run everything\" button.",
    customSummary: "## Managing Test Suites at Scale\n\nAt scale, the challenge shifts from writing tests to keeping a large suite fast, owned, and trustworthy.\nTag tests by owning team/module (custom markers) so failures have a clear owner and subsets can run independently.\nTest impact analysis runs only tests plausibly affected by a change (via coverage-mapping tooling + CI config), with full suite runs reserved for schedule/release gates.\nPeriodic audits catch duplicated coverage across contributors; requirements traceability (Ch. 46) is the more formal version of the same idea.\nTrack suite health as real metrics — runtime trend, flaky-test rate, pass-rate trend — not just a vague feeling that \"tests are slow now.\"\nSplit execution by frequency into tiers: critical-path (every commit), smoke (every PR), full regression (nightly/pre-release) — each answering a different question at a different cost.",
    chapterNum: 44,
  },
  {
    id: "pw-45-review",
    title: "45. Code Review & Best Practices",
    minutes: 30,
    level: "advanced",
    phase: "Part 6 · Pro-Level Practices",
    partName: "Part 6 · Pro-Level Practices",
    overviewText: "Review checklist for test PRs: locator stability, no time.sleep, POM adherence, fixture scope, secret handling, and anti-patterns that fail senior review.",
    why: "Test code is production code. Bad tests create false confidence and expensive maintenance — code review is where quality compounds or erodes.",
    when: "Read before reviewing your first teammate's Playwright PR or preparing for SDET interview loops.",
    practical: { app: "Team PR review", scenario: "PR uses page.locator('.btn-primary').nth(3).click() and time.sleep(2).", pass: "Request get_by_role with filter scoping; replace sleep with expect assertion.", fail: "Approve because 'tests pass locally' without reviewing locator strategy." },
    advantages: ["Review checklist catches brittle CSS before merge", "Banning time.sleep enforces Playwright auto-wait culture", "POM review ensures locator changes stay in page classes", "Fixture scope review prevents session-scoped state leaks", "Secret scan in review prevents credential commits", "Pair review spreads framework conventions across team"],
    limitations: ["Checklist fatigue leads to rubber-stamp approvals", "Senior reviewers scarce — bottleneck on small teams", "Style debates (POM vs screenplay) waste review cycles", "No automated enforcement without custom lint rules", "Review standards drift without documented team conventions", "Over-strict review slows feature delivery on greenfield projects"],
    contentMarkdown: "## Code Review & Best Practices\n\nTest code deserves the same review rigor as application code — a common and costly mistake is treating it as lesser. Because test code doesn't ship to users directly, it's tempting to review it more loosely than application code — but a badly-written test can be actively worse than no test at all (a flaky test that erodes trust, per Chapter 38; a test so tightly coupled to implementation that it blocks legitimate refactors; a test that passes even when the feature is broken, giving false confidence). Reviewing test PRs with the same seriousness as feature PRs is what prevents a framework built well in Chapter 43 from degrading over time as more people contribute to it.\n\nA practical review checklist, worth applying consistently rather than ad hoc.\n\nLocator quality — does it follow the strategy from Chapter 13 (role/label/test-id preferred, CSS/XPath only as a last resort), or does it introduce a brittle selector that'll break on the next unrelated CSS refactor?\n\nAssertion strength — is the test actually verifying the thing it claims to (a test titled \"shows validation error\" that only checks the button became disabled, without checking the error message text, is verifying the wrong thing)?\n\nTest independence — does this test rely on state left behind by another test, or on running in a specific order (a red flag tying back to Chapter 32's parallelization risks)?\n\nCleanup — if the test creates data, does it clean up after itself, including on failure (Chapter 25's teardown pattern)?\n\nNaming and clarity — does the test name and its structure make it obvious what's being verified without needing to read every line, so a future failure is diagnosable from the test report alone?\n\nSelf-healing locators are an emerging mitigation for locator brittleness, not a replacement for good locator strategy. Some tools and Playwright-adjacent libraries offer \"self-healing\" locator features — when a primary locator fails to resolve, the tool attempts to find a similar element using fallback heuristics (similar text, similar position, similar attributes) and either auto-corrects or flags the drift for a human to confirm. This can reduce nuisance failures from minor, non-meaningful UI changes (a class name changing during a refactor with no actual behavior change) — but it's a safety net, not a substitute for writing stable locators in the first place per Chapter 13's strategy, and teams should be cautious about self-healing silently \"fixing\" a locator that failed for a genuinely meaningful reason (the element actually disappeared, which is exactly the kind of thing a test should catch, not paper over).\n\nLocator maintenance discipline is an ongoing practice, not a one-time setup decision. As an application's UI evolves, locators inevitably need updates — the healthy pattern is treating this as routine maintenance woven into normal development (a developer renaming a button's accessible name flags the corresponding test-locator update in the same PR, rather than QA discovering it days later via a mysterious CI failure). This is where the test-id convention (Chapter 13) pays off most concretely at scale: a stable data-testid means far fewer of these maintenance touchpoints in the first place, so it's worth treating \"does this new UI element need a test-id\" as a standing question in code review on the application side, not just the test side.\n\nStyle guides and linting keep a multi-contributor suite visually and structurally consistent. Tools like ruff or flake8 (general Python linting) plus a documented internal style guide (naming conventions for fixtures, page object methods, test functions) reduce the \"every file looks like it was written by a different person\" problem that naturally emerges once more than one or two people contribute to a suite over time — consistency here is what makes the layered architecture from Chapter 43 actually hold up under real, sustained multi-contributor use rather than just looking good on day one.",
    customSummary: "## Code Review & Best Practices\n\nTest code needs the same review rigor as application code — a bad test can be worse than no test (flaky, over-coupled, or falsely passing).\nReview checklist: locator quality (role/label/test-id over brittle CSS/XPath), assertion strength (verifying the right thing, not just a proxy), test independence (no shared/order-dependent state), cleanup (even on failure), naming/clarity (diagnosable from the report alone).\nSelf-healing locators (auto-correcting to a similar element when a locator fails) reduce nuisance failures from meaningless UI changes, but are a safety net, not a substitute for good locator strategy — risk of silently masking a genuinely meaningful failure.\nLocator maintenance is routine, ongoing work — UI changes should trigger locator updates in the same PR; a strong data-testid convention minimizes how often this is needed.\nLinting + a documented style guide keep a multi-contributor suite consistent as it grows, which is what makes the Ch. 43 layered architecture hold up over time.",
    chapterNum: 45,
  },
  {
    id: "pw-46-performance",
    title: "46. Performance Considerations",
    minutes: 32,
    level: "advanced",
    phase: "Part 6 · Pro-Level Practices",
    partName: "Part 6 · Pro-Level Practices",
    overviewText: "Suite speed levers: storage_state auth reuse, API seeding vs UI setup, blocking images, worker count tuning, and distinguishing UI test speed from load testing.",
    why: "Slow suites get skipped. Performance optimization is not premature — it is what keeps automation trusted and actually run.",
    when: "Read when CI runtime doubles after adding 50 tests or when interviewers ask 'how would you speed up a 45-minute suite?'",
    practical: { app: "CI pipeline over budget", scenario: "Each test logs in via UI — 15 seconds × 80 tests = 20 minutes of login alone.", pass: "storage_state session fixture; API seed for test data; -m smoke on PR.", fail: "Add time.sleep to 'fix' flakes; run headed in CI for debugging permanently." },
    advantages: ["storage_state eliminates repeated UI login per test", "APIRequestContext setup is orders of magnitude faster than UI", "Blocking images/fonts speeds non-visual tests significantly", "pytest-xdist workers scale linearly until resource contention", "Session-scoped fixtures amortize expensive one-time setup", "Selective markers keep PR feedback under five minutes"],
    limitations: ["storage_state stale after auth flow changes — silent failures", "Too many xdist workers overwhelm staging server", "Blocking images breaks visual regression tests if forgotten", "Performance tuning trades debuggability for speed", "Load testing is k6/JMeter — Playwright speed ≠ app capacity", "Diminishing returns after CPU-saturated parallelization"],
    contentMarkdown: "## Performance Considerations\n\nTest suite performance is a discipline of its own, distinct from application performance testing. This chapter is about how fast the test suite itself runs — not about using Playwright to load-test the application (that's explicitly out of scope per Part 0, Chapter 4, and belongs to k6/JMeter/Locust). The two get confused because both involve the word \"performance,\" but they answer entirely different questions.\n\nReducing unnecessary waits and redundant navigation is usually the single biggest lever. A surprising amount of suite runtime in practice comes not from the application being slow, but from tests navigating to the same page repeatedly, re-authenticating unnecessarily (when storage-state reuse from Chapter 30 would avoid it), or using a broader wait than needed (networkidle when a specific element wait would resolve faster and more reliably, per Chapter 16). Auditing the slowest tests in a suite's reporting output (Chapter 40) and asking \"does this really need to re-navigate/re-login/wait this broadly\" routinely surfaces easy wins before reaching for more complex solutions like additional parallelization.\n\nSmart use of API setup (Chapter 27's hybrid pattern) is a performance technique as much as a reliability one. Every UI step replaced with an equivalent API call in test setup is both faster and less flaky — this is worth re-emphasizing here specifically as a performance lever, since it's easy to think of it purely as a \"best practice\" rather than recognizing it as often the single most impactful change available to a slow suite, especially for suites that grew organically driving everything through the UI including setup steps that were never actually the thing under test.\n\nParallelization tuning (Chapter 32) has a genuine sweet spot, not a \"more is always better\" curve. Worth revisiting here with a performance-specific lens: the right worker count depends on the CI runner's actual CPU/memory, not an arbitrary large number — over-provisioning workers on a resource-constrained runner causes browser instances to contend for memory and can make a suite slower and flakier than a lower worker count would. Measuring actual wall-clock time at a few different worker counts (rather than assuming) is the only reliable way to find the right setting for a given CI environment.\n\nRBAC (role-based access control) testing patterns are a structural approach to testing permission boundaries efficiently. For an app like an HRM system with distinct roles (employee, manager, admin/HR — tying back to Chapter 30's multi-role storage-state pattern), RBAC testing means systematically verifying both that each role can do what it should (an employee can submit their own leave request) and, just as importantly, that each role cannot do what it shouldn't (an employee cannot approve their own leave request, cannot view another employee's payroll data, cannot access admin-only settings pages). A practical pattern is parametrizing a single test across roles and expected outcomes rather than writing entirely separate test functions per role:\n\n```python\n@pytest.mark.parametrize(\"role,can_approve\", [\n```\n\n(\"employee\", False), (\"manager\", True), (\"admin\", True), ])\n\n```python\ndef test_leave_approval_permission(role, can_approve, role_contexts):\n    page = role_contexts[role].new_page()\n```\n\npage.goto(\"https://app.example.com/leave-requests/123\") approve_button = page.get_by_role(\"button\", name=\"Approve\") if can_approve:\n\n```python\nexpect(approve_button).to_be_visible()\n    else:\n        expect(approve_button).not_to_be_visible()\n```\n\nThis directly reuses the per-role storage_state fixtures from Chapter 30 (role_contexts here would be a fixture providing one context per role) and the parametrization technique from Chapter 22 — RBAC testing isn't really a new Playwright feature, it's a deliberate combination of tools already covered, applied with a permission-boundary mindset. Negative-permission checks (verifying access is correctly denied) are just as important as positive ones and are often under-tested in practice, since it's easy to test \"the feature works\" and forget to test \"the feature is correctly restricted.\"\n\nRequirements traceability and coverage reporting connect test coverage back to actual business requirements. Requirements traceability means maintaining a mapping between test cases and the specific requirement, user story, or acceptance criterion they verify — often via a shared identifier (a Jira ticket number, a requirement ID) referenced in the test's docstring, marker, or BDD scenario (tying back to Chapter 35's BDD integration, which doubles well as traceability documentation). The practical value: it makes coverage gaps visible (\"this requirement has no corresponding automated test\") and makes it possible to answer \"if we ship this release, which requirements have automated regression protection\" — a question audit-heavy industries (banking, healthcare, per Part 0) often need answered formally, and one that's valuable to be able to answer even informally in less regulated contexts.\n\n```python\n@allure.title(\"Leave request cannot exceed available balance\")\n@allure.tag(\"REQ-LEAVE-014\")\ndef test_leave_request_exceeds_balance_is_rejected(page):\n```\n\n...\n\nCoverage reporting in this sense is distinct from code coverage (a measure of which lines of application code get executed) — it's requirement coverage, a measure of which documented requirements have a corresponding automated check. Both are useful, but they answer different questions: code coverage tells you what code ran; requirement coverage tells you what business behavior is actually protected. A mature framework at scale (Chapter 44) typically tracks both, but requirement coverage is generally the more meaningful metric to report to non-engineering stakeholders, since \"80% of requirements have automated tests\" is a more actionable statement to a product owner than a code-coverage percentage.",
    customSummary: "## Performance Considerations\n\nThis is about the test suite's own runtime, not application load/performance testing (out of scope per Part 0).\nBiggest lever is usually eliminating unnecessary waits/re-navigation/re-auth — audit the slowest tests via reporting output rather than guessing.\nAPI-based setup (Ch. 27) is a performance technique as much as a reliability one — every UI step replaced with an API call is faster and less flaky.\nParallelization has a real sweet spot tied to actual CI runner resources — measure at a few worker counts rather than assuming \"more is better.\"\nRBAC testing systematically verifies both allowed and (just as important, often under-tested) denied actions per role — parametrize across role × expected-outcome, reusing Ch. 30's per-role storage-state fixtures.\nRequirements traceability maps tests back to specific requirements/user stories (via ticket IDs, markers, or BDD scenarios), surfacing coverage gaps; requirement coverage is generally more meaningful to non-engineering stakeholders than raw code coverage.",
    chapterNum: 46,
  },
];
