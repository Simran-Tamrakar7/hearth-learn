import type { ChapterRecord } from "../../../types";

/** 2. What Cypress Can Do */
export const chapter = {
  id: "cy-2-capabilities",
  title: "2. What Cypress Can Do",
  minutes: 32,
  level: "beginner",
  phase: "Part 0 · Background",
  partName: "Part 0 · Background",
  overviewText: "Cypress runs inside the browser with a custom command queue, time-travel DOM snapshots, retry-ability on mid-chain assertions, cy.intercept stubbing, and default video on cypress run.",
  why: "Async/promise confusion trips Playwright migrants — chainables and cy.wait('@alias') must be internalized early.",
  when: "Read before your second spec. Revisit when debugging timing or comparing retry models.",
  practical: { app: "E-commerce cart", scenario: "Dev writes const total = cy.get('.total').text() and gets undefined.", pass: "Explain chainables and .then() pattern.", fail: "Add cy.wait(3000) everywhere." },
  advantages: ["Time-travel restores real DOM not screenshots","Retry on .should() mid-chain is uniquely Cypress","cy.intercept().as() + cy.wait('@alias') idiom","Video on by default in cypress run","Color-coded Command Log per it() block","Zero-latency DOM via shared event loop"],
  limitations: ["cy.get() returns chainables not Promises","Default video bloats CI artifacts","Mid-chain retry differs from Playwright auto-wait","Time-travel GUI-only not in headless CI","Mental shift from await page.locator()","Electron default may differ from Chrome"],
  tools: [],
  customSummary: "- Runs inside the browser — cy.get() isn't a Promise, it's a queued chainable; commands execute after the sync test function returns (common newcomer confusion).\n- Time-travel debugging — live DOM snapshots per command, integrated directly into the Test Runner (vs. Playwright's post-hoc Trace Viewer).\n- Retry-ability wraps .should() mid-chain, retrying the whole chain up to that assertion — distinct from Playwright's action-centered auto-waiting.\n- Network stubbing idiom: cy.intercept().as() + cy.wait('@alias') — no Playwright one-liner equivalent.\n- cypress run auto-records video with zero config (Playwright requires an opt-in flag).",
  contentMarkdown: "## Runs inside the browser — go deeper on the async/promise confusion this creates for newcomers\n\nThis is worth spending real time on because it trips up almost everyone coming from Playwright/Selenium/plain JavaScript async patterns. In Playwright (or plain async JS), you'd write:\n```javascript\nconst value = await page.locator('.total').textContent();\nconsole.log(value);\nIn Cypress, the equivalent looks deceptively similar but works completely differently:\njavascript\ncy.get('.total').invoke('text').then((value) => {\n  console.log(value);\n});\ncy.get() does not return a Promise you can await. It returns a Cypress \"chainable\" — an object representing a queued, not-yet-executed command. Cypress internally builds up a queue of these commands as your test function runs synchronously top-to-bottom, then executes them one at a time, afterward, each one waiting for the previous to finish and retrying internally as needed. This is why you can't do const value = cy.get('.total').text() and expect value to hold anything meaningful outside a .then() callback — the command hasn't actually run yet at the point your synchronous test function returns control. This single misunderstanding is probably the most common source of confused first-week Cypress bugs, and it's worth internalizing early rather than fighting it chapter after chapter.\n```\n\n## Time-travel debugging — go deeper on the actual mechanism\n\nWhen the Test Runner executes your test, it doesn't just show a static list of pass/fail lines — each command in the log is backed by an actual DOM snapshot taken at that moment. Hovering over (or clicking) a command in the log restores the application's DOM in the browser preview pane to exactly how it looked right before and right after that command ran — and because this is a real DOM, not a screenshot image, you can open actual browser DevTools and inspect it: check computed CSS, look at attached event listeners, inspect the accessibility tree, whatever you'd normally do live. Playwright's Trace Viewer (Part 4, Ch. 24 of your Playwright manual) achieves something conceptually similar via recorded DOM snapshots, but it's a separate post-hoc viewer you open after the fact from a .zip trace file — Cypress's version is live, integrated directly into the same window you're already watching the test run in, with zero extra setup required.\n## Automatic waiting / retry-ability — go deeper on the assertion-level retry distinction\n\nThis is worth making very concrete, since the Playwright comparison matters for interview answers. In Playwright:\n```python\nexpect(page.locator(\".cart-count\")).to_have_text(\"3\")\nPlaywright re-queries the locator and re-checks the assertion together as one atomic retry loop — this is genuinely similar to Cypress conceptually. But the difference shows up in command chains that aren't pure assertions. Consider a Cypress chain like:\njavascript\ncy.get('.item-list').find('.item').should('have.length', 3).last().click();\nHere, the retry-ability specifically wraps the .should() in the middle of the chain — Cypress will re-run cy.get('.item-list').find('.item') repeatedly until the length assertion passes, and only once it passes does it continue to .last().click(). This \"retry until the assertion embedded mid-chain passes, then proceed\" behavior is a distinctly Cypress pattern — Playwright's equivalent would typically be written as two separate statements (an expect() call, then a follow-up action), since Playwright's auto-waiting model is centered on the action being performed, not an assertion sitting in the middle of a locator chain.\n```\n\n## Network stubbing — preview the cy.wait('@alias') idiom since it has no real Playwright equivalent\n\nBriefly worth flagging here even though Chapter 23 covers it fully: Cypress's intercept pattern typically looks like:\n```javascript\ncy.intercept('GET', '/api/users').as('getUsers');\ncy.visit('/users');\ncy.wait('@getUsers');  // pause the test until this specific network call completes\nThis \"name the intercepted route, then explicitly wait on that name\" pattern is idiomatic Cypress and doesn't have a one-line equivalent in Playwright, where you'd more typically wait on a specific UI state that indicates the data has arrived, or use page.wait_for_response() matching a URL pattern directly rather than a named alias. Worth knowing this exists now since it'll shape how you write nearly every network-mocking test in Cypress going forward.\n```\n\n## Built-in dashboard/videos/screenshots — go deeper on the \"on by default\" framing\n\nRunning cypress run (the headless CI mode) automatically records a full video of the entire spec file's execution and saves it to cypress/videos/ with zero configuration — this is genuinely different from Playwright, where video capture is a deliberate opt-in CLI flag (--video=retain-on-failure) precisely because Playwright's philosophy leans toward explicit configuration for anything with a storage/performance cost. Cypress's \"just works, capture everything by default\" approach is friendlier to a newcomer's first CI setup but can produce meaningfully larger CI artifact storage over time if a team never revisits the default — worth being aware of as a \"known gotcha\" for real production usage covered again in Chapter 33 (Performance).",
  exercises: [],
  resourceLinks: [],
  steps: [],
  learn: [],
} as ChapterRecord;
