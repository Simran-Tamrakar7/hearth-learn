import type { ChapterRecord } from "../../../types";

/** 11. Selector Strategies */
export const chapter = {
  id: "cy-11-selectors",
  title: "11. Selector Strategies",
  minutes: 30,
  level: "intermediate",
  phase: "Part 2 · Core Commands",
  partName: "Part 2 · Core Commands",
  overviewText: "Comprehensive coverage of Selector Strategies in Cypress with code examples, Playwright comparisons, and interview-ready depth paired with the Playwright manual.",
  why: "Mastering Selector Strategies in Cypress's command-queue model prevents flaky specs and wrong Playwright ports.",
  when: "Read when implementing or debugging selector strategies in your suite.",
  practical: { app: "Web application under test", scenario: "Spec fails around selector strategies — need Cypress-native pattern.", pass: "Apply chapter patterns with retry semantics not bare cy.wait(ms).", fail: "Port Playwright await code or fixed delays." },
  advantages: ["data-cy convention rationale","five-tier fallback checklist","anti-pattern examples","stability philosophy vs Playwright","html+js data-cy examples","refactor-survival mental test"],
  limitations: ["data-cy needs dev buy-in","third-party lacks attributes","aria tier less enforced","class selectors tempting","nth-child brittle","no codegen for data-cy"],
  tools: [],
  contentMarkdown: "## data-cy attributes — go deeper on why this is Cypress's specifically recommended convention\n\nCypress's own official documentation is unusually opinionated (more so than Playwright's docs) about recommending a dedicated testing attribute, conventionally named data-cy (though data-test or data-testid are also common, just pick one and be consistent):\n```html\n<button data-cy=\"submit-login\">Log In</button>\njavascript\ncy.get('[data-cy=submit-login]').click();\nThe reasoning, worth understanding rather than just following: CSS classes and element structure change for styling/refactoring reasons that have nothing to do with test stability, while a data-cy attribute is a signal added specifically and only for testing purposes — nobody refactors CSS and accidentally removes a data-* testing attribute the way they might rename a class during a redesign. This is functionally the same underlying philosophy as Playwright's role/accessible-name-first recommendation (Part 2, Chapter 5 of your Playwright manual) — both are trying to decouple test stability from implementation-detail churn — they just arrive at different mechanisms: Playwright leans on the accessibility tree (which has the side benefit of double-checking a11y), Cypress leans on a dedicated custom attribute (which requires deliberate developer buy-in to add across the codebase, but is arguably even more explicit and unambiguous once present).\nWorth flagging honestly: the data-cy convention requires cooperation from whoever's writing the application code — if Bizlevate's frontend developers aren't already adding these attributes, adopting this convention means either asking them to add data-cy attributes as part of normal development going forward, or falling back to less stable CSS/text-based selectors for existing markup you can't easily change.\n```\n\n## CSS selectors — go deeper on the fallback tiers, from most to least stable\n\nWhen a data-cy attribute isn't available (existing markup, third-party components), Cypress's own docs suggest a rough preference order, worth internalizing as a checklist:\ndata-cy / data-test / data-testid attributes (most stable)\nid attributes, if genuinely stable and unique on the page (some frameworks generate dynamic/random IDs — check first)\nSemantic attributes like name, type, aria-label (dual benefit: somewhat accessibility-aware, similar in spirit to Playwright's role-based approach)\n## Class names — only as a last resort, since these are the most likely to change for purely visual/refactoring reasons unrelated to functionality\n\n## Tag + nth-child/structural position — the most brittle, avoid unless truly nothing else distinguishes the element\n\n## Best practices — go deeper with concrete anti-pattern examples\n\n```javascript\n// Fragile — breaks if styling classes change, or if there are multiple .btn.btn-primary on the page\ncy.get('.btn.btn-primary').click();\n```\n\n## // Fragile — breaks if the DOM structure is refactored even slightly\n\ncy.get('div > div > form > div:nth-child(3) > button').click();\n\n## // Stable — survives styling refactors and structural changes\n\ncy.get('[data-cy=submit-login]').click();\nA useful mental test before writing any selector: \"if a frontend developer redesigned this component's CSS and HTML structure tomorrow, purely for visual reasons, with zero functional change — would my selector still find the right element?\" If the honest answer is no, it's worth pushing for a data-cy attribute to be added rather than accepting a brittle selector as permanent.",
  exercises: [],
  resourceLinks: [],
  steps: [],
  learn: [],
} as ChapterRecord;
