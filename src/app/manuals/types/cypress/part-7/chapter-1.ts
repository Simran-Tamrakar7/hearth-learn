import type { ChapterRecord } from "../../../types";

/** 7.1 Cookies, Local Storage, Session Storage */
export const chapter = {
  "id": "cy-7-1-cookies-local-storage-session-storage",
  "title": "7.1 Cookies, Local Storage, Session Storage",
  "minutes": 22,
  "level": "intermediate",
  "phase": "Part 7 · State Management & Test Isolation",
  "partName": "Part 7 · State Management & Test Isolation",
  "overviewText": "Since Cypress 12, cookies, localStorage, and sessionStorage are cleared between tests — and in-memory app state dies because the page itself resets to about:blank. Treat every it() as a blank browser unless you opt into cy.session() or { testIsolation: false }. Storage is origin-scoped; cy.origin() state is invisible outside that origin. clearAllCookies/getAllLocalStorage exist for whole-browser visibility.",
  "why": "The classic 'login test then a second it() that assumes still logged in' bug is isolation working as designed, not a Cypress defect. Knowing v12+ clears all stores — not just cookies — stops you from cargo-culting old cy.clearLocalStorage() tutorials.",
  "when": "When a second test in a spec is redirected to /login, when debugging auth with cy.getCookies(), or when SSO spans two origins.",
  "practical": {
    "app": "Bizlevate HRM",
    "scenario": "it('logs in') then it('views approvals') in the same describe, no beforeEach.",
    "pass": "You put cy.loginAs('manager') in beforeEach (cached in 7.2) so each test establishes its own state.",
    "fail": "You disable isolation to make the second test pass, or assume cookies survived because they survive cy.reload()."
  },
  "tools": [],
  "customSummary": "- This chapter's question, distinct from Part 3.10's mechanics: what actually persists across a test boundary, and what silently vanishes?\n- Cookies, localStorage, sessionStorage are all cleared between tests (v12+) — and so is in-memory JS application state (Redux/React state), because Cypress doesn't just clear storage, it resets the page itself\n- Mental model: treat every it() as starting from a blank browser, with two deliberate, opt-in exceptions — cy.session() (restores from a cache, 7.2) and { testIsolation: false } (disables the reset for a whole suite, 7.3). Absent either, assume nothing survives\n- This directly prevents the classic bug of a \"login\" test and a \"do something while logged in\" test being split across two it() blocks assuming shared state — the fix is making every test independently establish what it needs via beforeEach + cached login, not fighting the reset\n- Storage is origin-scoped by browser design, not a Cypress choice — state set inside a cy.origin() block (Part 9.1) is invisible outside that origin, exactly as for a real user. clearAllCookies()/getAllLocalStorage() (v12+) exist specifically for whole-browser, cross-origin visibility\n- Inspecting storage/cookies via cy.log() mid-test is a legitimate, fast first diagnostic step for state-smelling failures, before reaching for heavier tools (Part 12.1)\n- vs Playwright: storageState makes \"what persists\" an explicit, serializable file artifact from the start, rather than something reasoned about via hook placement. vs Selenium: no isolation policy at all — a WebDriver session's cookie jar persists until manually cleared, making Selenium suites more exposed to accidental cross-test leakage by default than Cypress's stricter default",
  "contentMarkdown": "Revisiting browser state specifically through the lens of test isolation — not \"how do I read a cookie\" (Part 3.10 covered that), but \"what state persists, what gets wiped, and how do I reason about it across a whole spec file.\"\n\n## Why this chapter exists separately from Part 3.10\n\nPart 3.10 taught the mechanics: `cy.getCookie()`, `cy.setCookie()`, `window.localStorage`. This chapter assumes those mechanics and asks a different question — as a test author, what can I actually rely on being there when my test starts, and what will silently vanish? That question is the foundation everything else in Part 7 builds on: `cy.session()` (7.2) is a caching strategy built on top of an understanding of what gets cleared; test isolation (7.3) is the policy that decides what gets cleared; and authentication patterns (7.4) are the practical payoff of getting all of this right.\n\n## The three stores, and their default lifetime under Cypress\n\n| Store | Cleared between tests? (v12+) | Cleared between specs? | Survives `cy.reload()`? |\n|---|---|---|---|\n| Cookies | Yes | Yes | Yes |\n| localStorage | Yes | Yes | Yes |\n| sessionStorage | Yes | Yes | Yes |\n| In-memory JS state (Redux store, React state) | Yes (full page reset) | Yes | No |\n\nThe important row to internalize is the last one: JS-in-memory application state is gone the instant a test boundary is crossed, because Cypress doesn't just clear storage — it resets the page itself under `testIsolation`. Cookies and Web Storage are the only things that can meaningfully persist across a page reset within a single origin, which is exactly why authentication (a concern that fundamentally needs to survive navigation) is built on top of them rather than on, say, a Redux store.\n\n## Reasoning about state as a test author\n\nThe practical mental model: treat every it() block as starting from a blank browser, with two narrow exceptions you opt into deliberately — `cy.session()` (7.2), which restores cookies/storage from a cache, and { `testIsolation: false` } (7.3), which disables the reset for an entire suite. Absent either of those, assume nothing survives.\nThis mental model directly prevents a category of bug that trips up people arriving from Selenium or Playwright-with-shared-context habits — writing a first test that logs in, and a second test in the same file that assumes it's still logged in:\n\n```js\n// ❌ Assumes state carries over — it doesn't, by default\ndescribe('Leave management', () => {\n  it('logs in', () => {\n    cy.visit('/login');\n    cy.get('[data-cy=email]').type('manager@bizlevate.test');\n    cy.get('[data-cy=password]').type('pw');\n    cy.get('[data-cy=submit]').click();\n    cy.url().should('include', '/dashboard');\n  });\n\n  it('views the leave approvals page', () => {\n```\n`cy.visit('/leave/approvals')`;    // ❌ not logged in — cookies were cleared before this test started\n\n```js\n    cy.get('[data-cy=leave-row]').should('have.length.greaterThan', 0);   // fails, redirected to /login instead\n  });\n});\n```\n\nThe fix isn't to disable isolation — it's to make each test independently responsible for the state it needs, which is exactly what beforeEach plus `cy.session()` exists to make cheap (7.2):\n\n```js\n// ✅ Each test independently establishes what it needs\ndescribe('Leave management', () => {\n  beforeEach(() => {\n```\n`cy.loginAs('manager')`;   // fast, cached — see 7.2\n\n```js\n    cy.visit('/leave/approvals');\n  });\n\n  it('shows pending requests', () => {\n    cy.get('[data-cy=leave-row]').should('have.length.greaterThan', 0);\n  });\n\n  it('allows approving a request', () => {\n    cy.get('[data-cy=leave-row]').first().find('[data-cy=approve]').click();\n    // ...\n  });\n});\n```\n\n## Cross-origin storage — a real, sharp edge\n\nCookies and storage are origin-scoped by browser design, not by any Cypress decision. If your app's flow genuinely spans two origins — your app plus a separate auth domain — storage set on one origin is invisible from the other, full stop, exactly as it would be for a real user's browser. This connects directly back to `cy.origin()` (Part 9.1): storage manipulation inside a `cy.origin()` block only affects that block's origin, and Cypress's `cy.clearCookies()`/clearAllCookies()/getAllLocalStorage() (v12+) commands exist specifically to reach across origins when you need whole-browser visibility rather than single-origin scoping:\n\n```js\ncy.clearAllCookies();          // all origins the browser currently holds cookies for\ncy.getAllLocalStorage().then((result) => {\n  // result is keyed by origin\n  expect(result['https://app.bizlevate.com']).to.have.property('authToken');\n});\n```\n\n## Storage as a debugging tool, not just a state concern\n\nWorth naming explicitly: when a test fails in a way that smells like an auth or state problem, inspecting storage directly is often faster than guessing from symptoms:\n\n```js\ncy.window().then((win) => {\n  cy.log('Current localStorage:', JSON.stringify(win.localStorage));\n});\ncy.getCookies().then((cookies) => {\n  cy.log('Current cookies:', JSON.stringify(cookies.map(c => c.name)));\n});\n```\n\nDropping these into a failing test temporarily is a legitimate, quick diagnostic step before reaching for heavier tools (Part 12.1).\n\n## Comparison\n\nPlaywright's `storageState` (previewed in Part 3.10) makes this whole chapter's concern — \"what state persists and how do I control it deliberately\" — an explicit, serializable, file-based artifact from the start, rather than something you reason about implicitly through hook placement. Selenium has no isolation policy of its own at all; whatever the WebDriver session's cookie jar happens to contain persists until you explicitly clear it or the session ends, which means Selenium suites are more prone to accidental cross-test state leakage by default, not less — Cypress's aggressive default clearing is stricter than Selenium's default behavior, even though it occasionally surprises newcomers expecting session persistence.",
  "advantages": [
    "7.1 Cookies, Local Storage, Session Storage — The classic 'login test then a second it() that assumes still logged in' bug is isolation working as designed, not a Cypress defect."
  ],
  "limitations": [
    "7.1 Cookies, Local Storage, Session Storage is this Part's slice only; later chapters go deeper rather than repeating this one."
  ],
  "exercises": [],
  "resourceLinks": [],
  "steps": [],
  "learn": []
} as ChapterRecord;
