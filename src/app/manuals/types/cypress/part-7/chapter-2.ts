import type { ChapterRecord } from "../../../types";

/** 7.2 cy.session() for Login Caching */
export const chapter = {
  "id": "cy-7-2-cy-session-for-login-caching",
  "title": "7.2 cy.session() for Login Caching",
  "minutes": 28,
  "level": "intermediate",
  "phase": "Part 7 · State Management & Test Isolation",
  "partName": "Part 7 · State Management & Test Isolation",
  "overviewText": "cy.session(name, setupFn) runs setup once per key, snapshots cookies and storage, and restores them later without re-running login. validate() re-checks before restore and re-runs setup on failure. cacheAcrossSpecs: true amortizes one login across every spec in a cypress run. After session, Cypress navigates to about:blank — always follow with cy.visit(). Setup can be API or full UI; you pay the real cost exactly once.",
  "why": "Isolation and performance pull against each other; cy.session() is the sanctioned exception to 'assume nothing persists.' Forgetting the follow-up visit is the most common first-time bug. Playwright storageState persists across CLI invocations; Cypress's cache does not.",
  "when": "Implementing cy.loginAs, switching employee/manager mid-spec, or caching an SSO UI login that has no API bypass.",
  "practical": {
    "app": "Bizlevate HRM",
    "scenario": "A spec needs employee (no approve button) then manager (approve exists), plus 40 other specs that also need manager.",
    "pass": "You cy.session(role, ...) with validate against /api/auth/me, cacheAcrossSpecs: true, then cy.visit after each restore.",
    "fail": "You set localStorage in the test body (wiped next test), skip validate, or forget cy.visit and assert against about:blank."
  },
  "tools": [],
  "customSummary": "- Reconciles isolation (fresh state every test) with performance (don't pay the login cost every test) — caches the result of an auth flow (cookies + storage), keyed by a name, and restores it without re-running the setup logic\n- Basic shape: cy.session(name, setupFn) — setupFn runs in full only the first time for a given key; every later call restores the snapshot instead\n- State set inside the setupFn (e.g. window.localStorage.setItem(...)) is captured in the snapshot — this is specifically the sanctioned exception to 7.1's \"assume nothing persists,\" unlike setting storage as a bare step in a test body\n- validate() is what makes the cache trustworthy over a long run: re-checked every time a cached session is about to be restored; on failure, Cypress transparently discards the cache and reruns setupFn fresh — turns \"silently stale session\" into a self-healing non-issue\n- cacheAcrossSpecs: true extends the cache across every spec file in one cypress run invocation, not just within one file — the first spec pays the cost once for the whole run\n- Sessions are keyed by the name/object argument — different roles get independent caches automatically, and switching between them mid-spec (loginAs('employee') → loginAs('manager')) is cheap every time, not just the first\n- cy.session() resets the page to about:blank afterward — always pair it with an explicit cy.visit(); forgetting this is the most common first-time mistake\n- The setup function can be a full UI login, not just an API call — the caching benefit is \"pay the real cost exactly once, however expensive,\" not \"you must use cy.request().\" Essential for apps where no clean login API exists (SSO — see 7.4)\n- vs Playwright: storageState is the same idea as an explicit, file-based artifact generated in a separate global-setup script — persists across separate CLI invocations (Cypress's cache does not, even with cacheAcrossSpecs), at the cost of more upfront wiring. vs Selenium: no equivalent whatsoever — session reuse is hand-rolled (manual cookie serialization) or, commonly, just not done, a real contributor to Selenium suites' reputation for being slow at scale",
  "contentMarkdown": "The mechanism that reconciles two things that otherwise pull against each other: test isolation demanding a fresh login every test, and performance demanding you don't actually pay for that every time.\n\n## The problem it solves, restated precisely\n\nPart 6.1 established that an API login costs roughly 100–300ms versus a UI login's 3–5 seconds. But even 200ms, repeated as a real network round-trip in every single beforeEach across a 150-test suite, adds up to real time — and more importantly, it's genuinely wasteful: the login itself isn't what most of those tests are about, and re-authenticating identically every single time provides zero additional test coverage over authenticating once and restoring that state.\n`cy.session()` caches the result of an authentication flow — the cookies and storage state it produces — keyed by a name you choose, and restores it on subsequent calls without re-running the login logic at all, while still fully honoring test isolation's guarantee that each test starts clean.\n\n## Basic usage\n\n```js\nCypress.Commands.add('loginAs', (role) => {\n  cy.session(role, () => {\n    cy.fixture('users').then((users) => {\n      const { email, password } = users[role];\n      cy.request('POST', '/api/auth/login', { email, password }).then((response) => {\n        window.localStorage.setItem('authToken', response.body.token);\n      });\n    });\n  });\n});\n\nbeforeEach(() => {\n  cy.loginAs('manager');\n  cy.visit('/leave/approvals');\n});\n```\n\nThe first time `cy.session('manager', setupFn)` runs in a spec, Cypress executes setupFn in full — the real login happens. Cypress then snapshots the resulting cookies and storage. Every subsequent call to `cy.session('manager', ...)` — in later tests, even in different spec files within the same run — skips setupFn entirely and instead restores the snapshotted cookies/storage directly. No network call, no re-running your login logic — just applying the saved state.\n\n## Why the window.localStorage assignment above actually works now\n\nThis directly resolves the subtlety flagged in Part 6.1: setting localStorage inside a `cy.session()` setup callback is captured as part of the session snapshot, unlike setting it as a bare step in a test body (which would be wiped by the next test-isolation reset). `cy.session()` is specifically designed to be the sanctioned place to establish state that should survive and be restored — it's the deliberate exception to \"assume nothing persists\" from 7.1.\n\n## The validation option — making the cache trustworthy\n\nA cached session is only useful if it's actually still valid. `cy.session()` accepts a validate function, run every time a cached session is about to be restored, to confirm the restored state is genuinely still good:\n\n```js\nCypress.Commands.add('loginAs', (role) => {\n  cy.session(\n    role,\n    () => {\n      cy.fixture('users').then((users) => {\n        const { email, password } = users[role];\n        cy.request('POST', '/api/auth/login', { email, password }).then((response) => {\n          window.localStorage.setItem('authToken', response.body.token);\n        });\n      });\n    },\n    {\n      validate() {\n        cy.request({\n          url: '/api/auth/me',\n          headers: { Authorization: `Bearer ${window.localStorage.getItem('authToken')}` },\n          failOnStatusCode: false,\n        }).its('status').should('eq', 200);\n      },\n    }\n  );\n});\n```\n\nIf validate() fails — say, the cached token expired mid-run, or a previous test explicitly logged out and mutated the stored token — Cypress discards the cached session and re-runs the setup function fresh, transparently. This is what makes `cy.session()` safe to rely on across a long-running suite rather than a source of \"works on the first test, mysteriously fails on the fortieth\" bugs: staleness is actively detected and self-healed rather than silently trusted forever.\n\n## cacheAcrossSpecs — extending the cache beyond one file\n\nBy default, a session cache lives for the duration of a single cypress run invocation but is scoped per spec file execution in some configurations; explicitly opting into cross-spec caching squeezes out further savings on large suites:\n\n```js\ncy.session(role, setupFn, {\n  validate() { /* ... */ },\n  cacheAcrossSpecs: true,\n});\n```\n\nWith this set, the very first spec file in the run pays the real login cost once, and every subsequent spec file in the same cypress run invocation restores from that same cache — genuinely amortizing the cost across the entire suite rather than per file.\n\n## Sessions are keyed — different arguments, different caches\n\nThe session name (first argument) is the cache key. Different roles naturally get independent caches:\n\n```js\ncy.loginAs('admin');      // cached under 'admin'\n```\n`cy.loginAs('manager')`;    // cached under 'manager' — a completely separate session\n\n```js\ncy.loginAs('employee');   // cached under 'employee'\n```\n\nThis means a spec file that needs to test behavior across multiple roles can freely switch between them, and each restore is still fast:\n\n```js\nit('a manager can see what an employee cannot', () => {\n  cy.loginAs('employee');\n  cy.visit('/leave/approvals');\n  cy.get('[data-cy=approve-btn]').should('not.exist');   // employees can't approve\n```\n\n`cy.loginAs('manager')`;    // switches to a DIFFERENT cached session — fast, no re-login cost\n\n```js\n  cy.visit('/leave/approvals');\n  cy.get('[data-cy=approve-btn]').should('exist');\n});\n```\n\nYou can also key sessions on more than a role name when the underlying state genuinely differs per test — an object serializes into a stable key:\n\n```js\ncy.session({ role: 'manager', team: 'engineering' }, setupFn);\n```\n\n## cy.session() resets the page — the behavior worth knowing precisely\n\nAfter restoring or establishing a session, Cypress clears the page (navigates to about:blank) — it does not automatically leave you on whatever page the setup function last visited. This means `cy.session()` should be paired with an explicit `cy.visit()` afterward, which is exactly the pattern shown throughout this chapter (`cy.loginAs(role)`; `cy.visit(path)`;). Forgetting the follow-up `cy.visit()` is a common first-time mistake — the session is established correctly, but the test is left looking at a blank page.\n\n## UI-based sessions — when login genuinely can't be bypassed via API\n\nNot every application exposes a clean login API endpoint a test can call directly — some are behind SSO flows, some have login logic with side effects that must run through the actual UI (MFA challenges, terms-acceptance modals tied to session creation). `cy.session()` supports a full UI login inside its setup function just as well as an API call, and still gets the caching benefit — the expensive part only happens once, regardless of how expensive it is:\n\n```js\nCypress.Commands.add('loginAs', (role) => {\n  cy.session(role, () => {\n    cy.fixture('users').then((users) => {\n      const { email, password } = users[role];\n      cy.visit('/login');\n      cy.get('[data-cy=email]').type(email);\n      cy.get('[data-cy=password]').type(password, { log: false });\n      cy.get('[data-cy=submit]').click();\n      cy.url().should('include', '/dashboard');   // anchor: confirm login actually completed\n    });\n  });\n});\n```\n\nThis is a genuinely important pattern to know: `cy.session()`'s value isn't \"always use `cy.request()` instead of the UI\" — it's \"pay the real cost exactly once, however expensive that real cost is.\" For an app where the only reliable way to authenticate is the full UI flow, this still delivers nearly all the same performance benefit as the API-shortcut version, because the 3–5 second UI login now happens once per role per run instead of once per test.\n\n## Comparison\n\n```js\n// Playwright — storageState as an explicit, file-based, cross-run artifact\n// global-setup.ts\nconst browser = await chromium.launch();\nconst page = await browser.newPage();\nawait page.goto('/login');\nawait page.fill('[data-cy=email]', 'manager@bizlevate.test');\nawait page.fill('[data-cy=password]', 'pw');\nawait page.click('[data-cy=submit]');\nawait page.context().storageState({ path: 'auth/manager.json' });\n\n// playwright.config.ts\nprojects: [\n  { name: 'as-manager', use: { storageState: 'auth/manager.json' } },\n]\n```\n\nPlaywright's version is philosophically the same idea — pay the login cost once, restore many times — but implemented as an explicit file artifact generated in a separate global-setup script and wired in via project configuration, rather than an in-memory cache keyed by a string inside the test file itself. Playwright's approach persists across separate npx playwright test invocations (the file's still on disk tomorrow), whereas Cypress's `cy.session()` cache — even with `cacheAcrossSpecs`: true — lives only for the duration of one cypress run. That's a real trade-off: Cypress's version requires zero separate setup infrastructure and works immediately inside any test file; Playwright's version requires more upfront wiring but persists further and is more explicitly inspectable (you can literally open the JSON file).\nSelenium has no equivalent mechanism at all. Session reuse in a Selenium suite is either hand-rolled (serialize cookies to a file yourself, load them back with driver.add_cookie() in later tests) or simply not done, with every test paying the full UI login cost — which is a large part of why Selenium-based E2E suites have a longstanding reputation for being slow at scale, beyond just the WebDriver protocol overhead itself.",
  "blocks": [
    {
      "id": "cy-7-2-md-0",
      "type": "overview",
      "content": "The mechanism that reconciles two things that otherwise pull against each other: test isolation demanding a fresh login every test, and performance demanding you don't actually pay for that every time.",
      "order": 0
    },
    {
      "id": "cy-7-2-md-1",
      "type": "overview",
      "heading": "The problem it solves, restated precisely",
      "content": "Part 6.1 established that an API login costs roughly 100–300ms versus a UI login's 3–5 seconds. But even 200ms, repeated as a real network round-trip in every single beforeEach across a 150-test suite, adds up to real time — and more importantly, it's genuinely wasteful: the login itself isn't what most of those tests are about, and re-authenticating identically every single time provides zero additional test coverage over authenticating once and restoring that state.\n`cy.session()` caches the result of an authentication flow — the cookies and storage state it produces — keyed by a name you choose, and restores it on subsequent calls without re-running the login logic at all, while still fully honoring test isolation's guarantee that each test starts clean.",
      "order": 1
    },
    {
      "id": "cy-7-2-md-2",
      "type": "overview",
      "heading": "Basic usage",
      "content": "```js\nCypress.Commands.add('loginAs', (role) => {\n  cy.session(role, () => {\n    cy.fixture('users').then((users) => {\n      const { email, password } = users[role];\n      cy.request('POST', '/api/auth/login', { email, password }).then((response) => {\n        window.localStorage.setItem('authToken', response.body.token);\n      });\n    });\n  });\n});\n\nbeforeEach(() => {\n  cy.loginAs('manager');\n  cy.visit('/leave/approvals');\n});\n```\n\nThe first time `cy.session('manager', setupFn)` runs in a spec, Cypress executes setupFn in full — the real login happens. Cypress then snapshots the resulting cookies and storage. Every subsequent call to `cy.session('manager', ...)` — in later tests, even in different spec files within the same run — skips setupFn entirely and instead restores the snapshotted cookies/storage directly. No network call, no re-running your login logic — just applying the saved state.",
      "order": 2
    },
    {
      "id": "cy-7-2-md-3",
      "type": "overview",
      "heading": "Why the window.localStorage assignment above actually works now",
      "content": "This directly resolves the subtlety flagged in Part 6.1: setting localStorage inside a `cy.session()` setup callback is captured as part of the session snapshot, unlike setting it as a bare step in a test body (which would be wiped by the next test-isolation reset). `cy.session()` is specifically designed to be the sanctioned place to establish state that should survive and be restored — it's the deliberate exception to \"assume nothing persists\" from 7.1.",
      "order": 3
    },
    {
      "id": "cy-7-2-md-4",
      "type": "overview",
      "heading": "The validation option — making the cache trustworthy",
      "content": "A cached session is only useful if it's actually still valid. `cy.session()` accepts a validate function, run every time a cached session is about to be restored, to confirm the restored state is genuinely still good:\n\n```js\nCypress.Commands.add('loginAs', (role) => {\n  cy.session(\n    role,\n    () => {\n      cy.fixture('users').then((users) => {\n        const { email, password } = users[role];\n        cy.request('POST', '/api/auth/login', { email, password }).then((response) => {\n          window.localStorage.setItem('authToken', response.body.token);\n        });\n      });\n    },\n    {\n      validate() {\n        cy.request({\n          url: '/api/auth/me',\n          headers: { Authorization: `Bearer ${window.localStorage.getItem('authToken')}` },\n          failOnStatusCode: false,\n        }).its('status').should('eq', 200);\n      },\n    }\n  );\n});\n```\n\nIf validate() fails — say, the cached token expired mid-run, or a previous test explicitly logged out and mutated the stored token — Cypress discards the cached session and re-runs the setup function fresh, transparently. This is what makes `cy.session()` safe to rely on across a long-running suite rather than a source of \"works on the first test, mysteriously fails on the fortieth\" bugs: staleness is actively detected and self-healed rather than silently trusted forever.",
      "order": 4
    },
    {
      "id": "cy-7-2-md-5",
      "type": "overview",
      "heading": "cacheAcrossSpecs — extending the cache beyond one file",
      "content": "By default, a session cache lives for the duration of a single cypress run invocation but is scoped per spec file execution in some configurations; explicitly opting into cross-spec caching squeezes out further savings on large suites:\n\n```js\ncy.session(role, setupFn, {\n  validate() { /* ... */ },\n  cacheAcrossSpecs: true,\n});\n```\n\nWith this set, the very first spec file in the run pays the real login cost once, and every subsequent spec file in the same cypress run invocation restores from that same cache — genuinely amortizing the cost across the entire suite rather than per file.",
      "order": 5
    },
    {
      "id": "cy-7-2-md-6",
      "type": "overview",
      "heading": "Sessions are keyed — different arguments, different caches",
      "content": "The session name (first argument) is the cache key. Different roles naturally get independent caches:\n\n```js\ncy.loginAs('admin');      // cached under 'admin'\n```\n`cy.loginAs('manager')`;    // cached under 'manager' — a completely separate session\n\n```js\ncy.loginAs('employee');   // cached under 'employee'\n```\n\nThis means a spec file that needs to test behavior across multiple roles can freely switch between them, and each restore is still fast:\n\n```js\nit('a manager can see what an employee cannot', () => {\n  cy.loginAs('employee');\n  cy.visit('/leave/approvals');\n  cy.get('[data-cy=approve-btn]').should('not.exist');   // employees can't approve\n```\n\n`cy.loginAs('manager')`;    // switches to a DIFFERENT cached session — fast, no re-login cost\n\n```js\n  cy.visit('/leave/approvals');\n  cy.get('[data-cy=approve-btn]').should('exist');\n});\n```\n\nYou can also key sessions on more than a role name when the underlying state genuinely differs per test — an object serializes into a stable key:\n\n```js\ncy.session({ role: 'manager', team: 'engineering' }, setupFn);\n```",
      "order": 6
    },
    {
      "id": "cy-7-2-md-7",
      "type": "overview",
      "heading": "cy.session() resets the page — the behavior worth knowing precisely",
      "content": "After restoring or establishing a session, Cypress clears the page (navigates to about:blank) — it does not automatically leave you on whatever page the setup function last visited. This means `cy.session()` should be paired with an explicit `cy.visit()` afterward, which is exactly the pattern shown throughout this chapter (`cy.loginAs(role)`; `cy.visit(path)`;). Forgetting the follow-up `cy.visit()` is a common first-time mistake — the session is established correctly, but the test is left looking at a blank page.",
      "order": 7
    },
    {
      "id": "cy-7-2-md-8",
      "type": "overview",
      "heading": "UI-based sessions — when login genuinely can't be bypassed via API",
      "content": "Not every application exposes a clean login API endpoint a test can call directly — some are behind SSO flows, some have login logic with side effects that must run through the actual UI (MFA challenges, terms-acceptance modals tied to session creation). `cy.session()` supports a full UI login inside its setup function just as well as an API call, and still gets the caching benefit — the expensive part only happens once, regardless of how expensive it is:\n\n```js\nCypress.Commands.add('loginAs', (role) => {\n  cy.session(role, () => {\n    cy.fixture('users').then((users) => {\n      const { email, password } = users[role];\n      cy.visit('/login');\n      cy.get('[data-cy=email]').type(email);\n      cy.get('[data-cy=password]').type(password, { log: false });\n      cy.get('[data-cy=submit]').click();\n      cy.url().should('include', '/dashboard');   // anchor: confirm login actually completed\n    });\n  });\n});\n```\n\nThis is a genuinely important pattern to know: `cy.session()`'s value isn't \"always use `cy.request()` instead of the UI\" — it's \"pay the real cost exactly once, however expensive that real cost is.\" For an app where the only reliable way to authenticate is the full UI flow, this still delivers nearly all the same performance benefit as the API-shortcut version, because the 3–5 second UI login now happens once per role per run instead of once per test.",
      "order": 8
    },
    {
      "id": "cy-7-2-md-9",
      "type": "overview",
      "heading": "Comparison",
      "content": "```js\n// Playwright — storageState as an explicit, file-based, cross-run artifact\n// global-setup.ts\nconst browser = await chromium.launch();\nconst page = await browser.newPage();\nawait page.goto('/login');\nawait page.fill('[data-cy=email]', 'manager@bizlevate.test');\nawait page.fill('[data-cy=password]', 'pw');\nawait page.click('[data-cy=submit]');\nawait page.context().storageState({ path: 'auth/manager.json' });\n\n// playwright.config.ts\nprojects: [\n  { name: 'as-manager', use: { storageState: 'auth/manager.json' } },\n]\n```\n\nPlaywright's version is philosophically the same idea — pay the login cost once, restore many times — but implemented as an explicit file artifact generated in a separate global-setup script and wired in via project configuration, rather than an in-memory cache keyed by a string inside the test file itself. Playwright's approach persists across separate npx playwright test invocations (the file's still on disk tomorrow), whereas Cypress's `cy.session()` cache — even with `cacheAcrossSpecs`: true — lives only for the duration of one cypress run. That's a real trade-off: Cypress's version requires zero separate setup infrastructure and works immediately inside any test file; Playwright's version requires more upfront wiring but persists further and is more explicitly inspectable (you can literally open the JSON file).\nSelenium has no equivalent mechanism at all. Session reuse in a Selenium suite is either hand-rolled (serialize cookies to a file yourself, load them back with driver.add_cookie() in later tests) or simply not done, with every test paying the full UI login cost — which is a large part of why Selenium-based E2E suites have a longstanding reputation for being slow at scale, beyond just the WebDriver protocol overhead itself.",
      "order": 9
    }
  ],
  "advantages": [
    "7.2 cy.session() for Login Caching — Isolation and performance pull against each other; cy."
  ],
  "limitations": [
    "7.2 cy.session() for Login Caching is this Part's slice only; later chapters go deeper rather than repeating this one."
  ],
  "exercises": [],
  "resourceLinks": [],
  "steps": [],
  "learn": []
} as ChapterRecord;
