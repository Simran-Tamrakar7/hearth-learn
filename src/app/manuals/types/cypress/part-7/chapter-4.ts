import type { ChapterRecord } from "../../../types";

/** 7.4 Handling Authentication */
export const chapter = {
  "id": "cy-7-4-handling-authentication",
  "title": "7.4 Handling Authentication",
  "minutes": 32,
  "level": "intermediate",
  "phase": "Part 7 · State Management & Test Isolation",
  "partName": "Part 7 · State Management & Test Isolation",
  "overviewText": "UI login is for testing login itself (validation, lockout, MFA). Everything else uses programmatic login cached in cy.session(). Token apps set localStorage inside the session setup; cookie apps let cy.request() apply Set-Cookie. SSO: prefer a test-tenant grant; else cache a real cy.origin() UI login inside session; else stub the IdP. Logout tests must re-visit a protected route, not just check the URL.",
  "why": "A mature suite is not either/or. The canonical cy.loginAs(role) command is the payoff of Parts 6–7. SSO via cy.origin() inside session is the interview answer for Okta/Azure AD.",
  "when": "On day one of an HRM suite, when adding RBAC tests across employee/manager/admin, and when login redirects through login.okta.com.",
  "practical": {
    "app": "Bizlevate HRM",
    "scenario": "Login-form lockout tests, 80 leave tests needing a manager, Okta SSO, and payroll RBAC across three roles.",
    "pass": "You UI-test only the login form; cy.loginAs uses session+validate+cacheAcrossSpecs; SSO setup wraps Okta in cy.origin inside session; RBAC switches roles cheaply.",
    "fail": "You UI-login every leave test, skip logout's protected-route revisit, or try cy.request() against Okta's login page as if it were a password API."
  },
  "tools": [],
  "customSummary": "- The core decision, and it's not either/or: UI login for testing the login mechanism itself (validation, lockout, MFA, \"remember me\"); programmatic login (cached via cy.session()) for every other test that merely requires an authenticated session as a precondition — the large majority of a real suite\n- Canonical token-based pattern (brings together 6.1 + 7.1 + 7.2): cy.session(role, () => { cy.request POST /login → set localStorage }, { validate() {...}, cacheAcrossSpecs: true }), exposed as a typed cy.loginAs(role) custom command\n- Cookie-based variant is simpler: no manual storage step needed, since cy.request() in the same browser context applies Set-Cookie automatically\n- SSO/OAuth resists the clean shortcut because many identity providers deliberately offer no simple credential API. Three strategies in preference order: (1) a test-tenant / non-interactive grant if the provider offers one — functionally identical to the token pattern; (2) cache a real UI-driven SSO login inside cy.session(), using cy.origin() (Part 9.1) to cross into the identity-provider domain — still only pays the real cross-origin cost once per role per run; (3) stub the identity provider via cy.intercept() when realism can be traded for speed on the large majority of tests that don't care the handshake itself was faked\n- Multi-role testing is made cheap by cy.session()'s per-role keying — switching identities mid-suite to test access control restores near-instantly each time, while still genuinely exercising distinct authenticated identities\n- Logout deserves its own honest UI-driven test — and the strong version of that test doesn't just check the UI looks logged out, it re-visits a protected route afterward to confirm the app actually enforces the logged-out state, not merely displays it\n- vs Playwright: identical strategic split, built on storageState; SSO handling is simpler — no cy.origin()-equivalent wrapper needed since Playwright was never origin-bound, so a global-setup SSO script reads as one continuous flow. vs Selenium: same UI-vs-programmatic split via a paired HTTP client and manual add_cookie() seeding, but no equivalent to cy.session()'s automatic caching-with-validation — teams hand-roll session reuse or simply accept paying full login cost per test",
  "contentMarkdown": "Bringing 7.1's storage mechanics, 7.2's caching, and 7.3's isolation policy together into the concrete decision every Cypress project has to make on day one: how do logged-in tests actually get logged in.\n\n## The two fundamental approaches, restated as a real decision\n\nUI login — drive the actual login form: type credentials, click submit, wait for redirect. Slow, but exercises real code, including the login form itself.\nProgrammatic login — bypass the form entirely: call a login API directly (`cy.request()`, Part 6.1) or seed storage directly (Part 7.1), and land on an authenticated page without ever touching a login <input>.\nThese aren't competing options for the same purpose — they answer different questions, and a mature suite uses both, deliberately, for different tests.\n\n## When UI login is the right choice\n\nTesting the login form itself is, definitionally, a UI-login test — validation messages, \"remember me\" behavior, password visibility toggles, lockout after failed attempts, MFA challenges:\n\n```js\ndescribe('Login form', () => {\n  it('shows an error for invalid credentials', () => {\n    cy.visit('/login');\n    cy.get('[data-cy=email]').type('wrong@bizlevate.test');\n    cy.get('[data-cy=password]').type('wrongpassword');\n    cy.get('[data-cy=submit]').click();\n    cy.get('[data-cy=login-error]').should('contain', 'Invalid email or password');\n    cy.url().should('include', '/login');\n  });\n\n  it('redirects to the dashboard on success', () => {\n    cy.visit('/login');\n    cy.get('[data-cy=email]').type('manager@bizlevate.test');\n    cy.get('[data-cy=password]').type('correctpassword');\n    cy.get('[data-cy=submit]').click();\n    cy.url().should('include', '/dashboard');\n  });\n\n  it('locks the account after 5 failed attempts', () => {\n    cy.visit('/login');\n    for (let i = 0; i < 5; i++) {\n      cy.get('[data-cy=email]').clear().type('manager@bizlevate.test');\n      cy.get('[data-cy=password]').clear().type('wrongpassword');\n      cy.get('[data-cy=submit]').click();\n    }\n    cy.get('[data-cy=login-error]').should('contain', 'Account locked');\n  });\n});\n```\n\nThis handful of tests is precisely what should exercise the real form — nothing here can be meaningfully shortcut, because the login mechanism itself is the subject under test.\n\n## When programmatic login is the right choice — everything else\n\nEvery other test in the suite that merely requires a logged-in session as a precondition, but isn't testing the login mechanism itself, should use programmatic login. This is the overwhelming majority of a real application's test suite:\n\n```js\ndescribe('Leave approvals', () => {\n  beforeEach(() => {\n```\n`cy.loginAs('manager')`;    // programmatic, cached via `cy.session()` — Part 7.2\n\n```js\n    cy.visit('/leave/approvals');\n  });\n\n  it('shows pending requests', () => { /* ... this test is about leave approvals, not login */ });\n});\n```\n\n## The token-based programmatic pattern, complete\n\nBringing together everything from 6.1, 7.1, and 7.2 into one canonical, reusable implementation:\n\n```js\n// cypress/support/commands.js\nCypress.Commands.add('loginAs', (role) => {\n  cy.session(\n    role,\n    () => {\n      cy.fixture('users').then((users) => {\n        const { email, password } = users[role];\n        cy.request('POST', '/api/auth/login', { email, password }).then((response) => {\n          window.localStorage.setItem('authToken', response.body.token);\n          window.localStorage.setItem('refreshToken', response.body.refreshToken);\n        });\n      });\n    },\n    {\n      validate() {\n        const token = window.localStorage.getItem('authToken');\n        cy.request({\n          url: '/api/auth/me',\n          headers: { Authorization: `Bearer ${token}` },\n          failOnStatusCode: false,\n        }).its('status').should('eq', 200);\n      },\n      cacheAcrossSpecs: true,\n    }\n  );\n});\n\n// cypress/support/index.d.ts\ndeclare global {\n  namespace Cypress {\n    interface Chainable {\n      loginAs(role: 'admin' | 'manager' | 'employee'): Chainable<void>;\n    }\n  }\n}\n```\n\nThis single command — cached, validated, typed — is what every test in the suite that merely needs to be logged in should call. It's the payoff for everything built up across this Part.\n\n## The cookie-based variant\n\nFor apps using server-set session cookies rather than a client-stored JWT, the pattern simplifies, since `cy.request()` running in the same browser context applies Set-Cookie automatically (as noted in 6.1):\n\n```js\nCypress.Commands.add('loginAs', (role) => {\n  cy.session(role, () => {\n    cy.fixture('users').then((users) => {\n      cy.request('POST', '/api/auth/login', users[role]);\n      // no manual storage step needed — the session cookie is applied automatically\n    });\n  }, {\n    validate() {\n      cy.request({ url: '/api/auth/me', failOnStatusCode: false }).its('status').should('eq', 200);\n    },\n  });\n});\n```\n\n## SSO / OAuth — the case that resists a clean shortcut\n\nRecall the SSO scenario from Part 0.6: your app redirects to login.okta.com (or Azure AD, Google Workspace) and back. A pure `cy.request()`-based login often isn't available here at all — many SSO providers don't expose a simple username/password API endpoint by design, precisely because they want authentication to happen through their own controlled UI.\nThree realistic strategies, in order of preference:\n1. Ask the SSO provider for a test/service-account bypass. Many enterprise identity providers offer a non-interactive grant type (client credentials, or a dedicated test-tenant API) specifically for automated testing. If this exists, it's functionally equivalent to the token-based pattern above — get a token programmatically, seed it, skip the UI entirely.\n2. Cache a real UI-driven SSO login via `cy.session()`. When no programmatic bypass exists, fall back to what 7.2 already demonstrated — a full UI login inside the session setup function, using `cy.origin()` (Part 9.1) to cross into the SSO domain:\n\n```js\nCypress.Commands.add('loginAs', (role) => {\n  cy.session(role, () => {\n    cy.fixture('users').then((users) => {\n      const { email, password } = users[role];\n      cy.visit('/login');   // app redirects to the SSO provider\n\n      cy.origin('https://login.okta.com', { args: { email, password } }, ({ email, password }) => {\n        cy.get('#username').type(email);\n        cy.get('#password').type(password);\n        cy.get('button[type=submit]').click();\n      });\n\n      cy.url().should('include', '/dashboard');   // back on the app's own origin\n    });\n  });\n});\n```\n\nThis still only pays the real (slow, cross-origin) cost once per role per run — `cy.session()`'s caching benefit applies just as fully here as it does to the pure-API case, because the caching layer doesn't care how expensive or complex the setup function was, only that it ran once and its result can be restored.\n3. Stub the SSO provider's response entirely via cy.intercept(), if your architecture allows it — treating the identity provider as an external dependency to mock rather than a real system to authenticate against, similar in spirit to the payment-widget stubbing discussed in Part 3.6. This trades realism for speed and reliability, and is a defensible choice specifically for the large majority of tests that need a logged-in session and don't care that the SSO handshake itself was faked.\n\n## Multi-role testing within one spec\n\nA pattern that ties 7.1 through 7.4 together concretely — verifying role-based access control requires genuinely switching identities mid-suite, which `cy.session()`'s per-role keying (7.2) makes cheap:\n\n```js\ndescribe('Role-based access to payroll', () => {\n  it('employees cannot access payroll admin', () => {\n    cy.loginAs('employee');\n    cy.visit('/payroll/admin', { failOnStatusCode: false });\n    cy.get('[data-cy=forbidden]').should('be.visible');\n  });\n\n  it('managers can view but not run payroll', () => {\n    cy.loginAs('manager');\n    cy.visit('/payroll/admin');\n    cy.get('[data-cy=run-payroll-btn]').should('not.exist');\n    cy.get('[data-cy=payroll-summary]').should('be.visible');\n  });\n\n  it('admins can run payroll', () => {\n    cy.loginAs('admin');\n    cy.visit('/payroll/admin');\n    cy.get('[data-cy=run-payroll-btn]').should('be.visible').and('be.enabled');\n  });\n});\n```\n\nEach `cy.loginAs()` call here restores a distinct cached session almost instantly — none of the cost of three separate real logins, while still genuinely exercising three separate authenticated identities.\n\n## Testing logout\n\nLogout deserves its own honest test, and it's naturally a UI-driven one since the logout mechanism itself is what's under test:\n\n```js\nit('clears the session on logout and redirects to login', () => {\n  cy.loginAs('employee');\n  cy.visit('/dashboard');\n\n  cy.get('[data-cy=logout]').click();\n\n  cy.url().should('include', '/login');\n  cy.window().its('localStorage.authToken').should('be.undefined');\n  cy.getCookie('session_id').should('be.null');\n\n  // and confirm a subsequent protected-route visit is actually blocked, not just visually redirected\n  cy.visit('/dashboard');\n  cy.url().should('include', '/login');\n});\n```\n\nThat last assertion matters — it distinguishes \"the UI looks logged out\" from \"the app actually enforces the logged-out state,\" which is a meaningfully stronger and more honest test.\n\n## Comparison\n\nThe strategic split — UI login for testing login itself, programmatic login (cached) for everything else — is identical in Playwright, built on `storageState` instead of `cy.session()`, with the same SSO fallback options (a test-tenant bypass, a cached real UI flow via page.context().`storageState`(), or stubbing the identity provider via page.route()). Playwright's cross-origin handling for the SSO case is meaningfully simpler, needing no `cy.origin()`-equivalent wrapper at all, since Playwright was never origin-bound to begin with (Part 0.6) — the SSO login script in a Playwright global-setup file reads as one continuous flow with no special block required for the identity-provider domain.\nSelenium implements the same UI-vs-programmatic split via whatever HTTP client is paired alongside it for the programmatic path (as in Part 6.1's comparison), and handles cookie-seeding manually via driver.add_cookie() after an initial driver.get() to the target domain — functionally similar to Cypress's `onBeforeLoad` pattern in spirit, but with no equivalent to `cy.session()`'s automatic caching-with-validation, meaning teams either hand-roll their own session-reuse layer or, commonly, simply accept the cost of a real login per test.",
  "blocks": [
    {
      "id": "cy-7-4-md-0",
      "type": "overview",
      "content": "Bringing 7.1's storage mechanics, 7.2's caching, and 7.3's isolation policy together into the concrete decision every Cypress project has to make on day one: how do logged-in tests actually get logged in.",
      "order": 0
    },
    {
      "id": "cy-7-4-md-1",
      "type": "overview",
      "heading": "The two fundamental approaches, restated as a real decision",
      "content": "UI login — drive the actual login form: type credentials, click submit, wait for redirect. Slow, but exercises real code, including the login form itself.\nProgrammatic login — bypass the form entirely: call a login API directly (`cy.request()`, Part 6.1) or seed storage directly (Part 7.1), and land on an authenticated page without ever touching a login <input>.\nThese aren't competing options for the same purpose — they answer different questions, and a mature suite uses both, deliberately, for different tests.",
      "order": 1
    },
    {
      "id": "cy-7-4-md-2",
      "type": "overview",
      "heading": "When UI login is the right choice",
      "content": "Testing the login form itself is, definitionally, a UI-login test — validation messages, \"remember me\" behavior, password visibility toggles, lockout after failed attempts, MFA challenges:\n\n```js\ndescribe('Login form', () => {\n  it('shows an error for invalid credentials', () => {\n    cy.visit('/login');\n    cy.get('[data-cy=email]').type('wrong@bizlevate.test');\n    cy.get('[data-cy=password]').type('wrongpassword');\n    cy.get('[data-cy=submit]').click();\n    cy.get('[data-cy=login-error]').should('contain', 'Invalid email or password');\n    cy.url().should('include', '/login');\n  });\n\n  it('redirects to the dashboard on success', () => {\n    cy.visit('/login');\n    cy.get('[data-cy=email]').type('manager@bizlevate.test');\n    cy.get('[data-cy=password]').type('correctpassword');\n    cy.get('[data-cy=submit]').click();\n    cy.url().should('include', '/dashboard');\n  });\n\n  it('locks the account after 5 failed attempts', () => {\n    cy.visit('/login');\n    for (let i = 0; i < 5; i++) {\n      cy.get('[data-cy=email]').clear().type('manager@bizlevate.test');\n      cy.get('[data-cy=password]').clear().type('wrongpassword');\n      cy.get('[data-cy=submit]').click();\n    }\n    cy.get('[data-cy=login-error]').should('contain', 'Account locked');\n  });\n});\n```\n\nThis handful of tests is precisely what should exercise the real form — nothing here can be meaningfully shortcut, because the login mechanism itself is the subject under test.",
      "order": 2
    },
    {
      "id": "cy-7-4-md-3",
      "type": "overview",
      "heading": "When programmatic login is the right choice — everything else",
      "content": "Every other test in the suite that merely requires a logged-in session as a precondition, but isn't testing the login mechanism itself, should use programmatic login. This is the overwhelming majority of a real application's test suite:\n\n```js\ndescribe('Leave approvals', () => {\n  beforeEach(() => {\n```\n`cy.loginAs('manager')`;    // programmatic, cached via `cy.session()` — Part 7.2\n\n```js\n    cy.visit('/leave/approvals');\n  });\n\n  it('shows pending requests', () => { /* ... this test is about leave approvals, not login */ });\n});\n```",
      "order": 3
    },
    {
      "id": "cy-7-4-md-4",
      "type": "overview",
      "heading": "The token-based programmatic pattern, complete",
      "content": "Bringing together everything from 6.1, 7.1, and 7.2 into one canonical, reusable implementation:\n\n```js\n// cypress/support/commands.js\nCypress.Commands.add('loginAs', (role) => {\n  cy.session(\n    role,\n    () => {\n      cy.fixture('users').then((users) => {\n        const { email, password } = users[role];\n        cy.request('POST', '/api/auth/login', { email, password }).then((response) => {\n          window.localStorage.setItem('authToken', response.body.token);\n          window.localStorage.setItem('refreshToken', response.body.refreshToken);\n        });\n      });\n    },\n    {\n      validate() {\n        const token = window.localStorage.getItem('authToken');\n        cy.request({\n          url: '/api/auth/me',\n          headers: { Authorization: `Bearer ${token}` },\n          failOnStatusCode: false,\n        }).its('status').should('eq', 200);\n      },\n      cacheAcrossSpecs: true,\n    }\n  );\n});\n\n// cypress/support/index.d.ts\ndeclare global {\n  namespace Cypress {\n    interface Chainable {\n      loginAs(role: 'admin' | 'manager' | 'employee'): Chainable<void>;\n    }\n  }\n}\n```\n\nThis single command — cached, validated, typed — is what every test in the suite that merely needs to be logged in should call. It's the payoff for everything built up across this Part.",
      "order": 4
    },
    {
      "id": "cy-7-4-md-5",
      "type": "overview",
      "heading": "The cookie-based variant",
      "content": "For apps using server-set session cookies rather than a client-stored JWT, the pattern simplifies, since `cy.request()` running in the same browser context applies Set-Cookie automatically (as noted in 6.1):\n\n```js\nCypress.Commands.add('loginAs', (role) => {\n  cy.session(role, () => {\n    cy.fixture('users').then((users) => {\n      cy.request('POST', '/api/auth/login', users[role]);\n      // no manual storage step needed — the session cookie is applied automatically\n    });\n  }, {\n    validate() {\n      cy.request({ url: '/api/auth/me', failOnStatusCode: false }).its('status').should('eq', 200);\n    },\n  });\n});\n```",
      "order": 5
    },
    {
      "id": "cy-7-4-md-6",
      "type": "overview",
      "heading": "SSO / OAuth — the case that resists a clean shortcut",
      "content": "Recall the SSO scenario from Part 0.6: your app redirects to login.okta.com (or Azure AD, Google Workspace) and back. A pure `cy.request()`-based login often isn't available here at all — many SSO providers don't expose a simple username/password API endpoint by design, precisely because they want authentication to happen through their own controlled UI.\nThree realistic strategies, in order of preference:\n1. Ask the SSO provider for a test/service-account bypass. Many enterprise identity providers offer a non-interactive grant type (client credentials, or a dedicated test-tenant API) specifically for automated testing. If this exists, it's functionally equivalent to the token-based pattern above — get a token programmatically, seed it, skip the UI entirely.\n2. Cache a real UI-driven SSO login via `cy.session()`. When no programmatic bypass exists, fall back to what 7.2 already demonstrated — a full UI login inside the session setup function, using `cy.origin()` (Part 9.1) to cross into the SSO domain:\n\n```js\nCypress.Commands.add('loginAs', (role) => {\n  cy.session(role, () => {\n    cy.fixture('users').then((users) => {\n      const { email, password } = users[role];\n      cy.visit('/login');   // app redirects to the SSO provider\n\n      cy.origin('https://login.okta.com', { args: { email, password } }, ({ email, password }) => {\n        cy.get('#username').type(email);\n        cy.get('#password').type(password);\n        cy.get('button[type=submit]').click();\n      });\n\n      cy.url().should('include', '/dashboard');   // back on the app's own origin\n    });\n  });\n});\n```\n\nThis still only pays the real (slow, cross-origin) cost once per role per run — `cy.session()`'s caching benefit applies just as fully here as it does to the pure-API case, because the caching layer doesn't care how expensive or complex the setup function was, only that it ran once and its result can be restored.\n3. Stub the SSO provider's response entirely via cy.intercept(), if your architecture allows it — treating the identity provider as an external dependency to mock rather than a real system to authenticate against, similar in spirit to the payment-widget stubbing discussed in Part 3.6. This trades realism for speed and reliability, and is a defensible choice specifically for the large majority of tests that need a logged-in session and don't care that the SSO handshake itself was faked.",
      "order": 6
    },
    {
      "id": "cy-7-4-md-7",
      "type": "overview",
      "heading": "Multi-role testing within one spec",
      "content": "A pattern that ties 7.1 through 7.4 together concretely — verifying role-based access control requires genuinely switching identities mid-suite, which `cy.session()`'s per-role keying (7.2) makes cheap:\n\n```js\ndescribe('Role-based access to payroll', () => {\n  it('employees cannot access payroll admin', () => {\n    cy.loginAs('employee');\n    cy.visit('/payroll/admin', { failOnStatusCode: false });\n    cy.get('[data-cy=forbidden]').should('be.visible');\n  });\n\n  it('managers can view but not run payroll', () => {\n    cy.loginAs('manager');\n    cy.visit('/payroll/admin');\n    cy.get('[data-cy=run-payroll-btn]').should('not.exist');\n    cy.get('[data-cy=payroll-summary]').should('be.visible');\n  });\n\n  it('admins can run payroll', () => {\n    cy.loginAs('admin');\n    cy.visit('/payroll/admin');\n    cy.get('[data-cy=run-payroll-btn]').should('be.visible').and('be.enabled');\n  });\n});\n```\n\nEach `cy.loginAs()` call here restores a distinct cached session almost instantly — none of the cost of three separate real logins, while still genuinely exercising three separate authenticated identities.",
      "order": 7
    },
    {
      "id": "cy-7-4-md-8",
      "type": "overview",
      "heading": "Testing logout",
      "content": "Logout deserves its own honest test, and it's naturally a UI-driven one since the logout mechanism itself is what's under test:\n\n```js\nit('clears the session on logout and redirects to login', () => {\n  cy.loginAs('employee');\n  cy.visit('/dashboard');\n\n  cy.get('[data-cy=logout]').click();\n\n  cy.url().should('include', '/login');\n  cy.window().its('localStorage.authToken').should('be.undefined');\n  cy.getCookie('session_id').should('be.null');\n\n  // and confirm a subsequent protected-route visit is actually blocked, not just visually redirected\n  cy.visit('/dashboard');\n  cy.url().should('include', '/login');\n});\n```\n\nThat last assertion matters — it distinguishes \"the UI looks logged out\" from \"the app actually enforces the logged-out state,\" which is a meaningfully stronger and more honest test.",
      "order": 8
    },
    {
      "id": "cy-7-4-md-9",
      "type": "overview",
      "heading": "Comparison",
      "content": "The strategic split — UI login for testing login itself, programmatic login (cached) for everything else — is identical in Playwright, built on `storageState` instead of `cy.session()`, with the same SSO fallback options (a test-tenant bypass, a cached real UI flow via page.context().`storageState`(), or stubbing the identity provider via page.route()). Playwright's cross-origin handling for the SSO case is meaningfully simpler, needing no `cy.origin()`-equivalent wrapper at all, since Playwright was never origin-bound to begin with (Part 0.6) — the SSO login script in a Playwright global-setup file reads as one continuous flow with no special block required for the identity-provider domain.\nSelenium implements the same UI-vs-programmatic split via whatever HTTP client is paired alongside it for the programmatic path (as in Part 6.1's comparison), and handles cookie-seeding manually via driver.add_cookie() after an initial driver.get() to the target domain — functionally similar to Cypress's `onBeforeLoad` pattern in spirit, but with no equivalent to `cy.session()`'s automatic caching-with-validation, meaning teams either hand-roll their own session-reuse layer or, commonly, simply accept the cost of a real login per test.",
      "order": 9
    }
  ],
  "advantages": [
    "7.4 Handling Authentication — A mature suite is not either/or."
  ],
  "limitations": [
    "7.4 Handling Authentication is this Part's slice only; later chapters go deeper rather than repeating this one."
  ],
  "exercises": [],
  "resourceLinks": [],
  "steps": [],
  "learn": []
} as ChapterRecord;
