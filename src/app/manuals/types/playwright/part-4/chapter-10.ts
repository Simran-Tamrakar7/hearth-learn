import type { ChapterRecord } from "../../../types";

/** 35. Debugging Tools */
export const chapter = {
  id: "pw-35-debug",
  title: "35. Debugging Tools",
  minutes: 50,
  level: "advanced",
  phase: "Part 4 · Advanced Techniques",
  partName: "Part 4 · Advanced Techniques",
  overviewText: "Browser launch options (slow_mo, devtools), locale/timezone/color_scheme emulation, page.clock for time control, console/pageerror listeners, DB-level assertions, BDD with pytest-bdd, OAuth popup handling, and choosing Inspector vs UI Mode vs Trace Viewer.",
  why: "CI failures with bare timeouts give zero context. Debugging tools surface DOM state, JS errors, and timing issues that assertions alone cannot explain.",
  when: "Use PWDEBUG=1 Inspector while authoring tests. Wire page.on('pageerror') as autouse fixture. Use page.clock for time-dependent features. Open traces after CI failures.",
  practical: { app: "HRMS — Flaky login in CI", scenario: "Spinner overlay blocks welcome text for 8 seconds — trace reveals it; fix waits for spinner hidden.", pass: "playwright show-trace ci-trace.zip shows overlay at failure step.", fail: "time.sleep(10) blind fix — passes sometimes, fails on slow CI." },
  tools: [],
  contentMarkdown: "## 35. Debugging Tools\n\nBrowser launch options control how the browser itself behaves during a test run, useful for debugging edge cases.\n```python\nbrowser = p.chromium.launch(\n    headless=False,\n    slow_mo=500,\n    devtools=True,\n    args=[\"--start-maximized\"]\n)\n```\n\n\nslow_mo (number, ms, optional) adds a delay after every Playwright action, making it easier to visually follow what a test is doing step by step. devtools=True (Chromium only) opens the browser's own DevTools panel automatically, letting you inspect console output, network requests, and DOM state manually alongside the automated actions. args (list of strings, optional) passes raw Chromium command-line flags for advanced cases.\nLocale, timezone, and color-scheme emulation help debug environment-specific UI issues.\n```python\ncontext = browser.new_context(\n    locale=\"ja-JP\",\n    timezone_id=\"Asia/Tokyo\",\n    color_scheme=\"dark\"\n)\n```\n\n\ncolor_scheme (string: \"light\", \"dark\", or \"no-preference\", optional) simulates the OS-level light/dark mode preference, useful for debugging dark-mode-specific rendering bugs without needing to actually change your own OS settings.\npage.clock lets you control time itself within a test, rather than waiting for real time to pass.\n```python\npage.clock.install(time=\"2026-01-01T00:00:00\")\npage.clock.fast_forward(\"01:00:00\")  # jump forward 1 hour\n```\n\n\npage.clock.install(time=...) freezes the browser's notion of the current time to a specific value. .fast_forward(duration) advances that frozen clock instantly, without actually waiting in real time — invaluable for testing time-dependent features (a session that expires after 30 minutes, a \"your leave request will auto-expire in 24 hours\" countdown) without a test genuinely needing to run for 30 real minutes.\nConsole and page-error listening surfaces JavaScript errors that would otherwise be invisible to a passing test.\n```python\npage.on(\"console\", lambda msg: print(f\"Console [{msg.type}]: {msg.text}\"))\npage.on(\"pageerror\", lambda exc: print(f\"Page error: {exc}\"))\n```\n\n\npage.on(\"console\", handler) fires for every console.log/console.warn/console.error call the page's own JavaScript makes. page.on(\"pageerror\", handler) fires when an uncaught JavaScript exception occurs on the page. A test can pass all its explicit assertions while the page is silently throwing JS errors in the background — wiring up these listeners (often as an autouse fixture, tying back to Chapter 21, that fails the test if any error-level console message or page error occurs) catches these silent failures that would otherwise go completely unnoticed.\nDB-level assertions verify the actual database state, not just what the UI displays.\n```python\ndef test_leave_request_persists_correctly(page, db_connection):\n    page.get_by_role(\"button\", name=\"Submit leave request\").click()\n    row = db_connection.execute(\"SELECT * FROM leave_requests WHERE employee_id = 42\").fetchone()\n    assert row[\"status\"] == \"pending\"\n```\n\n\nFor flows where correctness genuinely depends on what got persisted (not just what the UI shows immediately after an action), connecting directly to a test database and querying it after a UI action provides a stronger check than trusting the UI alone — the UI could theoretically show a success message even if the backend write silently failed. This is a deliberate exception to \"test through the UI,\" reserved for cases where UI-only verification wouldn't actually prove the thing you need to prove.\nBDD integration lets tests be written in Gherkin (Given/When/Then) syntax, readable by non-technical stakeholders.\nFeature: Leave request submission\n  Scenario: Employee submits a valid leave request\n    Given the employee is logged in\n    When they submit a leave request for 3 days\n    Then the request should appear as pending\n\nTools like pytest-bdd map these Gherkin steps to underlying Python functions containing the actual Playwright code, so business stakeholders can read/write scenario descriptions in plain language while engineers implement the corresponding automation underneath. This is particularly relevant to your QA background and documentation-focused role — BDD-style scenarios can double as both executable tests and structured requirements documentation, which ties directly into the requirements-traceability practice mentioned in Part 6.\nOAuth popup handling addresses a specific tricky case: third-party login flows that open in a genuinely separate popup window.\n```python\nwith page.context.expect_page() as popup_info:\n    page.get_by_role(\"button\", name=\"Sign in with Google\").click()\n```\n\n\npopup = popup_info.value\n```python\npopup.get_by_label(\"Email\").fill(\"test@example.com\")\npopup.get_by_role(\"button\", name=\"Next\").click()\n```\n\n\nThis combines the new-page-capture pattern from Chapter 17 with the reality that OAuth providers (Google, Microsoft, etc.) typically open their login form in a genuinely separate popup window rather than an iframe — you capture it the same way as any new tab, then interact with it as its own Page object. A practical note: automating a real third-party OAuth provider's login form directly is fragile (it can trigger bot-detection/CAPTCHA) and generally discouraged for routine test runs — most teams instead use a test-specific OAuth provider account, a mocked OAuth flow, or (preferably) the storage-state approach from Chapter 30 to bypass the login flow entirely after doing it once manually.\nPlaywright Inspector, UI Mode, and Trace Viewer are three different debugging tools for three different moments.\nPWDEBUG=1 pytest launches the Playwright Inspector — a live, step-through debugger for a currently-running test, letting you pause execution, step line by line, and interactively explore locators in real time. This is the right tool while you're actively writing or troubleshooting a specific test locally. UI Mode (pytest --ui in some configurations, more commonly a JS/TS-native feature — see Chapter 36) is an interactive test-runner interface for browsing, running, and watching your whole suite visually, with time-travel through a test's steps. Trace Viewer (Chapter 37) is fundamentally different from the other two because it's post-mortem — it doesn't require a live running test at all, instead replaying a recorded trace file after the fact, which is essential for a test that already failed in CI, where you have no live process to attach a debugger to. The practical rule: reach for the Inspector while actively authoring a test locally, UI Mode for browsing/running your suite interactively during development, and Trace Viewer for understanding what happened in a test run you weren't watching live (almost always a CI failure).",
  customSummary: "## 35. Debugging Tools\n\nLaunch options (slow_mo, devtools=True, args) aid manual debugging; color_scheme emulates dark/light mode.\npage.clock freezes/fast-forwards time — test time-dependent features (session expiry, countdowns) without waiting in real time.\npage.on(\"console\"/\"pageerror\") surfaces silent JS errors a passing test would otherwise miss.\nDB-level assertions verify actual persisted state, not just what the UI displays — use when UI-only checks aren't strong enough proof.\nBDD (pytest-bdd, Gherkin syntax) makes tests readable by non-technical stakeholders and can double as documentation.\nOAuth popups are captured like any new tab (expect_page()) — but prefer storage-state reuse over automating a real third-party login form routinely (fragile, bot-detection risk).\nThree tools, three moments: Playwright Inspector (PWDEBUG=1, live step-through while authoring), UI Mode (interactive browsing/running during development), Trace Viewer (post-mortem replay of a CI failure you weren't watching live).",
  exercises: [],
  resourceLinks: [],
  steps: [],
  learn: [],
} as ChapterRecord;
