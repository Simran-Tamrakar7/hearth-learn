import type { ChapterRecord } from "../../../types";

/** 27. API Testing with Playwright */
export const chapter = {
  id: "pw-27-api",
  title: "27. API Testing with Playwright",
  minutes: 50,
  level: "advanced",
  phase: "Part 4 · Advanced Techniques",
  partName: "Part 4 · Advanced Techniques",
  overviewText: "APIRequestContext (p.request.new_context) sends GET/POST/PUT/DELETE without a browser. The hybrid pattern seeds test data via API (fast) and uses the UI only for the feature under test. Use page.context.request when API calls must share the browser session's cookies.",
  why: "UI-based setup is slow and flake-prone. API calls take milliseconds and remove unrelated UI flows from tests that only care about one screen's behavior.",
  when: "Default to API for create/delete setup when endpoints exist. Write pure API tests for backend-only contracts. Use page.context.request for authenticated hybrid tests.",
  practical: { app: "HRMS — Leave request admin view", scenario: "POST a leave request via API, verify it appears in admin UI, DELETE via API teardown.", pass: "request_context.post('/leave-requests', data={...}) then page.goto detail URL — under 5 seconds.", fail: "Walk through 6-step UI submission for every test needing a leave record." },
  tools: [],
  contentMarkdown: "## 27. API Testing with Playwright\n\nAPIRequestContext sends raw HTTP requests without a browser.\n```python\nfrom playwright.sync_api import sync_playwright\n\nwith sync_playwright() as p:\n    request_context = p.request.new_context(base_url=\"https://api.example.com\")\n    response = request_context.get(\"/users/1\")\n    print(response.status)\n    print(response.json())\n```\n\n\np.request.new_context(base_url=...) creates a standalone context for making API calls, entirely independent of any browser page. base_url (string, optional) lets subsequent calls use relative paths. This gives you a real, dedicated API testing tool inside the same framework and language you already use for UI tests, rather than needing a separate tool like Postman or requests for backend checks.\nGET, POST, PUT, DELETE cover the full range of typical API testing needs.\n```python\nresponse = request_context.post(\"/users\", data={\"name\": \"Simran\", \"email\": \"s@example.com\"})\nassert response.status == 201\n\nresponse = request_context.put(f\"/users/{user_id}\", data={\"name\": \"Updated Name\"})\nresponse = request_context.delete(f\"/users/{user_id}\")\n```\n\n\ndata (dict, optional) is sent as the request body, typically JSON-encoded automatically. headers (dict, optional) lets you attach auth tokens or custom headers per call. response.status (integer) gives the HTTP status code, and response.json() parses a JSON response body directly into a Python dict/list — no manual parsing needed.\nAPI calls are a fast, reliable way to set up and verify test state — the \"hybrid pattern\" from Part 0.\n```python\ndef test_leave_request_appears_in_ui(page, request_context):\n    # Setup: seed data fast via API instead of clicking through the UI\n    response = request_context.post(\"/leave-requests\", data={\"employee_id\": 42, \"days\": 3})\n    leave_id = response.json()[\"id\"]\n\n    # Test: only the actual feature under test uses the slow UI layer\n    page.goto(f\"https://app.example.com/leave-requests/{leave_id}\")\n    expect(page.get_by_text(\"3 days\")).to_be_visible()\n```\n\n\nThis pattern is worth internalizing deeply: use API calls to create the preconditions a test needs (a user, an existing record, a particular account state), and reserve the UI layer for the actual behavior under test. This cuts test runtime significantly and removes an entire category of flakiness (an unrelated UI flow breaking and taking down tests that don't even care about that flow) — directly ties back to the Testing Pyramid guidance from Part 0's Chapter 5.\nCombining API setup with UI-context authentication.\nA subtlety worth flagging: an APIRequestContext created independently (as above) doesn't automatically share cookies/auth with a browser page — if your API calls need to be authenticated as the same user the UI test is using, you typically derive the API context from the page's own browser context (page.context.request) instead of creating a fresh standalone one, so both layers share the same session/cookies.",
  customSummary: "## 27. API Testing with Playwright\n\np.request.new_context(base_url=...) gives a standalone APIRequestContext for GET/POST/PUT/DELETE calls, no browser needed.\nresponse.status / response.json() read results directly.\nHybrid pattern: seed/verify state via API (fast), reserve UI for the actual feature under test — cuts runtime and removes unrelated flakiness.\nUse page.context.request (not a fresh context) when API calls need to share the UI test's own authenticated session.",
  exercises: [],
  resourceLinks: [],
  steps: [],
  learn: [],
} as ChapterRecord;
