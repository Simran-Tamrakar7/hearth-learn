import type { ChapterRecord } from "../../../types";

/** 33. Cross-browser & Cross-device Testing */
export const chapter = {
  id: "pw-33-cross-browser",
  title: "33. Cross-browser & Cross-device Testing",
  minutes: 40,
  level: "advanced",
  phase: "Part 4 · Advanced Techniques",
  partName: "Part 4 · Advanced Techniques",
  overviewText: "pytest --browser chromium|firefox|webkit runs the same suite on three engines. playwright.devices presets emulate mobile viewports, touch, and user-agent. geolocation and permissions options simulate GPS and camera without real hardware.",
  why: "Chrome-only testing ships WebKit and Firefox bugs to 30–40% of users. Mobile emulation catches responsive and touch issues desktop tests miss.",
  when: "Run smoke against all three browsers on every PR; full suite nightly. Use device presets for phone/tablet pages. Set permissions=['geolocation'] for location features.",
  practical: { app: "HRMS — Leave request form", scenario: "Date picker overflows in WebKit; submit button below fold on iPhone 13 emulation.", pass: "CI matrix runs chromium/firefox/webkit; iPhone 13 test confirms submit reachable.", fail: "Chromium-only — Safari layout and mobile usability bugs ship." },
  tools: [],
  contentMarkdown: "## 33. Cross-browser & Cross-device Testing\n\nRunning the same suite against multiple browser engines catches engine-specific bugs.\n```python\npytest --browser chromium\npytest --browser firefox\npytest --browser webkit\n```\n\n\nSince Chromium, Firefox, and WebKit each have their own rendering engine and JavaScript engine, subtle differences can exist — a CSS flexbox quirk that only shows in Safari/WebKit, or a JS API behaving slightly differently in Firefox. Running the identical test suite against all three, ideally as a standard part of CI, catches these before real users on those browsers do.\nplaywright.devices[] provides pre-configured device descriptors for realistic mobile emulation.\n```python\niphone = p.devices[\"iPhone 13\"]\ncontext = browser.new_context(**iphone)\npage = context.new_page()\npage.goto(\"https://m.example.com\")\n```\n\n\np.devices is a dictionary of named device profiles (e.g., \"iPhone 13\", \"Pixel 5\", \"iPad Pro 11\") each bundling a matching viewport size, user-agent string, device pixel ratio, and touch-event support into one object, which you spread (**iphone) directly into browser.new_context(). This gives realistic mobile-web emulation with zero manual configuration — you get the correct viewport and user-agent for that specific device automatically, rather than guessing values yourself.\nGeolocation and permission emulation test location- and permission-gated features without a real device.\n```python\ncontext = browser.new_context(\n    geolocation={\"latitude\": 27.7172, \"longitude\": 85.3240},  # Kathmandu\n    permissions=[\"geolocation\"]\n)\npage = context.new_page()\npage.goto(\"https://app.example.com/nearby-offices\")\n```\n\n\ngeolocation (dict with latitude/longitude, optional) sets a fake GPS position the page's navigator.geolocation API will report. permissions (list of strings, optional) grants specific browser permissions upfront — \"geolocation\", \"camera\", \"microphone\", \"notifications\" — without the user needing to click through a real permission prompt (which Playwright can't interact with directly, since it's a native OS-level UI element, not part of the page's DOM). This lets you test permission-dependent features (a \"find nearby office\" feature, a notification opt-in flow) deterministically, simulating both the granted and denied states on demand rather than depending on manual browser configuration.\nCross-device testing (mobile web emulation) is distinct from cross-browser testing (engine coverage) — both matter for different reasons.\nCross-browser testing catches engine-specific rendering/JS bugs; cross-device testing catches responsive-design and touch-interaction bugs (a button too small to tap comfortably, a layout that breaks at a narrow viewport, hover-dependent UI that doesn't work on a touchscreen). A thorough suite typically covers both dimensions somewhat independently — running critical flows across the three engines, and separately running key mobile-specific flows against one or two representative device profiles, rather than needing the full cross-product of every browser × every device for every single test.",
  customSummary: "## 33. Cross-browser & Cross-device Testing\n\nRun the identical suite against Chromium/Firefox/WebKit via --browser to catch engine-specific bugs.\nplaywright.devices[\"iPhone 13\"] etc. bundle viewport/user-agent/pixel-ratio/touch support — spread into new_context() for realistic mobile-web emulation.\ngeolocation/permissions context options simulate GPS position and grant camera/notification/etc. access without a real device or manual permission prompts.\nCross-browser (engine bugs) and cross-device (responsive/touch bugs) are distinct concerns — cover both, but not necessarily as a full cross-product for every test.",
  exercises: [],
  resourceLinks: [],
  steps: [],
  learn: [],
} as ChapterRecord;
