import type { ChapterRecord } from "../../../types";

/** 44. Managing Test Suites at Scale */
export const chapter = {
  id: "pw-44-scale",
  title: "44. Managing Test Suites at Scale",
  minutes: 35,
  level: "advanced",
  phase: "Part 6 · Pro-Level Practices",
  partName: "Part 6 · Pro-Level Practices",
  overviewText: "Marker discipline, flake diagnosis workflow, quarantine strategies, TestRail integration, ownership models, and selective execution at hundreds of tests.",
  why: "A 45-minute suite blocking every PR is a process failure. Scale management is about signal-per-minute, not raw test count.",
  when: "Read when CI runtime exceeds team patience or flaky tests erode trust in the suite.",
  practical: { app: "400-test regression suite", scenario: "Suite takes 45 minutes; developers skip CI and merge without waiting.", pass: "Smoke on PR (3 min), sharded regression nightly, quarantine @flaky tests with ticket.", fail: "Run everything always; disable failing tests without tracking tickets." },
  advantages: ["Marker vocabulary (smoke/regression/critical) enables reliable CI slicing","pytest-xdist -n auto parallelizes across CPU cores","Quarantine pattern documents known flakes without deleting coverage","TestRail sync links automated results to manual test cases","Ownership per module folder clarifies who fixes broken tests","Historical flake tracking identifies tests needing rewrite"],
  limitations: ["Parallel workers need isolated test data — shared DB causes collisions","Quarantine without expiry dates accumulates permanent skipped tests","TestRail integration adds process overhead small teams skip","Sharding requires CI platform matrix or custom orchestration","Marker sprawl without convention makes -m filtering unreliable","Over-parallelizing hits staging environment rate limits"],
  tools: [],
  contentMarkdown: "## Managing Test Suites at Scale\n\nThe core problem at scale isn't writing more tests — it's keeping an already-large suite trustworthy and fast. Once a suite passes a few hundred tests, new problems appear that don't exist at a small scale: runtime becomes a real bottleneck in the CI feedback loop, ownership becomes unclear (\"whose test is this, and who fixes it when it breaks?\"), and duplication creeps in as different contributors unknowingly write near-identical coverage for the same flow.\n\nTest ownership and tagging by module keeps a large suite navigable.\n\n```python\n@pytest.mark.owner(\"payroll-team\")\n@pytest.mark.module(\"payroll\")\ndef test_payroll_calculates_overtime_correctly():\n```\n\n...\n\nCustom markers (Chapter 22) aren't limited to smoke/regression — tagging tests by owning team or product module makes it possible to answer \"who do I ask about this failing test\" and \"run only the Payroll module's tests\" without reading file paths. This becomes genuinely necessary once a suite spans multiple teams, each responsible for a different slice of the application (mirroring how Bizlevate's own modules — Employee Management, Attendance, Leave, Payroll, Onboarding/Offboarding, Appraisal — might eventually be owned by different feature teams).\n\nTest impact analysis avoids running the entire suite for every small change. A large, mature suite increasingly can't afford to run everything on every commit — test impact analysis (conceptually: mapping which tests actually exercise which parts of the codebase, often via code-coverage data) lets CI run only the subset of tests plausibly affected by a given change, with the full suite still running on a schedule or before release as a safety net. This is more of an engineering practice than a single Playwright feature — it typically involves coverage tooling and CI configuration working together — but it's worth knowing the concept exists as a suite scales, since it directly addresses the \"CI takes 40 minutes and nobody wants to wait for it\" problem. It resurfaces again in Chapter 50 as a named skill worth having for senior QA automation roles.\n\nDeduplication and coverage mapping prevent a suite from silently testing the same thing five different ways. As a suite grows across contributors, it's common for three different people to each write a slightly different \"can a user log in\" test without realizing the others exist. A periodic audit — grouping tests by the actual feature/flow they exercise (not just by file name) — surfaces this kind of redundancy. Requirements traceability (covered in more depth in Chapter 46's addendum) is the more formal version of this same idea: mapping each test back to a specific requirement or user story, which makes both duplication and gaps visible at the same time.\n\nSuite health metrics turn \"the suite feels slow/flaky\" into something measurable and trackable over time. Worth tracking deliberately, usually via CI/reporting tooling (Chapter 40): total runtime trend (is the suite getting slower release over release?), flaky-test rate (tying back to Chapter 38's detection discipline), and pass-rate trend on the full suite versus just smoke tests. Treating these as metrics with an owner — not just a vague feeling that \"tests take a while now\" — is what actually keeps a large suite healthy over years rather than degrading quietly until someone declares test-automation bankruptcy and rewrites it from scratch.\n\nSplitting suites by execution frequency is the practical response to growing runtime. This ties directly back to the smoke/regression split from Chapter 22 and the CI patterns from Chapter 39, but at scale it often grows a third tier: a very small \"critical path\" tier (a handful of tests, run on every single commit, minutes), a broader smoke tier (run on every PR merge, tens of minutes), and a full regression tier (nightly or pre-release, potentially hours) — each tier answering a different question at a different cost, rather than one undifferentiated \"run everything\" button.",
  customSummary: "## Managing Test Suites at Scale\n\nAt scale, the challenge shifts from writing tests to keeping a large suite fast, owned, and trustworthy.\nTag tests by owning team/module (custom markers) so failures have a clear owner and subsets can run independently.\nTest impact analysis runs only tests plausibly affected by a change (via coverage-mapping tooling + CI config), with full suite runs reserved for schedule/release gates.\nPeriodic audits catch duplicated coverage across contributors; requirements traceability (Ch. 46) is the more formal version of the same idea.\nTrack suite health as real metrics — runtime trend, flaky-test rate, pass-rate trend — not just a vague feeling that \"tests are slow now.\"\nSplit execution by frequency into tiers: critical-path (every commit), smoke (every PR), full regression (nightly/pre-release) — each answering a different question at a different cost.",
  exercises: [],
  resourceLinks: [],
  steps: [],
  learn: [],
} as ChapterRecord;
