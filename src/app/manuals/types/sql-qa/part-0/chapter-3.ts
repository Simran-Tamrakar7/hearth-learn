import type { ChapterRecord } from "../../../types";

/** 0.3 What Testers Use SQL For */
export const chapter = {
  "id": "sql-0-3-what-testers-use-sql-for",
  "title": "0.3 What Testers Use SQL For",
  "minutes": 22,
  "level": "beginner",
  "phase": "Part 0 · Orientation",
  "partName": "Part 0 · Orientation",
  "overviewText": "SQL isn't a \"nice to have\" skill for QA. It's often the difference between a tester who can only validate what's visible on the UI and one who can independently confirm whether the actual data is correct. Most bugs that matter (wrong totals, missing records, orphaned data, incorrect status transitions) don't show up cleanly in the UI. They show up in the database, and SQL is how you get there. Here are the core categories where testers rely on it. The most common use: do something in the UI or API, then check the database. After creating an employee through the onboarding form, does exactly one row appear in employees, with the right department, a status of active, and a non-null created_at?",
  "why": "0.3 What Testers Use SQL For is how a tester proves stored state, not just the screen. SQL isn't a \"nice to have\" skill for QA.",
  "when": "Open this chapter when you are on the Orientation path and need What Testers Use SQL For against the hrms_practice schema, a shared QA database, or a Bizlevate HRMS screen.",
  "practical": {
    "app": "Bizlevate HRMS / hrms_practice",
    "scenario": "A leave, payroll, attendance, or appointment check depends on What Testers Use SQL For.",
    "pass": "You apply What Testers Use SQL For on the sample database (or a tagged QA row) and can say what empty vs non-empty results mean.",
    "fail": "You copy a blog query, skip What Testers Use SQL For, or treat a UI success toast as proof the row is correct."
  },
  "tools": [],
  "customSummary": "- Confirm UI/API actions actually persisted.\n- Validate calculations (payroll, leave balance, attendance hours).\n- Seed and clean test data quickly.\n- Find duplicates, orphans, nulls, impossible values.\n- Pinpoint whether a bug is in the UI, API, or data.\n- Verify migrations and add DB assertions inside Playwright/Selenium/pytest (use parameterized queries).",
  "contentMarkdown": "SQL isn't a \"nice to have\" skill for QA. It's often the difference between a tester who can only validate what's visible on the UI and one who can independently confirm whether the actual data is correct. Most bugs that matter (wrong totals, missing records, orphaned data, incorrect status transitions) don't show up cleanly in the UI. They show up in the database, and SQL is how you get there.\nHere are the core categories where testers rely on it.\n## Confirming that an action really persisted\nThe most common use: do something in the UI or API, then check the database. After creating an employee through the onboarding form, does exactly one row appear in employees, with the right department, a status of active, and a non-null created_at? UI success messages can lie; rows can't.\n```sql\nSELECT employee_id, first_name, email, department_id, status, created_at\nFROM   employees\nWHERE  email = 'asha.qa001@example.com';\n```\n\n## Validating what the UI displays, and the calculations behind it\nCross-checking what the application displays against what is actually stored is the single most common QA use case. When a screen shows \"Annual leave balance: 12 days,\" how do you know that's correct? You could recalculate by hand, or you could ask the database. In the sample schema, approved days live in leave_requests and the yearly entitlement lives in leave_types (18 days for Annual):\n```sql\nSELECT employee_id, SUM(days) AS days_taken\n| FROM   leave_requests | WHERE  status = 'approved' | AND  leave_type_id = 1                                   -- Annual | AND  start_date >= '2026-01-01' AND start_date < '2027-01-01' | GROUP BY employee_id; |\n|---|---|---|---|---|\n| If this returns 8 days taken for an employee, the balance should be 18 − 8 = 10. If the UI shows 12, it is displaying a balance that only makes sense if 6 days were taken. You've just caught a data-integrity bug that no amount of UI clicking would have revealed. | Payroll, leave accrual, attendance hours, appraisal scoring and appointment totals are all computed values, and much of that logic happens entirely in the backend. The UI just displays the result. To test the logic and not just the display, compute the expected value independently in SQL, query the intermediate tables, and compare. | sql | -- Expected net pay according to raw components | SELECT employee_id, |\n| basic_salary + allowances - deductions AS expected_net, | net_pay                                AS stored_net | FROM   payroll | WHERE  pay_month = '2026-08-01' | AND  basic_salary + allowances - deductions <> net_pay;   -- any row here is a bug |\n```\n\n## Setting up and cleaning up test data\nAutomated tests (Playwright, Cypress, Selenium) need known starting states. If a test needs \"an employee with exactly 3 pending leave requests,\" it's far more reliable to insert that data directly than to click through the UI three times. UI-based setup is slow, brittle, and itself subject to bugs. Many test frameworks use SQL scripts or ORM-based seeders for exactly this reason. A few statements can also create scenarios that are painful in the UI: a payroll run with a specific mix of statuses, an appointment slot in the past.\nsql\n-- Setup: 3 pending requests for employee 3 (treated as a test-owned employee here)\n```sql\nINSERT INTO leave_requests (employee_id, leave_type_id, start_date, end_date, days, status) VALUES\n(3, 1, '2026-10-05', '2026-10-05', 1, 'pending'),\n(3, 1, '2026-10-12', '2026-10-13', 2, 'pending'),\n(3, 1, '2026-10-19', '2026-10-21', 3, 'pending');\n```\nNote that leave_type_id and days are NOT NULL in the sample schema, so the insert must supply them. Teardown works the same way. After the test run, you remove what you inserted so the next run starts fresh:\n```sql\nDELETE FROM leave_requests\nWHERE  employee_id = 3\n  AND  status = 'pending';\n```\nThis cleanup is only safe because employee 3 is owned by the test. On a shared database, scope your cleanup to rows you created (tagged IDs, e-mail domains), as described in 0.4. Part 7 covers this in depth.\n## Debugging failed tests and investigating bugs\nWhen an automated test fails, the first question is always \"is this a real bug, or a data/environment problem?\" SQL lets you answer that in seconds. Suppose a Cypress test expects to pick \"Sita Rai\" from an employee dropdown and fails:\n```sql\nSELECT employee_id, first_name, last_name, status, is_deleted\nFROM   employees\nWHERE  first_name = 'Sita' AND last_name = 'Rai';\n```\nNo rows (or is_deleted = 1, or a status of resigned) means the environment simply doesn't have that seed data, or the record is filtered out by design. You've saved yourself from chasing a phantom UI bug.\nThe same idea applies to bugs a user reports. When a screen looks wrong (\"why does this employee show 0 leave balance?\"), SQL tells you whether the fault is in the UI, the API, or the data. If the database has the right value, the fault is upstream. If the database has the wrong value, you've narrowed it to backend logic or a bad data load. That single distinction saves hours of back-and-forth with developers.\n## Finding hidden data problems\nDuplicates, orphaned rows, impossible dates, nulls where there should be values, records stuck in a status. Tests that click through screens rarely find these. Queries do. Part 6 and Part 10 are built around this.\n## Verifying migrations, releases and environment differences\nBefore/after row counts, checksum comparisons and schema comparisons across environments (Part 6). When a bug appears in only one environment, SQL lets you compare row counts, specific records or schema differences between, say, staging and production directly, rather than guessing. Do any comparison involving production through a read-only account or a read replica (11.2).\n## Adding DB assertions inside automation\nHere is how it looks side by side. A Playwright test with a database check at the end:\n```ts\nimport { test, expect } from '@playwright/test';\nimport { Client } from 'pg';\n```\n\n| test('creating an employee stores a correct row', async ({ page }) => { | await page.goto('/employees/new'); | await page.getByLabel('First name').fill('Asha'); |\n|---|---|---|\n| await page.getByLabel('Email').fill('asha.qa001@example.com'); | await page.getByRole('button', { name: 'Save' }).click(); | await expect(page.getByText('Employee created')).toBeVisible(); |\n\n| // --- DB assertion: the UI said \"created\", now prove it --- | const db = new Client({ connectionString: process.env.QA_DB_URL }); | await db.connect(); | const { rows } = await db.query( |\n|---|---|---|---|\n| 'SELECT first_name, status FROM employees WHERE email = $1', | ['asha.qa001@example.com'] | ); | await db.end(); |\n\n  expect(rows).toHaveLength(1);          // exactly one, no duplicates\n| expect(rows[0].first_name).toBe('Asha'); | expect(rows[0].status).toBe('active'); | }); |\n|---|---|---|\n| The same idea in Selenium with Python: | python | import pymysql |\n\n```py\ndef test_create_employee(driver):\n    # ... UI steps with driver.find_element(...).click() ...\n```\n\n    conn = pymysql.connect(host=\"qa-db\", user=\"qa_readonly\", password=\"...\", database=\"hrms_qa\")\n```sql\n    with conn.cursor() as cur:\n        cur.execute(\"SELECT status FROM employees WHERE email = %s\", (\"asha.qa001@example.com\",))\n        rows = cur.fetchall()\n    conn.close()\n```\n\n    assert len(rows) == 1\n## assert rows[0][0] == \"active\"\nNotice the $1 and %s placeholders instead of pasting the email into the string. That is parameterized querying, which avoids quoting bugs and SQL injection. Two practical notes: the Playwright example uses the PostgreSQL client (pg); with MySQL you would use a client such as mysql2, whose placeholder is ?. And because email is unique in the sample schema, a real test should generate a fresh address per run (for example, with a timestamp) or clean up afterwards, otherwise the second run fails for the wrong reason. Part 9 goes deeper on wiring SQL into Playwright, Cypress, pytest and CI.\n",
  "blocks": [
    {
      "id": "sql-0-3-md-0",
      "type": "overview",
      "content": "SQL isn't a \"nice to have\" skill for QA. It's often the difference between a tester who can only validate what's visible on the UI and one who can independently confirm whether the actual data is correct. Most bugs that matter (wrong totals, missing records, orphaned data, incorrect status transitions) don't show up cleanly in the UI. They show up in the database, and SQL is how you get there.\nHere are the core categories where testers rely on it.",
      "order": 0
    },
    {
      "id": "sql-0-3-md-1",
      "type": "overview",
      "heading": "Confirming that an action really persisted",
      "content": "The most common use: do something in the UI or API, then check the database. After creating an employee through the onboarding form, does exactly one row appear in employees, with the right department, a status of active, and a non-null created_at? UI success messages can lie; rows can't.\n```sql\nSELECT employee_id, first_name, email, department_id, status, created_at\nFROM   employees\nWHERE  email = 'asha.qa001@example.com';\n```",
      "order": 1
    },
    {
      "id": "sql-0-3-md-2",
      "type": "overview",
      "heading": "Validating what the UI displays, and the calculations behind it",
      "content": "Cross-checking what the application displays against what is actually stored is the single most common QA use case. When a screen shows \"Annual leave balance: 12 days,\" how do you know that's correct? You could recalculate by hand, or you could ask the database. In the sample schema, approved days live in leave_requests and the yearly entitlement lives in leave_types (18 days for Annual):\n```sql\nSELECT employee_id, SUM(days) AS days_taken\n| FROM   leave_requests | WHERE  status = 'approved' | AND  leave_type_id = 1                                   -- Annual | AND  start_date >= '2026-01-01' AND start_date < '2027-01-01' | GROUP BY employee_id; |\n|---|---|---|---|---|\n| If this returns 8 days taken for an employee, the balance should be 18 − 8 = 10. If the UI shows 12, it is displaying a balance that only makes sense if 6 days were taken. You've just caught a data-integrity bug that no amount of UI clicking would have revealed. | Payroll, leave accrual, attendance hours, appraisal scoring and appointment totals are all computed values, and much of that logic happens entirely in the backend. The UI just displays the result. To test the logic and not just the display, compute the expected value independently in SQL, query the intermediate tables, and compare. | sql | -- Expected net pay according to raw components | SELECT employee_id, |\n| basic_salary + allowances - deductions AS expected_net, | net_pay                                AS stored_net | FROM   payroll | WHERE  pay_month = '2026-08-01' | AND  basic_salary + allowances - deductions <> net_pay;   -- any row here is a bug |\n```",
      "order": 2
    },
    {
      "id": "sql-0-3-md-3",
      "type": "overview",
      "heading": "Setting up and cleaning up test data",
      "content": "Automated tests (Playwright, Cypress, Selenium) need known starting states. If a test needs \"an employee with exactly 3 pending leave requests,\" it's far more reliable to insert that data directly than to click through the UI three times. UI-based setup is slow, brittle, and itself subject to bugs. Many test frameworks use SQL scripts or ORM-based seeders for exactly this reason. A few statements can also create scenarios that are painful in the UI: a payroll run with a specific mix of statuses, an appointment slot in the past.\nsql\n-- Setup: 3 pending requests for employee 3 (treated as a test-owned employee here)\n```sql\nINSERT INTO leave_requests (employee_id, leave_type_id, start_date, end_date, days, status) VALUES\n(3, 1, '2026-10-05', '2026-10-05', 1, 'pending'),\n(3, 1, '2026-10-12', '2026-10-13', 2, 'pending'),\n(3, 1, '2026-10-19', '2026-10-21', 3, 'pending');\n```\nNote that leave_type_id and days are NOT NULL in the sample schema, so the insert must supply them. Teardown works the same way. After the test run, you remove what you inserted so the next run starts fresh:\n```sql\nDELETE FROM leave_requests\nWHERE  employee_id = 3\n  AND  status = 'pending';\n```\nThis cleanup is only safe because employee 3 is owned by the test. On a shared database, scope your cleanup to rows you created (tagged IDs, e-mail domains), as described in 0.4. Part 7 covers this in depth.",
      "order": 3
    },
    {
      "id": "sql-0-3-md-4",
      "type": "overview",
      "heading": "Debugging failed tests and investigating bugs",
      "content": "When an automated test fails, the first question is always \"is this a real bug, or a data/environment problem?\" SQL lets you answer that in seconds. Suppose a Cypress test expects to pick \"Sita Rai\" from an employee dropdown and fails:\n```sql\nSELECT employee_id, first_name, last_name, status, is_deleted\nFROM   employees\nWHERE  first_name = 'Sita' AND last_name = 'Rai';\n```\nNo rows (or is_deleted = 1, or a status of resigned) means the environment simply doesn't have that seed data, or the record is filtered out by design. You've saved yourself from chasing a phantom UI bug.\nThe same idea applies to bugs a user reports. When a screen looks wrong (\"why does this employee show 0 leave balance?\"), SQL tells you whether the fault is in the UI, the API, or the data. If the database has the right value, the fault is upstream. If the database has the wrong value, you've narrowed it to backend logic or a bad data load. That single distinction saves hours of back-and-forth with developers.",
      "order": 4
    },
    {
      "id": "sql-0-3-md-5",
      "type": "overview",
      "heading": "Finding hidden data problems",
      "content": "Duplicates, orphaned rows, impossible dates, nulls where there should be values, records stuck in a status. Tests that click through screens rarely find these. Queries do. Part 6 and Part 10 are built around this.",
      "order": 5
    },
    {
      "id": "sql-0-3-md-6",
      "type": "overview",
      "heading": "Verifying migrations, releases and environment differences",
      "content": "Before/after row counts, checksum comparisons and schema comparisons across environments (Part 6). When a bug appears in only one environment, SQL lets you compare row counts, specific records or schema differences between, say, staging and production directly, rather than guessing. Do any comparison involving production through a read-only account or a read replica (11.2).",
      "order": 6
    },
    {
      "id": "sql-0-3-md-7",
      "type": "overview",
      "heading": "Adding DB assertions inside automation",
      "content": "Here is how it looks side by side. A Playwright test with a database check at the end:\n```ts\nimport { test, expect } from '@playwright/test';\nimport { Client } from 'pg';\n```\n\n| test('creating an employee stores a correct row', async ({ page }) => { | await page.goto('/employees/new'); | await page.getByLabel('First name').fill('Asha'); |\n|---|---|---|\n| await page.getByLabel('Email').fill('asha.qa001@example.com'); | await page.getByRole('button', { name: 'Save' }).click(); | await expect(page.getByText('Employee created')).toBeVisible(); |\n\n| // --- DB assertion: the UI said \"created\", now prove it --- | const db = new Client({ connectionString: process.env.QA_DB_URL }); | await db.connect(); | const { rows } = await db.query( |\n|---|---|---|---|\n| 'SELECT first_name, status FROM employees WHERE email = $1', | ['asha.qa001@example.com'] | ); | await db.end(); |\n\n  expect(rows).toHaveLength(1);          // exactly one, no duplicates\n| expect(rows[0].first_name).toBe('Asha'); | expect(rows[0].status).toBe('active'); | }); |\n|---|---|---|\n| The same idea in Selenium with Python: | python | import pymysql |\n\n```py\ndef test_create_employee(driver):\n    # ... UI steps with driver.find_element(...).click() ...\n```\n\n    conn = pymysql.connect(host=\"qa-db\", user=\"qa_readonly\", password=\"...\", database=\"hrms_qa\")\n```sql\n    with conn.cursor() as cur:\n        cur.execute(\"SELECT status FROM employees WHERE email = %s\", (\"asha.qa001@example.com\",))\n        rows = cur.fetchall()\n    conn.close()\n```\n\n    assert len(rows) == 1",
      "order": 7
    },
    {
      "id": "sql-0-3-md-8",
      "type": "overview",
      "heading": "assert rows[0][0] == \"active\"",
      "content": "Notice the $1 and %s placeholders instead of pasting the email into the string. That is parameterized querying, which avoids quoting bugs and SQL injection. Two practical notes: the Playwright example uses the PostgreSQL client (pg); with MySQL you would use a client such as mysql2, whose placeholder is ?. And because email is unique in the sample schema, a real test should generate a fresh address per run (for example, with a timestamp) or clean up afterwards, otherwise the second run fails for the wrong reason. Part 9 goes deeper on wiring SQL into Playwright, Cypress, pytest and CI.",
      "order": 8
    }
  ],
  "advantages": [
    "0.3 What Testers Use SQL For — 0."
  ],
  "limitations": [
    "0.3 What Testers Use SQL For is this Part's slice only; later chapters go deeper rather than repeating this one."
  ],
  "exercises": [],
  "resourceLinks": [],
  "steps": [],
  "learn": []
} as ChapterRecord;
