import type { ChapterRecord } from "../../../types";

/** 11.1 Running Queries via DB Clients */
export const chapter = {
  "id": "sql-11-1-running-queries-via-db-clients",
  "title": "11.1 Running Queries via DB Clients",
  "minutes": 23,
  "level": "advanced",
  "phase": "Part 11 · Tools & Workflow",
  "partName": "Part 11 · Tools & Workflow",
  "overviewText": "Everything in this manual has been the SQL itself. This chapter is about the program you type it into: a GUI tool (DBeaver, MySQL Workbench, pgAdmin, Azure Data Studio, TablePlus), a command-line shell (mysql, psql, sqlcmd), or an IDE plugin (JetBrains' database tool, VS Code extensions). The client is a lens on the database, and it has its own settings, quirks and failure modes that are easy to mistake for database behaviour. A tester who understands the client avoids a whole category of false alarms. Every connection profile has a host, port, database/schema, user and (often) a default character set and time zone. Before running anything on an unfamiliar or shared connection, confirm where",
  "why": "11.1 Running Queries via DB Clients is how a tester proves stored state, not just the screen. Everything in this manual has been the SQL itself.",
  "when": "Open this chapter when you are on the Tools & Workflow path and need Running Queries via DB Clients against the hrms_practice schema, a shared QA database, or a Bizlevate HRMS screen.",
  "practical": {
    "app": "Bizlevate HRMS / hrms_practice",
    "scenario": "A leave, payroll, attendance, or appointment check depends on Running Queries via DB Clients.",
    "pass": "You apply Running Queries via DB Clients on the sample database (or a tagged QA row) and can say what empty vs non-empty results mean.",
    "fail": "You copy a blog query, skip Running Queries via DB Clients, or treat a UI success toast as proof the row is correct."
  },
  "tools": [],
  "customSummary": "- The client (GUI or CLI) is a lens on the database with its own settings and quirks, easily mistaken for database behavior.\n- Confirm connection identity (DATABASE(), host, user, version) before running anything unfamiliar; use unmistakable, color-coded connection names, especially to mark production.\n- Check and control autocommit/transaction mode in the client's UI (DBeaver/Workbench toggle, psql/mysql CLI defaults) before writes on shared databases.\n- Distinguish \"run current statement\" from \"run whole script\" shortcuts; run one statement at a time when experimenting on shared data.\n- Result grids apply their own formatting: row-count caps with silent pagination, text truncation, inconsistent NULL-vs-empty rendering, date/numeric display rounding — verify anything suspicious with an explicit query (HEX, LENGTH, CAST), never trust the rendered cell.\n- Inline grid editing generates UPDATEs that may key off all visible columns, not just the PK, risking unintended matches; prefer explicit UPDATE ... WHERE statements for anything that matters.\n- Autocomplete/schema caches go stale after migrations; refresh before trusting what autocomplete implies, and verify structure with information_schema/SHOW CREATE TABLE instead.\n- Multi-statement runs can produce several result tabs; check every tab, not just the first, for silent errors or unexpected rows.\n- Learn format/beautify, comment-toggle, and explain-plan shortcuts.\n- CLI clients (mysql, psql, sqlcmd) have no safety net by default; use --safe-updates/sql_safe_updates, \\set ON_ERROR_STOP on, and read scripts fully before piping them in.\n- Match client to task: GUI for exploration/diagrams, CLI for scripted/CI work, a cross-dialect tool when working across databases.",
  "contentMarkdown": "## The client is not the database\nEverything in this manual has been the SQL itself. This chapter is about the program you type it into: a GUI tool (DBeaver, MySQL Workbench, pgAdmin, Azure Data Studio, TablePlus), a command-line shell (mysql, psql, sqlcmd), or an IDE plugin (JetBrains' database tool, VS Code extensions). The client is a lens on the database, and it has its own settings, quirks and failure modes that are easy to mistake for database behaviour. A tester who understands the client avoids a whole category of false alarms.\n## Connecting: know exactly what you're pointed at\nEvery connection profile has a host, port, database/schema, user and (often) a default character set and time zone. Before running anything on an unfamiliar or shared connection, confirm where you actually are:\n```sql\nSELECT DATABASE() AS db, @@hostname AS host, @@port AS port, CURRENT_USER() AS you, @@version AS version;\n-- PostgreSQL: SELECT current_database(), inet_server_addr(), inet_server_port(), current_user, version();\n-- SQL Server: SELECT DB_NAME(), @@SERVERNAME, SYSTEM_USER, @@VERSION;\n```\n\nSave connection profiles with unmistakable names and colours (\"QA — safe\", \"STAGING — careful\", \"PROD — read-only, ask first\") rather than generic labels like \"MySQL 1\" and \"MySQL 2.\" Most GUI clients let you colour-code a connection's tab or toolbar; use production red and never let it look like anything else. The single most common way testers cause real damage is running a script against the wrong tab because two connections looked identical.\n## Autocommit and transaction mode, in the client's own UI\n0.4 and 7.3 covered the concept; here's where to check and change it in practice. DBeaver shows an \"Auto-commit\" toggle in the SQL editor toolbar (a green checkmark icon) and lets you switch to manual commit per connection. MySQL Workbench has the same toggle in the results grid toolbar. psql defaults to autocommit-per-statement unless you type BEGIN; mysql CLI is autocommit by default (\\c doesn't change it — use SET autocommit=0 or START TRANSACTION). Before any UPDATE/DELETE on a shared database, glance at that toggle. If manual-commit mode is on and you forget to commit or roll back, you can leave a transaction open and block other sessions for the rest of your coffee break (7.3's \"hung QA environment\" story).\n## Query execution: run one statement, not the whole script\nGUI editors typically bind two different shortcuts: \"execute the statement under the cursor\" and \"execute the whole script/file.\" It's easy to hit the wrong one and run every DELETE in a scratch file at once. Get comfortable with your client's statement-boundary shortcut (often Ctrl+Enter / Cmd+Enter for \"current statement\" versus Ctrl+Shift+Enter / a toolbar \"execute script\" button for \"everything\"), and prefer running one statement at a time when experimenting on a shared database, even inside a transaction.\n## Result grids: what you see is not always what's stored\nClient grids apply their own formatting and truncation, and treating the grid's display as ground truth causes false bug reports:\nRow limits. Many GUI tools cap the visible result set (DBeaver defaults to a \"fetch size\" and a max rows setting, often 200), silently paginating with a \"Fetch more\" link at the bottom. A COUNT(*) you never ran can look like \"only 200 rows exist\" if you just scroll the grid and stop.\nColumn truncation. Long text and TEXT/BLOB columns are often shown abbreviated with an ellipsis, or as (BLOB)/<binary> placeholders. A trailing space or an invisible character (2.5) is invisible in a truncated cell.\nNULL vs empty string rendering. Some clients show both as a blank cell, some show NULL in italics or grey, some show <null> literally. Confirm your client's convention before concluding a column \"has no value\" — DBeaver, for instance, is configurable on this point, and the default can differ between its SQL editor grid and its data editor grid.\nDate/time display and rounding. A client may apply its own locale formatting or truncate seconds/microseconds on screen while the stored value is more precise; don't diagnose a \"the time is wrong\" bug from the grid alone — re-query with explicit formatting (DATE_FORMAT, TO_CHAR) to see the raw stored value.\nNumeric display. A grid may show 57500 for a DECIMAL(10,2) value of 57500.00, hiding trailing zero precision that might matter for a rounding investigation (2.4).\nWhen something in a grid looks suspicious, the fix is always the same: query for it explicitly (SELECT HEX(col), SELECT LENGTH(col), SELECT CAST(col AS CHAR)) rather than trusting the rendered cell, exactly the technique 2.5 used to unmask invisible whitespace.\n## Editing data directly in a grid\nMost GUI clients let you double-click a cell and edit it, generating an UPDATE behind the scenes when you commit the change. This is convenient and dangerous in equal measure: it's easy to fat-finger an edit into the wrong row (especially in an unsorted or unfiltered grid where row order isn't guaranteed, 2.2), and some tools generate the UPDATE's WHERE clause from all visible columns rather than the primary key, which can silently match and update more rows than you intended if the \"identifying\" columns aren't actually unique. Prefer writing an explicit UPDATE ... WHERE id = ... statement over inline grid editing whenever the change matters, precisely so you control the WHERE clause yourself (0.4's safe-workflow habit).\n## Autocomplete and object caches\nGUI clients cache the list of tables/columns for autocomplete, and that cache can go stale after a schema migration (6.4) — autocomplete still offering a dropped column, or not yet offering a newly added one, is a client artifact, not evidence the migration failed. Refresh the schema cache (usually F5 or a right-click \"Refresh\") before trusting what autocomplete implies about current structure, and always verify structural questions with an actual information_schema query or SHOW CREATE TABLE, not the autocomplete list.\n## Multiple result sets and multi-statement scripts\nRunning several SELECTs (or a stored procedure that returns more than one result set, 8.3) in one execution produces several grids or tabs. It's easy to look at only the first and miss that a later statement returned unexpected rows, or errored silently while the client moved on. Check whether your client shows a per-statement status/error indicator, and get in the habit of scrolling through every result tab, not just the first, after a multi-statement run.\n## Keyboard shortcuts worth learning\nBeyond \"execute statement\" vs \"execute script,\" the shortcuts that save the most time are: format/beautify SQL (turns a one-line paste into readable indentation, useful when a developer pastes a query in Slack), comment/uncomment a line or block (for toggling a WHERE clause on and off while debugging a query, as 2.1 recommended), and \"explain plan\" (often bound directly, skipping the need to type EXPLAIN by hand, 8.5). Different clients bind these differently; five minutes with your specific tool's shortcut cheat sheet pays for itself within the first debugging session.\n## Command-line clients: no safety net by default\nThe mysql, psql and sqlcmd CLIs have no grid, no autocomplete cache, and (by default) no confirmation prompts — they execute exactly what you type, and pressing Enter after a DELETE FROM employees with no WHERE runs it immediately. Their advantage is scriptability (exactly what 6.4's clone/restore commands and 7.5's batch loads rely on) and that they're always available over SSH when a GUI tool isn't. A few defensive habits: MySQL's --safe-updates (or -U) flag refuses UPDATE/DELETE without a WHERE or LIMIT (the CLI equivalent of sql_safe_updates, 0.4); psql's \\set ON_ERROR_STOP on halts a script on the first error instead of plowing through the rest, which matters when a multi-statement script partially fails (6.4's half-migrated-database scenario); and piping a script through less or cat to read it fully before piping it into the client (mysql < script.sql) is worth the extra ten seconds every time, especially for a script someone else handed you.\n\n| Choosing a client for the task | Task | Good fit | Why |\n|---|---|---|---|\n| Ad hoc exploration, autocomplete, ER diagrams | GUI (DBeaver, Workbench, pgAdmin) | Visual schema browsing (8.6), grid editing for quick checks | Scripted/repeatable runs, CI, automation |\n| CLI (mysql, psql) | Scriptable, no GUI dependency, works over SSH | Cross-dialect work (MySQL one day, PostgreSQL the next) | DBeaver or Azure Data Studio |\n| One tool, consistent shortcuts, across many dialects | Reviewing a colleague's query before running it | Any editor with syntax highlighting, read it before connecting | Catches an obviously missing WHERE before execution |\n",
  "blocks": [
    {
      "id": "sql-11-1-md-0",
      "type": "overview",
      "heading": "The client is not the database",
      "content": "Everything in this manual has been the SQL itself. This chapter is about the program you type it into: a GUI tool (DBeaver, MySQL Workbench, pgAdmin, Azure Data Studio, TablePlus), a command-line shell (mysql, psql, sqlcmd), or an IDE plugin (JetBrains' database tool, VS Code extensions). The client is a lens on the database, and it has its own settings, quirks and failure modes that are easy to mistake for database behaviour. A tester who understands the client avoids a whole category of false alarms.",
      "order": 0
    },
    {
      "id": "sql-11-1-md-1",
      "type": "overview",
      "heading": "Connecting: know exactly what you're pointed at",
      "content": "Every connection profile has a host, port, database/schema, user and (often) a default character set and time zone. Before running anything on an unfamiliar or shared connection, confirm where you actually are:\n```sql\nSELECT DATABASE() AS db, @@hostname AS host, @@port AS port, CURRENT_USER() AS you, @@version AS version;\n-- PostgreSQL: SELECT current_database(), inet_server_addr(), inet_server_port(), current_user, version();\n-- SQL Server: SELECT DB_NAME(), @@SERVERNAME, SYSTEM_USER, @@VERSION;\n```\n\nSave connection profiles with unmistakable names and colours (\"QA — safe\", \"STAGING — careful\", \"PROD — read-only, ask first\") rather than generic labels like \"MySQL 1\" and \"MySQL 2.\" Most GUI clients let you colour-code a connection's tab or toolbar; use production red and never let it look like anything else. The single most common way testers cause real damage is running a script against the wrong tab because two connections looked identical.",
      "order": 1
    },
    {
      "id": "sql-11-1-md-2",
      "type": "overview",
      "heading": "Autocommit and transaction mode, in the client's own UI",
      "content": "0.4 and 7.3 covered the concept; here's where to check and change it in practice. DBeaver shows an \"Auto-commit\" toggle in the SQL editor toolbar (a green checkmark icon) and lets you switch to manual commit per connection. MySQL Workbench has the same toggle in the results grid toolbar. psql defaults to autocommit-per-statement unless you type BEGIN; mysql CLI is autocommit by default (\\c doesn't change it — use SET autocommit=0 or START TRANSACTION). Before any UPDATE/DELETE on a shared database, glance at that toggle. If manual-commit mode is on and you forget to commit or roll back, you can leave a transaction open and block other sessions for the rest of your coffee break (7.3's \"hung QA environment\" story).",
      "order": 2
    },
    {
      "id": "sql-11-1-md-3",
      "type": "overview",
      "heading": "Query execution: run one statement, not the whole script",
      "content": "GUI editors typically bind two different shortcuts: \"execute the statement under the cursor\" and \"execute the whole script/file.\" It's easy to hit the wrong one and run every DELETE in a scratch file at once. Get comfortable with your client's statement-boundary shortcut (often Ctrl+Enter / Cmd+Enter for \"current statement\" versus Ctrl+Shift+Enter / a toolbar \"execute script\" button for \"everything\"), and prefer running one statement at a time when experimenting on a shared database, even inside a transaction.",
      "order": 3
    },
    {
      "id": "sql-11-1-md-4",
      "type": "overview",
      "heading": "Result grids: what you see is not always what's stored",
      "content": "Client grids apply their own formatting and truncation, and treating the grid's display as ground truth causes false bug reports:\nRow limits. Many GUI tools cap the visible result set (DBeaver defaults to a \"fetch size\" and a max rows setting, often 200), silently paginating with a \"Fetch more\" link at the bottom. A COUNT(*) you never ran can look like \"only 200 rows exist\" if you just scroll the grid and stop.\nColumn truncation. Long text and TEXT/BLOB columns are often shown abbreviated with an ellipsis, or as (BLOB)/<binary> placeholders. A trailing space or an invisible character (2.5) is invisible in a truncated cell.\nNULL vs empty string rendering. Some clients show both as a blank cell, some show NULL in italics or grey, some show <null> literally. Confirm your client's convention before concluding a column \"has no value\" — DBeaver, for instance, is configurable on this point, and the default can differ between its SQL editor grid and its data editor grid.\nDate/time display and rounding. A client may apply its own locale formatting or truncate seconds/microseconds on screen while the stored value is more precise; don't diagnose a \"the time is wrong\" bug from the grid alone — re-query with explicit formatting (DATE_FORMAT, TO_CHAR) to see the raw stored value.\nNumeric display. A grid may show 57500 for a DECIMAL(10,2) value of 57500.00, hiding trailing zero precision that might matter for a rounding investigation (2.4).\nWhen something in a grid looks suspicious, the fix is always the same: query for it explicitly (SELECT HEX(col), SELECT LENGTH(col), SELECT CAST(col AS CHAR)) rather than trusting the rendered cell, exactly the technique 2.5 used to unmask invisible whitespace.",
      "order": 4
    },
    {
      "id": "sql-11-1-md-5",
      "type": "overview",
      "heading": "Editing data directly in a grid",
      "content": "Most GUI clients let you double-click a cell and edit it, generating an UPDATE behind the scenes when you commit the change. This is convenient and dangerous in equal measure: it's easy to fat-finger an edit into the wrong row (especially in an unsorted or unfiltered grid where row order isn't guaranteed, 2.2), and some tools generate the UPDATE's WHERE clause from all visible columns rather than the primary key, which can silently match and update more rows than you intended if the \"identifying\" columns aren't actually unique. Prefer writing an explicit UPDATE ... WHERE id = ... statement over inline grid editing whenever the change matters, precisely so you control the WHERE clause yourself (0.4's safe-workflow habit).",
      "order": 5
    },
    {
      "id": "sql-11-1-md-6",
      "type": "overview",
      "heading": "Autocomplete and object caches",
      "content": "GUI clients cache the list of tables/columns for autocomplete, and that cache can go stale after a schema migration (6.4) — autocomplete still offering a dropped column, or not yet offering a newly added one, is a client artifact, not evidence the migration failed. Refresh the schema cache (usually F5 or a right-click \"Refresh\") before trusting what autocomplete implies about current structure, and always verify structural questions with an actual information_schema query or SHOW CREATE TABLE, not the autocomplete list.",
      "order": 6
    },
    {
      "id": "sql-11-1-md-7",
      "type": "overview",
      "heading": "Multiple result sets and multi-statement scripts",
      "content": "Running several SELECTs (or a stored procedure that returns more than one result set, 8.3) in one execution produces several grids or tabs. It's easy to look at only the first and miss that a later statement returned unexpected rows, or errored silently while the client moved on. Check whether your client shows a per-statement status/error indicator, and get in the habit of scrolling through every result tab, not just the first, after a multi-statement run.",
      "order": 7
    },
    {
      "id": "sql-11-1-md-8",
      "type": "overview",
      "heading": "Keyboard shortcuts worth learning",
      "content": "Beyond \"execute statement\" vs \"execute script,\" the shortcuts that save the most time are: format/beautify SQL (turns a one-line paste into readable indentation, useful when a developer pastes a query in Slack), comment/uncomment a line or block (for toggling a WHERE clause on and off while debugging a query, as 2.1 recommended), and \"explain plan\" (often bound directly, skipping the need to type EXPLAIN by hand, 8.5). Different clients bind these differently; five minutes with your specific tool's shortcut cheat sheet pays for itself within the first debugging session.",
      "order": 8
    },
    {
      "id": "sql-11-1-md-9",
      "type": "overview",
      "heading": "Command-line clients: no safety net by default",
      "content": "The mysql, psql and sqlcmd CLIs have no grid, no autocomplete cache, and (by default) no confirmation prompts — they execute exactly what you type, and pressing Enter after a DELETE FROM employees with no WHERE runs it immediately. Their advantage is scriptability (exactly what 6.4's clone/restore commands and 7.5's batch loads rely on) and that they're always available over SSH when a GUI tool isn't. A few defensive habits: MySQL's --safe-updates (or -U) flag refuses UPDATE/DELETE without a WHERE or LIMIT (the CLI equivalent of sql_safe_updates, 0.4); psql's \\set ON_ERROR_STOP on halts a script on the first error instead of plowing through the rest, which matters when a multi-statement script partially fails (6.4's half-migrated-database scenario); and piping a script through less or cat to read it fully before piping it into the client (mysql < script.sql) is worth the extra ten seconds every time, especially for a script someone else handed you.\n\n| Choosing a client for the task | Task | Good fit | Why |\n|---|---|---|---|\n| Ad hoc exploration, autocomplete, ER diagrams | GUI (DBeaver, Workbench, pgAdmin) | Visual schema browsing (8.6), grid editing for quick checks | Scripted/repeatable runs, CI, automation |\n| CLI (mysql, psql) | Scriptable, no GUI dependency, works over SSH | Cross-dialect work (MySQL one day, PostgreSQL the next) | DBeaver or Azure Data Studio |\n| One tool, consistent shortcuts, across many dialects | Reviewing a colleague's query before running it | Any editor with syntax highlighting, read it before connecting | Catches an obviously missing WHERE before execution |",
      "order": 9
    }
  ],
  "advantages": [
    "11.1 Running Queries via DB Clients — 11."
  ],
  "limitations": [
    "11.1 Running Queries via DB Clients is this Part's slice only; later chapters go deeper rather than repeating this one."
  ],
  "exercises": [],
  "resourceLinks": [],
  "steps": [],
  "learn": []
} as ChapterRecord;
