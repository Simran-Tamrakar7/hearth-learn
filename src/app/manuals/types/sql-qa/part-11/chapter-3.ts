import type { ChapterRecord } from "../../../types";

/** 11.3 Exporting Query Results for Bug Reports */
export const chapter = {
  "id": "sql-11-3-exporting-query-results-for-bug-reports",
  "title": "11.3 Exporting Query Results for Bug Reports",
  "minutes": 22,
  "level": "advanced",
  "phase": "Part 11 · Tools & Workflow",
  "partName": "Part 11 · Tools & Workflow",
  "overviewText": "Every chapter from 4.3 onward has ended defect-hunting queries with some version of \"attach this as evidence.\" This chapter is the mechanics: how to actually get a query result out of the database and into a bug tracker, a chat message, or a shared document, in a form the reader can use without re-running anything themselves. A screenshot of a result grid, cropped to fit, with the query nowhere in sight, is close to useless: the reader can't verify it, can't re-run it after a fix, and often can't even tell which environment it came from. Useful evidence consistently includes four things: the exact query that produced it (so it's reproducible), the environment and timestamp it was run against",
  "why": "11.3 Exporting Query Results for Bug Reports is how a tester proves stored state, not just the screen. Every chapter from 4.3 onward has ended defect-hunting queries with some version of \"attach this as evidence.\" This chapter is the mechanics: how to actually get a query result out of the database and into a bug tracker, a chat message, or a shared document, in a form the reader can use without re-running anything themselves.",
  "when": "Open this chapter when you are on the Tools & Workflow path and need Exporting Query Results for Bug Reports against the hrms_practice schema, a shared QA database, or a Bizlevate HRMS screen.",
  "practical": {
    "app": "Bizlevate HRMS / hrms_practice",
    "scenario": "A leave, payroll, attendance, or appointment check depends on Exporting Query Results for Bug Reports.",
    "pass": "You apply Exporting Query Results for Bug Reports on the sample database (or a tagged QA row) and can say what empty vs non-empty results mean.",
    "fail": "You copy a blog query, skip Exporting Query Results for Bug Reports, or treat a UI success toast as proof the row is correct."
  },
  "tools": [],
  "customSummary": "- Evidence must survive outside the SQL client: a cropped grid screenshot with no visible query is close to useless.\n- Useful evidence has four parts: the exact query, environment and timestamp, the full relevant result (not cropped), and enough context to know what \"pass\" would look like.\n- CSV export is the default for tabular evidence; client-side export (GUI \"Export result set\") works even on restricted read-only accounts, unlike server-side INTO OUTFILE.\n- Formatted text/Markdown tables suit quick chat/ticket evidence for small results; most clients generate them directly.\n- Always include the exact query alongside any export: lets the fix be verified by re-running it, lets a reviewer catch a query mistake (e.g., join fan-out) instead of chasing a phantom bug, and documents the filters/assumptions in play.\n- JSON export suits nested/structured evidence (API responses, automated test attachments); watch Decimal/datetime serialization.\n- Redact PII (7.6) before evidence leaves the database — mask in the export query or restrict to IDs and non-sensitive columns, letting developers re-run against the controlled real environment for the sensitive view.\n- Screenshots are right only when the visual rendering itself is the bug, paired with a query result showing the correct expected value; a client grid screenshot should almost always be an export instead.\n- Full investigation package (the template implicit throughout Parts 4, 6, 8, 10): summary, query with environment/time, full result, severity/impact, a stated hypothesis (not a claim), and what \"fixed\" (zero rows, or the accepted baseline) looks like.",
  "contentMarkdown": "## Evidence needs to survive outside the SQL client\nEvery chapter from 4.3 onward has ended defect-hunting queries with some version of \"attach this as evidence.\" This chapter is the mechanics: how to actually get a query result out of the database and into a bug tracker, a chat message, or a shared document, in a form the reader can use without re-running anything themselves.\n## What makes exported evidence useful versus useless\nA screenshot of a result grid, cropped to fit, with the query nowhere in sight, is close to useless: the reader can't verify it, can't re-run it after a fix, and often can't even tell which environment it came from. Useful evidence consistently includes four things: the exact query that produced it (so it's reproducible), the environment and timestamp it was run against (so staleness is visible, 6.5's drift concerns apply to evidence too), the full relevant result (not a cropped screenshot missing rows), and enough surrounding context (row counts, what an empty result would have meant) that the reader doesn't have to guess what \"success\" would have looked like.\n## CSV export: the default for tabular evidence\nEvery client supports exporting a result grid to CSV, and it's the right default for anything the reader might want to filter, sort, or paste into a spreadsheet themselves.\n```sql\n-- MySQL: server-side export (writes to the DB server's filesystem, needs FILE privilege)\nSELECT employee_id, pay_month, basic_salary, allowances, deductions, net_pay\nFROM payroll WHERE pay_month = '2026-08-01'\n```\n## INTO OUTFILE '/tmp/payroll_aug_evidence.csv'\nFIELDS TERMINATED BY ',' OPTIONALLY ENCLOSED BY '\"' LINES TERMINATED BY '\\n';\n\n```sql\n-- PostgreSQL (psql, client-side, works with a read-only account and no server filesystem access)\n```\n\\copy (SELECT employee_id, pay_month, basic_salary, net_pay FROM payroll WHERE pay_month = '2026-08-01') TO 'payroll_aug.csv' CSV HEADER\n\nMost GUI clients offer a one-click \"Export result set\" that produces the same thing without touching the server's filesystem at all (DBeaver, Workbench, pgAdmin all have this in the results grid's right-click menu or toolbar) — for a tester on a restricted, read-only account, this client-side export is usually the only option and the right default regardless, since it doesn't need any server-side file privilege. Always include a header row (FIELDS/HEADER above) so the reader doesn't have to guess which column is which by position.\n## Formatted text for chat and tickets\nFor a quick Slack message or a short evidence block inside a ticket description, a manually-formatted (or client-generated) text table is often more readable inline than an attached file, especially for small result sets — exactly the |-delimited tables used throughout this manual:\nemployee_id | pay_month  | copies\n2           | 2026-08-01 | 2\n\nMost clients can generate this directly (DBeaver: right-click a result → \"Advanced Copy\" → various text formats including Markdown tables), which is faster and less error-prone than typing it by hand.\n## Always include the query itself\nWhatever export format, paste the exact SQL alongside it — as a code block in the ticket, or as a comment header in the CSV/script file. This is non-negotiable evidence, for three reasons that echo points made throughout Parts 6 and 10: it lets the developer re-run the query themselves to confirm the fix later (closing the loop 10.2's baselines describe); it lets a reviewer spot a mistake in the query itself (an unintended LEFT JOIN fan-out, 4.1, that inflated a count) rather than chasing a phantom application bug; and it documents exactly which filters and assumptions were in play (which environment, which date range, which status values were included), all of which earlier chapters (6.1's \"which rules were applied,\" 10.2's check headers) have insisted on capturing.\n## JSON export for structured/nested evidence\nWhen the evidence needs to show a hierarchical shape (an API response body alongside the row that produced it, 9.1's comparison pattern) or will be consumed by another tool (attached to an automated test failure, as in 9.4's testInfo.attach example), JSON is often more appropriate than a flat CSV:\n```sql\n-- MySQL: build a JSON array directly in SQL\nSELECT JSON_ARRAYAGG(JSON_OBJECT(\n```\n| 'leave_request_id', leave_request_id, 'employee_id', employee_id, | 'status', status, 'days', days |\n|---|---|\n| )) AS evidence | FROM leave_requests WHERE status = 'approved' AND approved_by IS NULL; |\n\nOr, more commonly, format the result as JSON in whichever scripting language ran the query (Python's json.dumps(cursor.fetchall(), default=str), handling Decimal and datetime serialization, which is exactly the string-conversion issue 3.1 and 6.2 already flagged for those types when comparing values — the same care applies when serializing them for a report).\n## Redacting sensitive data before it leaves the database\nBefore attaching any export to a bug report, screenshot, or chat message, check it against 7.6's PII rules — a query result is exactly the kind of place real customer names, emails, salaries or national IDs leak into tools (ticket trackers, chat history) that were never meant to hold them. If the evidence must show real values to make the bug reproducible, either mask the sensitive columns in the export query itself (the same substitution/generalisation techniques from 7.6, applied ad hoc: CONCAT('Employee ', employee_id) instead of the real name) or restrict the evidence to non-sensitive columns plus IDs, and let the developer re-run the query themselves against the real environment where access is already controlled, rather than copying real PII into a less-controlled tool.\n## Screenshots: when they're the right choice, and how to make them useful\nA screenshot is appropriate when the visual rendering itself is the bug (a UI table showing the wrong value, a chart with a missing bar, 9.6) — in that case, a companion query result showing the correct expected value belongs alongside the screenshot, not instead of it, so the reader can see both \"what appeared\" and \"what should have appeared\" together. A screenshot of a raw SQL client result grid, on the other hand, is almost always worse than a proper export: it can't be re-parsed, may be cropped, and often has the query itself scrolled out of view. If a screenshot of a grid feels like the fastest option in the moment, take thirty extra seconds to export the same result as CSV or a formatted table instead — the time saved on the reader's end more than pays it back.\n## Packaging a full investigation (the format Parts 4, 6 and 10 have been building toward)\nFor a substantial finding — an integrity violation from 4.3's scoreboard, a migration defect from 6.1, an anomaly from 10.1 — a single query result is rarely the whole story. The complete package that those chapters have consistently pointed toward looks like:\nSummary: one or two sentences, in plain language, of what's wrong.\nThe query that finds it (with a comment noting which environment and date/time it was run).\nThe result, exported in full (not truncated), or a representative sample plus a total count if the full result is very large.\nSeverity and impact: what this affects downstream (money, an audit trail, a user-facing count) — 6.1's severity table and 10.2's severity grading are the template.\nA hypothesis about the cause, stated as a hypothesis, not a claim (6.1's \"root cause candidates,\" 8.5's escalation template both modeled this).\nWhat \"fixed\" looks like: the same query, re-run, returning zero rows (or the accepted baseline count from 10.2) — so there's no ambiguity about what closing the ticket requires.\nThis is exactly the shape used, in miniature, throughout the worked examples in Parts 4, 6, 8 and 10 — this chapter just names it explicitly as the reusable template it's always implicitly been.\n",
  "blocks": [
    {
      "id": "sql-11-3-md-0",
      "type": "overview",
      "heading": "Evidence needs to survive outside the SQL client",
      "content": "Every chapter from 4.3 onward has ended defect-hunting queries with some version of \"attach this as evidence.\" This chapter is the mechanics: how to actually get a query result out of the database and into a bug tracker, a chat message, or a shared document, in a form the reader can use without re-running anything themselves.",
      "order": 0
    },
    {
      "id": "sql-11-3-md-1",
      "type": "overview",
      "heading": "What makes exported evidence useful versus useless",
      "content": "A screenshot of a result grid, cropped to fit, with the query nowhere in sight, is close to useless: the reader can't verify it, can't re-run it after a fix, and often can't even tell which environment it came from. Useful evidence consistently includes four things: the exact query that produced it (so it's reproducible), the environment and timestamp it was run against (so staleness is visible, 6.5's drift concerns apply to evidence too), the full relevant result (not a cropped screenshot missing rows), and enough surrounding context (row counts, what an empty result would have meant) that the reader doesn't have to guess what \"success\" would have looked like.",
      "order": 1
    },
    {
      "id": "sql-11-3-md-2",
      "type": "overview",
      "heading": "CSV export: the default for tabular evidence",
      "content": "Every client supports exporting a result grid to CSV, and it's the right default for anything the reader might want to filter, sort, or paste into a spreadsheet themselves.\n```sql\n-- MySQL: server-side export (writes to the DB server's filesystem, needs FILE privilege)\nSELECT employee_id, pay_month, basic_salary, allowances, deductions, net_pay\nFROM payroll WHERE pay_month = '2026-08-01'\n```",
      "order": 2
    },
    {
      "id": "sql-11-3-md-3",
      "type": "overview",
      "heading": "INTO OUTFILE '/tmp/payroll_aug_evidence.csv'",
      "content": "FIELDS TERMINATED BY ',' OPTIONALLY ENCLOSED BY '\"' LINES TERMINATED BY '\\n';\n\n```sql\n-- PostgreSQL (psql, client-side, works with a read-only account and no server filesystem access)\n```\n\\copy (SELECT employee_id, pay_month, basic_salary, net_pay FROM payroll WHERE pay_month = '2026-08-01') TO 'payroll_aug.csv' CSV HEADER\n\nMost GUI clients offer a one-click \"Export result set\" that produces the same thing without touching the server's filesystem at all (DBeaver, Workbench, pgAdmin all have this in the results grid's right-click menu or toolbar) — for a tester on a restricted, read-only account, this client-side export is usually the only option and the right default regardless, since it doesn't need any server-side file privilege. Always include a header row (FIELDS/HEADER above) so the reader doesn't have to guess which column is which by position.",
      "order": 3
    },
    {
      "id": "sql-11-3-md-4",
      "type": "overview",
      "heading": "Formatted text for chat and tickets",
      "content": "For a quick Slack message or a short evidence block inside a ticket description, a manually-formatted (or client-generated) text table is often more readable inline than an attached file, especially for small result sets — exactly the |-delimited tables used throughout this manual:\nemployee_id | pay_month  | copies\n2           | 2026-08-01 | 2\n\nMost clients can generate this directly (DBeaver: right-click a result → \"Advanced Copy\" → various text formats including Markdown tables), which is faster and less error-prone than typing it by hand.",
      "order": 4
    },
    {
      "id": "sql-11-3-md-5",
      "type": "overview",
      "heading": "Always include the query itself",
      "content": "Whatever export format, paste the exact SQL alongside it — as a code block in the ticket, or as a comment header in the CSV/script file. This is non-negotiable evidence, for three reasons that echo points made throughout Parts 6 and 10: it lets the developer re-run the query themselves to confirm the fix later (closing the loop 10.2's baselines describe); it lets a reviewer spot a mistake in the query itself (an unintended LEFT JOIN fan-out, 4.1, that inflated a count) rather than chasing a phantom application bug; and it documents exactly which filters and assumptions were in play (which environment, which date range, which status values were included), all of which earlier chapters (6.1's \"which rules were applied,\" 10.2's check headers) have insisted on capturing.",
      "order": 5
    },
    {
      "id": "sql-11-3-md-6",
      "type": "overview",
      "heading": "JSON export for structured/nested evidence",
      "content": "When the evidence needs to show a hierarchical shape (an API response body alongside the row that produced it, 9.1's comparison pattern) or will be consumed by another tool (attached to an automated test failure, as in 9.4's testInfo.attach example), JSON is often more appropriate than a flat CSV:\n```sql\n-- MySQL: build a JSON array directly in SQL\nSELECT JSON_ARRAYAGG(JSON_OBJECT(\n```\n| 'leave_request_id', leave_request_id, 'employee_id', employee_id, | 'status', status, 'days', days |\n|---|---|\n| )) AS evidence | FROM leave_requests WHERE status = 'approved' AND approved_by IS NULL; |\n\nOr, more commonly, format the result as JSON in whichever scripting language ran the query (Python's json.dumps(cursor.fetchall(), default=str), handling Decimal and datetime serialization, which is exactly the string-conversion issue 3.1 and 6.2 already flagged for those types when comparing values — the same care applies when serializing them for a report).",
      "order": 6
    },
    {
      "id": "sql-11-3-md-7",
      "type": "overview",
      "heading": "Redacting sensitive data before it leaves the database",
      "content": "Before attaching any export to a bug report, screenshot, or chat message, check it against 7.6's PII rules — a query result is exactly the kind of place real customer names, emails, salaries or national IDs leak into tools (ticket trackers, chat history) that were never meant to hold them. If the evidence must show real values to make the bug reproducible, either mask the sensitive columns in the export query itself (the same substitution/generalisation techniques from 7.6, applied ad hoc: CONCAT('Employee ', employee_id) instead of the real name) or restrict the evidence to non-sensitive columns plus IDs, and let the developer re-run the query themselves against the real environment where access is already controlled, rather than copying real PII into a less-controlled tool.",
      "order": 7
    },
    {
      "id": "sql-11-3-md-8",
      "type": "overview",
      "heading": "Screenshots: when they're the right choice, and how to make them useful",
      "content": "A screenshot is appropriate when the visual rendering itself is the bug (a UI table showing the wrong value, a chart with a missing bar, 9.6) — in that case, a companion query result showing the correct expected value belongs alongside the screenshot, not instead of it, so the reader can see both \"what appeared\" and \"what should have appeared\" together. A screenshot of a raw SQL client result grid, on the other hand, is almost always worse than a proper export: it can't be re-parsed, may be cropped, and often has the query itself scrolled out of view. If a screenshot of a grid feels like the fastest option in the moment, take thirty extra seconds to export the same result as CSV or a formatted table instead — the time saved on the reader's end more than pays it back.",
      "order": 8
    },
    {
      "id": "sql-11-3-md-9",
      "type": "overview",
      "heading": "Packaging a full investigation (the format Parts 4, 6 and 10 have been building toward)",
      "content": "For a substantial finding — an integrity violation from 4.3's scoreboard, a migration defect from 6.1, an anomaly from 10.1 — a single query result is rarely the whole story. The complete package that those chapters have consistently pointed toward looks like:\nSummary: one or two sentences, in plain language, of what's wrong.\nThe query that finds it (with a comment noting which environment and date/time it was run).\nThe result, exported in full (not truncated), or a representative sample plus a total count if the full result is very large.\nSeverity and impact: what this affects downstream (money, an audit trail, a user-facing count) — 6.1's severity table and 10.2's severity grading are the template.\nA hypothesis about the cause, stated as a hypothesis, not a claim (6.1's \"root cause candidates,\" 8.5's escalation template both modeled this).\nWhat \"fixed\" looks like: the same query, re-run, returning zero rows (or the accepted baseline count from 10.2) — so there's no ambiguity about what closing the ticket requires.\nThis is exactly the shape used, in miniature, throughout the worked examples in Parts 4, 6, 8 and 10 — this chapter just names it explicitly as the reusable template it's always implicitly been.",
      "order": 9
    }
  ],
  "advantages": [
    "11.3 Exporting Query Results for Bug Reports — 11."
  ],
  "limitations": [
    "11.3 Exporting Query Results for Bug Reports is this Part's slice only; later chapters go deeper rather than repeating this one."
  ],
  "exercises": [],
  "resourceLinks": [],
  "steps": [],
  "learn": []
} as ChapterRecord;
