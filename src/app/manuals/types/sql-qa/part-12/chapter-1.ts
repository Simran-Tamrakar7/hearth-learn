import type { ChapterRecord } from "../../../types";

/** 12.1 Sample HRMS Practice Database (schema + seed data) */
export const chapter = {
  "id": "sql-12-1-sample-hrms-practice-database-schema-seed-data",
  "title": "12.1 Sample HRMS Practice Database (schema + seed data)",
  "minutes": 20,
  "level": "advanced",
  "phase": "Part 12 · Practice & Wrap-up",
  "partName": "Part 12 · Practice & Wrap-up",
  "overviewText": "Every earlier Part added a table or two as it went, which is the right way to learn a topic but a clumsy way to practice, since rebuilding the whole database means re-reading nine Parts of setup scripts. This section is the single consolidated script: the full schema from Parts 0 to 10 in one place, with seed data restored to a clean, documented starting point (no Part 10 defects still sitting in the tables). Run it on a throwaway database — this is exactly the audience 0.6 described. If you have been running the scripts from each Part in order, your database already has everything below (once Parts 4, 6, 8 and 10's cleanup scripts have run) — the audit below is just to confirm that. If you'",
  "why": "12.1 Sample HRMS Practice Database (schema + seed data) is how a tester proves stored state, not just the screen. Every earlier Part added a table or two as it went, which is the right way to learn a topic but a clumsy way to practice, since rebuilding the whole database means re-reading nine Parts of setup scripts.",
  "when": "Open this chapter when you are on the Practice & Wrap-up path and need Sample HRMS Practice Database (schema + seed data) against the hrms_practice schema, a shared QA database, or a Bizlevate HRMS screen.",
  "practical": {
    "app": "Bizlevate HRMS / hrms_practice",
    "scenario": "A leave, payroll, attendance, or appointment check depends on Sample HRMS Practice Database (schema + seed data).",
    "pass": "You apply Sample HRMS Practice Database (schema + seed data) on the sample database (or a tagged QA row) and can say what empty vs non-empty results mean.",
    "fail": "You copy a blog query, skip Sample HRMS Practice Database (schema + seed data), or treat a UI success toast as proof the row is correct."
  },
  "tools": [],
  "customSummary": "- One consolidated script/baseline: full Parts 0–10 schema with seed data reset to a clean, defect-free starting point (Part 10's planted defects excluded).\n- Rebuild order: Part 0.6 core schema, Part 7 services/customers/appointments/balances, Part 8 lookup/documents/view/trigger/procedure, Part 10 reference tables only (not its defect inserts).\n- Consolidated ER overview diagram; full detail lives in Appendix E.\n- Audit query gives an exact 15-table row-count fingerprint to confirm your database matches the baseline before starting exercises; mismatches usually mean a Part's cleanup script wasn't run.\n- Deliberately no planted defects: exercises ask you to write a check against clean data (expect zero violations), then plant a defect yourself and confirm the check catches it — the mutation-testing discipline from 10.2, applied as practice.",
  "contentMarkdown": "## Why this section exists\nEvery earlier Part added a table or two as it went, which is the right way to learn a topic but a clumsy way to practice, since rebuilding the whole database means re-reading nine Parts of setup scripts. This section is the single consolidated script: the full schema from Parts 0 to 10 in one place, with seed data restored to a clean, documented starting point (no Part 10 defects still sitting in the tables). Run it on a throwaway database — this is exactly the audience 0.6 described.\n## Building it from scratch\nIf you have been running the scripts from each Part in order, your database already has everything below (once Parts 4, 6, 8 and 10's cleanup scripts have run) — the audit below is just to confirm that. If you're starting fresh, or want a guaranteed-consistent baseline before the exercises, drop and recreate:\n```sql\nDROP DATABASE IF EXISTS hrms_practice;\nCREATE DATABASE hrms_practice;\nUSE hrms_practice;\n```\n\nThen run, in order: the Part 0.6 core schema and seed (departments, employees, leave_types, leave_requests, attendance, payroll), Part 7's services/customers/appointments/leave_balances setup, Part 8's ref_payroll_status, employee_documents, the view/trigger/procedure block, and Part 10's company_holidays and leave_status_transitions reference tables (skip Part 10's defect-planting inserts — those exist to be found and cleaned, not to form the baseline). If any of that sounds unfamiliar, it means an earlier Part's script or cleanup wasn't run; the audit query below tells you exactly what's missing.\nThe consolidated ER overview\n| departments ──< employees >── leave_requests >── leave_status_history | │  │                │ | │  │                └──< (self-ref: approved_by → employees) |\n|---|---|---|\n| │  └──< attendance | │  └──< payroll | │  └──< leave_balances >── leave_types |\n| │  └──< employee_documents | │  └──< appointments (as provider) >── appointment_ledger | └── (self-ref: manager_id → employees) |\n\n## customers ──< appointments >── services\n\n(Full detail per table — columns, keys, constraints — is in Appendix E. This is the shape to hold in your head while doing the exercises.)\nAudit query: confirm your database matches this baseline\n```sql\nSELECT 'employees' AS tbl, COUNT(*) AS rows_ FROM employees\n```\n| UNION ALL SELECT 'departments', COUNT(*) FROM departments | UNION ALL SELECT 'leave_types', COUNT(*) FROM leave_types |\n|---|---|\n| UNION ALL SELECT 'leave_requests', COUNT(*) FROM leave_requests | UNION ALL SELECT 'leave_status_history', COUNT(*) FROM leave_status_history |\n| UNION ALL SELECT 'leave_balances', COUNT(*) FROM leave_balances | UNION ALL SELECT 'attendance', COUNT(*) FROM attendance |\n| UNION ALL SELECT 'payroll', COUNT(*) FROM payroll | UNION ALL SELECT 'services', COUNT(*) FROM services |\n| UNION ALL SELECT 'customers', COUNT(*) FROM customers | UNION ALL SELECT 'appointments', COUNT(*) FROM appointments |\n| UNION ALL SELECT 'employee_documents', COUNT(*) FROM employee_documents | UNION ALL SELECT 'ref_payroll_status', COUNT(*) FROM ref_payroll_status |\n| UNION ALL SELECT 'company_holidays', COUNT(*) FROM company_holidays | UNION ALL SELECT 'leave_status_transitions', COUNT(*) FROM leave_status_transitions; |\n\n| tbl                      \\| rows_ | employees                \\| 5 | departments              \\| 4 | leave_types              \\| 2 |\n|---|---|---|---|\n| leave_requests           \\| 9 | leave_status_history     \\| 12 | leave_balances           \\| 6 | attendance               \\| 8 |\n| payroll                  \\| 7 | services                 \\| 3 | customers                \\| 3 | appointments             \\| 3 |\n| employee_documents       \\| 4 | ref_payroll_status       \\| 4 | company_holidays         \\| 2 | leave_status_transitions \\| 5 |\n\nThis is the row-count fingerprint the exercises below assume. If your numbers differ, before doing any exercise, work out why — that diagnostic exercise (comparing your actual counts against this table) is itself good practice for 6.1's \"reconcile before you trust\" habit. A common cause is a Part's demo/defect rows (Part 10's 101+ IDs, for instance) still present; re-run that Part's cleanup script.\n## A note on what's deliberately still \"messy\"\nUnlike Part 10's setup, this baseline has no planted defects — every stored value is internally consistent (net pay matches components, approved requests have approvers, history chains are unbroken). This is intentional: the exercises below ask you to write the check first, run it against clean data and confirm zero violations, and then plant a defect yourself and confirm the check catches it — which is precisely the mutation-testing discipline 10.2 taught for validating your own sanity checks, now applied as a learning exercise rather than a one-off habit.\n",
  "blocks": [
    {
      "id": "sql-12-1-md-0",
      "type": "overview",
      "heading": "Why this section exists",
      "content": "Every earlier Part added a table or two as it went, which is the right way to learn a topic but a clumsy way to practice, since rebuilding the whole database means re-reading nine Parts of setup scripts. This section is the single consolidated script: the full schema from Parts 0 to 10 in one place, with seed data restored to a clean, documented starting point (no Part 10 defects still sitting in the tables). Run it on a throwaway database — this is exactly the audience 0.6 described.",
      "order": 0
    },
    {
      "id": "sql-12-1-md-1",
      "type": "overview",
      "heading": "Building it from scratch",
      "content": "If you have been running the scripts from each Part in order, your database already has everything below (once Parts 4, 6, 8 and 10's cleanup scripts have run) — the audit below is just to confirm that. If you're starting fresh, or want a guaranteed-consistent baseline before the exercises, drop and recreate:\n```sql\nDROP DATABASE IF EXISTS hrms_practice;\nCREATE DATABASE hrms_practice;\nUSE hrms_practice;\n```\n\nThen run, in order: the Part 0.6 core schema and seed (departments, employees, leave_types, leave_requests, attendance, payroll), Part 7's services/customers/appointments/leave_balances setup, Part 8's ref_payroll_status, employee_documents, the view/trigger/procedure block, and Part 10's company_holidays and leave_status_transitions reference tables (skip Part 10's defect-planting inserts — those exist to be found and cleaned, not to form the baseline). If any of that sounds unfamiliar, it means an earlier Part's script or cleanup wasn't run; the audit query below tells you exactly what's missing.\nThe consolidated ER overview\n| departments ──< employees >── leave_requests >── leave_status_history | │  │                │ | │  │                └──< (self-ref: approved_by → employees) |\n|---|---|---|\n| │  └──< attendance | │  └──< payroll | │  └──< leave_balances >── leave_types |\n| │  └──< employee_documents | │  └──< appointments (as provider) >── appointment_ledger | └── (self-ref: manager_id → employees) |",
      "order": 1
    },
    {
      "id": "sql-12-1-md-2",
      "type": "overview",
      "heading": "customers ──< appointments >── services",
      "content": "(Full detail per table — columns, keys, constraints — is in Appendix E. This is the shape to hold in your head while doing the exercises.)\nAudit query: confirm your database matches this baseline\n```sql\nSELECT 'employees' AS tbl, COUNT(*) AS rows_ FROM employees\n```\n| UNION ALL SELECT 'departments', COUNT(*) FROM departments | UNION ALL SELECT 'leave_types', COUNT(*) FROM leave_types |\n|---|---|\n| UNION ALL SELECT 'leave_requests', COUNT(*) FROM leave_requests | UNION ALL SELECT 'leave_status_history', COUNT(*) FROM leave_status_history |\n| UNION ALL SELECT 'leave_balances', COUNT(*) FROM leave_balances | UNION ALL SELECT 'attendance', COUNT(*) FROM attendance |\n| UNION ALL SELECT 'payroll', COUNT(*) FROM payroll | UNION ALL SELECT 'services', COUNT(*) FROM services |\n| UNION ALL SELECT 'customers', COUNT(*) FROM customers | UNION ALL SELECT 'appointments', COUNT(*) FROM appointments |\n| UNION ALL SELECT 'employee_documents', COUNT(*) FROM employee_documents | UNION ALL SELECT 'ref_payroll_status', COUNT(*) FROM ref_payroll_status |\n| UNION ALL SELECT 'company_holidays', COUNT(*) FROM company_holidays | UNION ALL SELECT 'leave_status_transitions', COUNT(*) FROM leave_status_transitions; |\n\n| tbl                      \\| rows_ | employees                \\| 5 | departments              \\| 4 | leave_types              \\| 2 |\n|---|---|---|---|\n| leave_requests           \\| 9 | leave_status_history     \\| 12 | leave_balances           \\| 6 | attendance               \\| 8 |\n| payroll                  \\| 7 | services                 \\| 3 | customers                \\| 3 | appointments             \\| 3 |\n| employee_documents       \\| 4 | ref_payroll_status       \\| 4 | company_holidays         \\| 2 | leave_status_transitions \\| 5 |\n\nThis is the row-count fingerprint the exercises below assume. If your numbers differ, before doing any exercise, work out why — that diagnostic exercise (comparing your actual counts against this table) is itself good practice for 6.1's \"reconcile before you trust\" habit. A common cause is a Part's demo/defect rows (Part 10's 101+ IDs, for instance) still present; re-run that Part's cleanup script.",
      "order": 2
    },
    {
      "id": "sql-12-1-md-3",
      "type": "overview",
      "heading": "A note on what's deliberately still \"messy\"",
      "content": "Unlike Part 10's setup, this baseline has no planted defects — every stored value is internally consistent (net pay matches components, approved requests have approvers, history chains are unbroken). This is intentional: the exercises below ask you to write the check first, run it against clean data and confirm zero violations, and then plant a defect yourself and confirm the check catches it — which is precisely the mutation-testing discipline 10.2 taught for validating your own sanity checks, now applied as a learning exercise rather than a one-off habit.",
      "order": 3
    }
  ],
  "advantages": [
    "12.1 Sample HRMS Practice Database (schema + seed data) — 12."
  ],
  "limitations": [
    "12.1 Sample HRMS Practice Database (schema + seed data) is this Part's slice only; later chapters go deeper rather than repeating this one."
  ],
  "exercises": [],
  "resourceLinks": [],
  "steps": [],
  "learn": []
} as ChapterRecord;
