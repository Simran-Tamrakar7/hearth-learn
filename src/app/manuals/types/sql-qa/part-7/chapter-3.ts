import type { ChapterRecord } from "../../../types";

/** 7.3 Transactions (COMMIT, ROLLBACK) */
export const chapter = {
  "id": "sql-7-3-transactions-commit-rollback",
  "title": "7.3 Transactions (COMMIT, ROLLBACK)",
  "minutes": 22,
  "level": "intermediate",
  "phase": "Part 7 · DML, DDL & Test Data",
  "partName": "Part 7 · DML, DDL & Test Data",
  "overviewText": "Approving a leave request in the HRMS is several writes: set the request status, record the history row, deduct the leave balance. If the server crashes after the first and before the last, you'd have an approved request whose days were never deducted. A transaction wraps the group so that all its changes are made permanent together with COMMIT, or none are, with ROLLBACK. Until the commit, other sessions can't see your changes (with the default isolation settings, see 7.7). Change ROLLBACK to COMMIT and the three changes become permanent as one unit.",
  "why": "7.3 Transactions (COMMIT, ROLLBACK) is how a tester proves stored state, not just the screen. Approving a leave request in the HRMS is several writes: set the request status, record the history row, deduct the leave balance.",
  "when": "Open this chapter when you are on the DML, DDL & Test Data path and need Transactions (COMMIT, ROLLBACK) against the hrms_practice schema, a shared QA database, or a Bizlevate HRMS screen.",
  "practical": {
    "app": "Bizlevate HRMS / hrms_practice",
    "scenario": "A leave, payroll, attendance, or appointment check depends on Transactions (COMMIT, ROLLBACK).",
    "pass": "You apply Transactions (COMMIT, ROLLBACK) on the sample database (or a tagged QA row) and can say what empty vs non-empty results mean.",
    "fail": "You copy a blog query, skip Transactions (COMMIT, ROLLBACK), or treat a UI success toast as proof the row is correct."
  },
  "tools": [],
  "customSummary": "- A transaction makes multi-statement changes all-or-nothing; COMMIT/ROLLBACK; syntax START TRANSACTION / BEGIN / BEGIN TRANSACTION.\n- Autocommit is on by default; explicit transactions suspend it; check the client's mode.\n- Savepoints allow partial rollback.\n- Not rollbackable: MySQL DDL (implicit commit), consumed sequences/IDs, emails and files already sent.\n- Failure behaviour differs: PostgreSQL aborts the transaction on any error; MySQL only undoes the statement; SQL Server depends on XACT_ABORT.\n- Use for safe \"what if\" experiments; never leave transactions open (locks, error 1205; find via innodb_trx, PROCESSLIST, pg_stat_activity).\n- Two-session demo: uncommitted changes are invisible; a second writer waits.\n- Atomicity tests: force a mid-operation failure, then assert all-or-nothing with a consistency query (status, history, balance); check the success path moves all three.\n- You can't wrap UI tests in a rolled-back transaction because the app uses its own connection.",
  "contentMarkdown": "## A transaction is a group of statements that succeed or fail together\nApproving a leave request in the HRMS is several writes: set the request status, record the history row, deduct the leave balance. If the server crashes after the first and before the last, you'd have an approved request whose days were never deducted. A transaction wraps the group so that all its changes are made permanent together with COMMIT, or none are, with ROLLBACK. Until the commit, other sessions can't see your changes (with the default isolation settings, see 7.7).\n```sql\nSTART TRANSACTION;                                                          -- MySQL. PostgreSQL: BEGIN;  SQL Server: BEGIN TRANSACTION;\n```\n\n```sql\nUPDATE leave_requests SET status = 'approved', approved_by = 3 WHERE leave_request_id = 1;\nINSERT INTO leave_status_history (leave_request_id, old_status, new_status, changed_by, changed_at)\n       VALUES (1, 'pending', 'approved', 3, NOW());\nUPDATE leave_balances SET balance = balance - 3 WHERE employee_id = 2 AND leave_type_id = 1;\n```\n\n```sql\nSELECT leave_request_id, status FROM leave_requests WHERE leave_request_id = 1;   -- you see 'approved'\nROLLBACK;                                                                          -- ...and now it's as if none of it happened\nSELECT leave_request_id, status FROM leave_requests WHERE leave_request_id = 1;   -- 'pending' again\n```\n\nChange ROLLBACK to COMMIT and the three changes become permanent as one unit.\n## Autocommit: the default that surprises people\nMost clients run in autocommit mode, where every statement is its own tiny transaction that commits immediately, so a lone DELETE can't be rolled back. Explicitly starting a transaction (START TRANSACTION/BEGIN) suspends autocommit until you commit or roll back. Some tools show the mode in the toolbar (DBeaver's \"Auto\"/\"Manual\" commit button), and SELECT @@autocommit; shows it in MySQL. Check it before every change on a shared database. If you close a client with an open transaction, it is usually rolled back, but the tool may also ask you or, worse, commit silently, depending on its settings.\n\n| Savepoints: partial rollback | Inside a transaction you can mark a point and roll back only to it: | START TRANSACTION; |\n|---|---|---|\n| UPDATE payroll SET status = 'processed' WHERE pay_month = '2026-09-01'; | SAVEPOINT after_status; | DELETE FROM payroll WHERE pay_month = '2026-09-01';       -- oops, too much |\n\nSQL Server spells these SAVE TRANSACTION name and ROLLBACK TRANSACTION name.\n## What can't be rolled back\nIn MySQL, DDL statements (CREATE, ALTER, DROP, TRUNCATE, RENAME) and several administrative commands cause an implicit commit, which silently ends your transaction and makes everything before it permanent. A \"safe experiment\" that includes ALTER TABLE isn't safe. PostgreSQL and SQL Server allow DDL inside transactions (except a few operations), so there you can roll back a DROP TABLE. Also outside the transaction's reach: sequences and auto-increment counters (the numbers are used up even after rollback, so IDs have gaps), emails or messages the application already sent, and files written to disk.\n## When something fails inside a transaction\nIn PostgreSQL, one failed statement aborts the whole transaction, and every later command returns ERROR: current transaction is aborted, commands ignored until end of transaction block until you ROLLBACK. MySQL by default keeps the transaction alive after a statement error (only that statement is undone), and SQL Server depends on XACT_ABORT. When you script transactions, expect different behaviour per database, and in application code always ROLLBACK in the error path.\n## Using transactions for safe experiments\nThe workflow from 0.4 in full: open the transaction, run the change, verify with SELECT, then decide. It also lets you probe \"what if?\" questions harmlessly: try a constraint violation, run an UPDATE and look at the effect on a report, then roll everything back. A common variation is to run the bulk fix first as a SELECT, then in a transaction, then compare row counts to your prediction.\n## Never leave a transaction open\nAn open transaction holds locks on the rows it changed (7.7), so other sessions that touch those rows wait, and after a timeout they fail:\nERROR 1205 (HY000): Lock wait timeout exceeded; try restarting transaction     (MySQL, default 50 seconds)\n\nA classic team incident: a tester runs an UPDATE in manual-commit mode, goes to lunch, and the whole QA environment \"hangs\" for everyone touching leave requests. If you ever see unexplained timeouts, check for open transactions (SHOW PROCESSLIST; or SELECT * FROM information_schema.innodb_trx; in MySQL, pg_stat_activity in PostgreSQL).\n## Two sessions, two views: see it once\nOpen two connections (two tabs in your client) and run this alternately:\nSession A                                          Session B\n```sql\nSTART TRANSACTION;\nUPDATE leave_requests SET status='approved'\n  WHERE leave_request_id = 1;\n                                                   SELECT status FROM leave_requests WHERE leave_request_id = 1;\n                                                   → pending      (B can't see A's uncommitted change)\n                                                   UPDATE leave_requests SET status='rejected' WHERE leave_request_id = 1;\n                                                   → ...waits...  (blocked by A's lock; after 50 s: error 1205)\nCOMMIT;\n                                                   → the UPDATE now proceeds\n```\n\nTwo lessons: uncommitted work is invisible to others, and a second writer must wait for the first. This is the foundation for the concurrency tests in 7.7.\n## Testing an application's use of transactions (atomicity tests)\nYour job isn't to write the application's transactions, but to check that it wraps related writes correctly. The typical bug is a multi-step operation that isn't atomic, so a failure in step 3 leaves steps 1 and 2 behind. A test needs a way to make the operation fail midway (invalid input on the last step, a database error, a killed connection, or a fault-injection switch the developers provide in test builds). Then it asserts all-or-nothing with a consistency query:\n-- After a FAILED approval of request 1, all three must still be in their original state\n```sql\nSELECT lr.status                                                            AS request_status,\n       (SELECT COUNT(*) FROM leave_status_history h\n         WHERE h.leave_request_id = lr.leave_request_id AND h.new_status = 'approved') AS approved_history_rows,\n       (SELECT balance FROM leave_balances b\n         WHERE b.employee_id = lr.employee_id AND b.leave_type_id = 1)      AS balance\nFROM   leave_requests lr WHERE lr.leave_request_id = 1;\n-- expected after failure: pending | 0 | 18.0     (a partial result such as approved | 0 | 18.0 is a defect)\n```\n\nAlso test the success path the same way: after approval, status, history and balance must all have moved. Any state where only some did indicates a missing transaction.\n## Caveat: you can't wrap a UI test in a rolled-back transaction\nIt's tempting to open a transaction in the test, run the UI steps, assert, and roll back, as unit-test frameworks do. It doesn't work for end-to-end tests, because the application uses its own database connection, so its writes are committed independently of your transaction, and your transaction can't see them until they commit (7.7). Wrapping helps only for tests that talk to the database through the same connection (for example, a test that tests a stored procedure directly).\n",
  "blocks": [
    {
      "id": "sql-7-3-md-0",
      "type": "overview",
      "heading": "A transaction is a group of statements that succeed or fail together",
      "content": "Approving a leave request in the HRMS is several writes: set the request status, record the history row, deduct the leave balance. If the server crashes after the first and before the last, you'd have an approved request whose days were never deducted. A transaction wraps the group so that all its changes are made permanent together with COMMIT, or none are, with ROLLBACK. Until the commit, other sessions can't see your changes (with the default isolation settings, see 7.7).\n```sql\nSTART TRANSACTION;                                                          -- MySQL. PostgreSQL: BEGIN;  SQL Server: BEGIN TRANSACTION;\n```\n\n```sql\nUPDATE leave_requests SET status = 'approved', approved_by = 3 WHERE leave_request_id = 1;\nINSERT INTO leave_status_history (leave_request_id, old_status, new_status, changed_by, changed_at)\n       VALUES (1, 'pending', 'approved', 3, NOW());\nUPDATE leave_balances SET balance = balance - 3 WHERE employee_id = 2 AND leave_type_id = 1;\n```\n\n```sql\nSELECT leave_request_id, status FROM leave_requests WHERE leave_request_id = 1;   -- you see 'approved'\nROLLBACK;                                                                          -- ...and now it's as if none of it happened\nSELECT leave_request_id, status FROM leave_requests WHERE leave_request_id = 1;   -- 'pending' again\n```\n\nChange ROLLBACK to COMMIT and the three changes become permanent as one unit.",
      "order": 0
    },
    {
      "id": "sql-7-3-md-1",
      "type": "overview",
      "heading": "Autocommit: the default that surprises people",
      "content": "Most clients run in autocommit mode, where every statement is its own tiny transaction that commits immediately, so a lone DELETE can't be rolled back. Explicitly starting a transaction (START TRANSACTION/BEGIN) suspends autocommit until you commit or roll back. Some tools show the mode in the toolbar (DBeaver's \"Auto\"/\"Manual\" commit button), and SELECT @@autocommit; shows it in MySQL. Check it before every change on a shared database. If you close a client with an open transaction, it is usually rolled back, but the tool may also ask you or, worse, commit silently, depending on its settings.\n\n| Savepoints: partial rollback | Inside a transaction you can mark a point and roll back only to it: | START TRANSACTION; |\n|---|---|---|\n| UPDATE payroll SET status = 'processed' WHERE pay_month = '2026-09-01'; | SAVEPOINT after_status; | DELETE FROM payroll WHERE pay_month = '2026-09-01';       -- oops, too much |\n\nSQL Server spells these SAVE TRANSACTION name and ROLLBACK TRANSACTION name.",
      "order": 1
    },
    {
      "id": "sql-7-3-md-2",
      "type": "overview",
      "heading": "What can't be rolled back",
      "content": "In MySQL, DDL statements (CREATE, ALTER, DROP, TRUNCATE, RENAME) and several administrative commands cause an implicit commit, which silently ends your transaction and makes everything before it permanent. A \"safe experiment\" that includes ALTER TABLE isn't safe. PostgreSQL and SQL Server allow DDL inside transactions (except a few operations), so there you can roll back a DROP TABLE. Also outside the transaction's reach: sequences and auto-increment counters (the numbers are used up even after rollback, so IDs have gaps), emails or messages the application already sent, and files written to disk.",
      "order": 2
    },
    {
      "id": "sql-7-3-md-3",
      "type": "overview",
      "heading": "When something fails inside a transaction",
      "content": "In PostgreSQL, one failed statement aborts the whole transaction, and every later command returns ERROR: current transaction is aborted, commands ignored until end of transaction block until you ROLLBACK. MySQL by default keeps the transaction alive after a statement error (only that statement is undone), and SQL Server depends on XACT_ABORT. When you script transactions, expect different behaviour per database, and in application code always ROLLBACK in the error path.",
      "order": 3
    },
    {
      "id": "sql-7-3-md-4",
      "type": "overview",
      "heading": "Using transactions for safe experiments",
      "content": "The workflow from 0.4 in full: open the transaction, run the change, verify with SELECT, then decide. It also lets you probe \"what if?\" questions harmlessly: try a constraint violation, run an UPDATE and look at the effect on a report, then roll everything back. A common variation is to run the bulk fix first as a SELECT, then in a transaction, then compare row counts to your prediction.",
      "order": 4
    },
    {
      "id": "sql-7-3-md-5",
      "type": "overview",
      "heading": "Never leave a transaction open",
      "content": "An open transaction holds locks on the rows it changed (7.7), so other sessions that touch those rows wait, and after a timeout they fail:\nERROR 1205 (HY000): Lock wait timeout exceeded; try restarting transaction     (MySQL, default 50 seconds)\n\nA classic team incident: a tester runs an UPDATE in manual-commit mode, goes to lunch, and the whole QA environment \"hangs\" for everyone touching leave requests. If you ever see unexplained timeouts, check for open transactions (SHOW PROCESSLIST; or SELECT * FROM information_schema.innodb_trx; in MySQL, pg_stat_activity in PostgreSQL).",
      "order": 5
    },
    {
      "id": "sql-7-3-md-6",
      "type": "overview",
      "heading": "Two sessions, two views: see it once",
      "content": "Open two connections (two tabs in your client) and run this alternately:\nSession A                                          Session B\n```sql\nSTART TRANSACTION;\nUPDATE leave_requests SET status='approved'\n  WHERE leave_request_id = 1;\n                                                   SELECT status FROM leave_requests WHERE leave_request_id = 1;\n                                                   → pending      (B can't see A's uncommitted change)\n                                                   UPDATE leave_requests SET status='rejected' WHERE leave_request_id = 1;\n                                                   → ...waits...  (blocked by A's lock; after 50 s: error 1205)\nCOMMIT;\n                                                   → the UPDATE now proceeds\n```\n\nTwo lessons: uncommitted work is invisible to others, and a second writer must wait for the first. This is the foundation for the concurrency tests in 7.7.",
      "order": 6
    },
    {
      "id": "sql-7-3-md-7",
      "type": "overview",
      "heading": "Testing an application's use of transactions (atomicity tests)",
      "content": "Your job isn't to write the application's transactions, but to check that it wraps related writes correctly. The typical bug is a multi-step operation that isn't atomic, so a failure in step 3 leaves steps 1 and 2 behind. A test needs a way to make the operation fail midway (invalid input on the last step, a database error, a killed connection, or a fault-injection switch the developers provide in test builds). Then it asserts all-or-nothing with a consistency query:\n-- After a FAILED approval of request 1, all three must still be in their original state\n```sql\nSELECT lr.status                                                            AS request_status,\n       (SELECT COUNT(*) FROM leave_status_history h\n         WHERE h.leave_request_id = lr.leave_request_id AND h.new_status = 'approved') AS approved_history_rows,\n       (SELECT balance FROM leave_balances b\n         WHERE b.employee_id = lr.employee_id AND b.leave_type_id = 1)      AS balance\nFROM   leave_requests lr WHERE lr.leave_request_id = 1;\n-- expected after failure: pending | 0 | 18.0     (a partial result such as approved | 0 | 18.0 is a defect)\n```\n\nAlso test the success path the same way: after approval, status, history and balance must all have moved. Any state where only some did indicates a missing transaction.",
      "order": 7
    },
    {
      "id": "sql-7-3-md-8",
      "type": "overview",
      "heading": "Caveat: you can't wrap a UI test in a rolled-back transaction",
      "content": "It's tempting to open a transaction in the test, run the UI steps, assert, and roll back, as unit-test frameworks do. It doesn't work for end-to-end tests, because the application uses its own database connection, so its writes are committed independently of your transaction, and your transaction can't see them until they commit (7.7). Wrapping helps only for tests that talk to the database through the same connection (for example, a test that tests a stored procedure directly).",
      "order": 8
    }
  ],
  "advantages": [
    "7.3 Transactions (COMMIT, ROLLBACK) — 7."
  ],
  "limitations": [
    "7.3 Transactions (COMMIT, ROLLBACK) is this Part's slice only; later chapters go deeper rather than repeating this one."
  ],
  "exercises": [],
  "resourceLinks": [],
  "steps": [],
  "learn": []
} as ChapterRecord;
