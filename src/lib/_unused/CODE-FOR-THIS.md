# Unused (not wired to any page)

These files are not imported by the app. Kept so nothing is silently git-deleted.

- `playwrightData.ts` / `playwrightMasterData.ts` — old Playwright dumps; live manual is `src/app/manuals/_content/playwright/data.js`
- `bookParser.ts` / `realBooks/` — inline Gutenberg text; live library is `src/app/library/_content/_registry.ts`
