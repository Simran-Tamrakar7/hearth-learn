# Unused (not wired to any page)

These files are not imported by the app. Kept so nothing is silently git-deleted.

- `playwrightData.ts` / `playwrightMasterData.ts` — old Playwright dumps; live manual is `_lib/pathwise-data/manuals/playwright-*.js`
- `bookParser.ts` / `realBooks/` — inline Gutenberg text; live library is `content/library/_registry.ts`
