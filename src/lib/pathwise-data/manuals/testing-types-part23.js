import { ch, r } from '../helpers.js'

/** Part 23 — catalog TOC entries aligned with TESTING_TYPES_CHAPTERS 89–92 */
export const testingTypesPart23Chapters = [
  ch({
    id: 'tt-incremental-integration-testing',
    kind: 'guide',
    phase: 'Part 23 · Incremental Integration, Spike, Session & Voice',
    level: 'intermediate',
    title: 'Incremental Integration Testing',
    minutes: 25,
    durationLabel: 'Chapter 89',
    overviewText:
      'Incremental integration testing adds modules one at a time to an already-tested core — the opposite of Big Bang (Chapter 64), which combines everything at once.',
    why: 'A Big Bang failure can live in any module or any interaction. Adding one piece at a time shrinks the search to the module just joined, so defects are caught early.',
    when: 'Whenever modules have a clear dependency order and can be integrated gradually. Prefer this over Big Bang unless modules cannot be exercised until they all exist.',
    practical: {
      app: 'HRMS Attendance → Leave → Payroll Chain',
      scenario: 'Attendance is the tested core; Leave is added next; Payroll only after that step is green.',
      fail: 'Leave treats Attendance’s 8 hours as 0.33 days — caught at this step, before Payroll is wired in.',
      pass: 'Unit conversion is fixed at the Attendance–Leave boundary; Payroll inherits a known-good mapping.',
    },
    steps: [{ title: 'Add one module to a known-good core', body: 'Stop on red before the next join. Do not collapse stages into one Big Bang script.', doThis: 'Write three staged Selenium tests: Attendance, Attendance+Leave, then all three.' }],
    checklist: ['Core tested alone', 'Next module added only after green', 'Failure localized to the new boundary', 'Stages kept in CI, not collapsed'],
    resources: [r('tool', 'Selenium', 'https://selenium.dev', 'EN')],
  }),
  ch({
    id: 'tt-spike-testing',
    kind: 'guide',
    phase: 'Part 23 · Incremental Integration, Spike, Session & Voice',
    level: 'intermediate',
    title: 'Spike Testing',
    minutes: 25,
    durationLabel: 'Chapter 90',
    overviewText:
      'Spike testing is a sudden sharp jump in load — not the gradual ramp of load testing (Chapter 14) and not climbing past capacity to find a ceiling (Chapter 15).',
    why: 'Shift clock-in and launches multiply users in seconds. A system that passed a slow ramp can still drop requests or fail to recover when the same crowd arrives at once.',
    when: 'Before events that produce a near-instant crowd (9 AM clock-in, campaign drop), and after auto-scale, pool, or queue changes meant to absorb a surge.',
    practical: {
      app: 'HRMS 9 AM Shift Clock-In Spike',
      scenario: 'Jump from ~20 night-shift users to 400 clock-ins in 10 seconds, hold, then drop back.',
      fail: 'p95 past 12s, 18% 503s/timeouts; endpoint stays slow after the surge because the pool does not recover.',
      pass: 'Queue and pool absorb the jump; p95 under 2s, errors under 1%, baseline latency restored within 30s of drop-back.',
    },
    steps: [{ title: 'Jump, hold, drop, recover', body: 'A multi-minute climb is load; raising the peak to find a ceiling is stress.', doThis: 'Write a k6 stages array that reaches peak in ~10 seconds, not over a minute.' }],
    checklist: ['Jump is seconds, not a slow ramp', 'Peak SLAs checked', 'Recovery after drop-back checked', 'Not confused with load (Ch 14) or stress (Ch 15)'],
    resources: [
      r('tool', 'k6', 'https://k6.io', 'EN'),
      r('tool', 'Apache JMeter', 'https://jmeter.apache.org', 'EN'),
    ],
  }),
  ch({
    id: 'tt-session-based-testing',
    kind: 'guide',
    phase: 'Part 23 · Incremental Integration, Spike, Session & Voice',
    level: 'intermediate',
    title: 'Session-Based Testing',
    minutes: 25,
    durationLabel: 'Chapter 91',
    overviewText:
      'Session-based testing is structured, time-boxed exploratory testing (Chapter 27): charter, timer, notes, and a debrief so exploration is accountable.',
    why: 'Unscripted exploration finds bugs scripts miss, but without a charter and debrief there is no record of coverage or a required follow-up.',
    when: 'New or fast-changing features, as a planned supplement to regression, and whenever someone asks what exploratory testing actually covered.',
    practical: {
      app: 'HRMS New Rule Groups Feature',
      scenario: '90-minute charter on creating, assigning, and overlapping Rule Groups.',
      fail: 'Second group’s overtime cap is silently ignored when two groups are assigned to one team.',
      pass: 'Debrief files the bug; a scripted dual-group regression case is added so the finding survives the session.',
    },
    steps: [{ title: 'Charter, time-box, notes, debrief', body: 'A finding without a ticket or new scripted case is only a note.', doThis: 'Write one charter for Rule Groups and list the follow-up you would take from a finding.' }],
    checklist: ['Charter written', 'Timer running', 'Notes timestamped', 'Debrief with explicit follow-up'],
    resources: [r('tool', 'Rapid Reporter', 'https://github.com/testingcurator/RapidReporter', 'EN')],
  }),
  ch({
    id: 'tt-voice-conversational-ui-testing',
    kind: 'guide',
    phase: 'Part 23 · Incremental Integration, Spike, Session & Voice',
    level: 'intermediate',
    title: 'Voice / Conversational UI Testing',
    minutes: 25,
    durationLabel: 'Chapter 92',
    overviewText:
      'Voice and conversational UI testing verifies intents, multi-turn context, and the words the user receives — not only that the API behind the bot still works.',
    why: 'A correct leave API can still be wrapped by a bot that maps “sick leave” to casual, drops follow-up context, or guesses on unknown intents.',
    when: 'As soon as a bot or voice skill is in scope, and whenever intents, utterances, or backend contracts change — including fallback and correction turns.',
    practical: {
      app: 'HRMS Leave-Balance Chatbot',
      scenario: 'Botium convo: remaining leave, follow-up sick leave, plus a nonsense utterance.',
      fail: 'Follow-up starts a new session; first turn returns casual balance when the user asked for sick leave.',
      pass: 'Intents split, context kept, unknown-intent fallback passes on the same convo after the fix.',
    },
    steps: [{ title: 'Script the dialogue, including fallback', body: 'Assert the bot’s words and context, not only HTTP 200.', doThis: 'Write a two-turn leave-balance convo plus one unknown-intent turn.' }],
    checklist: ['Happy-path multi-turn convo', 'Unknown intent fallback', 'Context kept on follow-up', 'Utterance variants, not one canonical phrase'],
    resources: [r('tool', 'Botium Community Edition', 'https://github.com/codeforequity-at/botium-core', 'EN')],
  }),
]
