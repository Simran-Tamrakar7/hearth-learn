/* ============================================================================
 * HEADING: SHARED — SHIM — library catalog
 * Not a page. These pages all use this same file:
 *   /library
 * Edit the real file: content/library/_registry.ts
 * Changing this file changes all of those pages at once.
 * ========================================================================== */

export * from "@content/library/_registry";
